window.TGG_DATA = {"catalogs":{"playKits":[{"id":"horde-survival","label":"Horde Survival","verbs":["survive","move","auto-attack","collect xp","choose upgrades"],"bestFor":["coffee","virus","monster","office","stress","battle"],"template":"horde-survival.html","requiredMechanics":["upgrade-cards","enemy-variety"],"baseScore":10},{"id":"runner-lane","label":"Lane Runner","verbs":["switch lanes","dodge","collect","hit gates"],"bestFor":["racing","adventure","escape","subway","delivery"],"template":"runner-lane.html","requiredMechanics":["risk-reward"],"baseScore":8},{"id":"case-deduction","label":"Case Deduction","verbs":["read evidence","file clues","infer","choose interpretation","manage trust"],"bestFor":["fortune","divination","oracle","tarot","astrology","detective","mystery","consulting","diagnosis","算命","占卜","塔罗","星盘","八字","推理","侦探","咨询","诊断"],"template":"case-deduction.html","requiredMechanics":["trust-pressure","contradiction-mark"],"baseScore":12},{"id":"tower-defense","label":"Tower Defense","verbs":["place towers","hold lanes","survive waves","upgrade defense"],"bestFor":["tower","defense","guard","protect","siege","base","city","cafe","office","塔防","防守","守护","保卫","基地","城市","咖啡店"],"template":"tower-defense.html","requiredMechanics":["tower-placement","wave-defense"],"baseScore":11},{"id":"deck-builder","label":"Deck Builder","verbs":["draw cards","spend energy","play combos","build deck"],"bestFor":["card","deck","builder","strategy","business","negotiation","battle","tarot","卡牌","构筑","牌组","商业","谈判","策略"],"template":"deck-builder.html","requiredMechanics":["card-energy","card-combo"],"baseScore":11},{"id":"match-puzzle","label":"Match Puzzle","verbs":["match tiles","clear groups","chain bonuses","solve board"],"bestFor":["match","puzzle","tile","clear","gem","coffee","消除","连线","方块","宝石","咖啡豆"],"template":"match-puzzle.html","requiredMechanics":["match-chain","board-clear"],"baseScore":10},{"id":"physics-arc","label":"Physics Arc","verbs":["aim","drag","release","bounce","hit target"],"bestFor":["throw","arc","slingshot","rescue","launch","physics","投掷","弹射","救援","发射","物理"],"template":"physics-arc.html","requiredMechanics":["arc-aim","limited-shots"],"baseScore":10},{"id":"rhythm-tap","label":"Rhythm Tap","verbs":["tap timing","keep beat","chain perfects","avoid misses"],"bestFor":["rhythm","music","beat","tap","dance","timing","节奏","音乐","点击","打拍","时机"],"template":"rhythm-tap.html","requiredMechanics":["beat-timing","combo-streak"],"baseScore":10},{"id":"platform-climb","label":"Platform Climb","verbs":["move","jump","land","climb","avoid hazards"],"bestFor":["platform","jump","climb","adventure","tower","escape","平台","跳跃","爬塔","攀爬","冒险","逃离"],"template":"platform-climb.html","requiredMechanics":["platform-jump","rising-pressure"],"baseScore":10},{"id":"idle-automation","label":"Idle Automation","verbs":["produce","upgrade","automate","remove bottlenecks"],"bestFor":["idle","automation","factory","business","shop","cafe","growth","放置","自动化","经营","工厂","店铺","增长","咖啡店"],"template":"idle-automation.html","requiredMechanics":["production-loop","bottleneck-upgrade"],"baseScore":10},{"id":"grid-tactics","label":"Grid Tactics","verbs":["move on grid","control zones","block enemies","plan turns"],"bestFor":["grid","tactics","strategy","contain","virus","board","围堵","战术","棋盘","格子","策略","病毒"],"template":"grid-tactics.html","requiredMechanics":["grid-move","area-control"],"baseScore":10},{"id":"merge-drop","label":"Merge Drop","verbs":["drop tile","merge equals","chain","grow number"],"bestFor":["merge","2048","watermelon","number","合成","数字","西瓜"],"template":"merge-drop.html","requiredMechanics":["merge-grow"],"baseScore":11},{"id":"tile-match","label":"Tile Match","verbs":["tap tile","fill tray","clear three","unstack"],"bestFor":["match","tile","mahjong","sheep","三消","叠叠","羊","连连看"],"template":"tile-match.html","requiredMechanics":["tile-tray"],"baseScore":11},{"id":"brick-breaker","label":"Brick Breaker","verbs":["move paddle","bounce ball","break brick"],"bestFor":["brick","ball","paddle","arcade","打砖块","弹球","街机"],"template":"brick-breaker.html","requiredMechanics":["paddle-bounce"],"baseScore":10},{"id":"flappy-gap","label":"Flappy Gap","verbs":["tap","flap","dodge pipe"],"bestFor":["flappy","fly","bird","dodge","飞","鸟","躲","像素鸟"],"template":"flappy-gap.html","requiredMechanics":["tap-flap"],"baseScore":10}],"styleKits":[{"id":"toy-diorama","label":"Toy Diorama","bestFor":["coffee","toy","table","cute","object"],"colors":{"backgroundA":"#211610","backgroundB":"#6b3f25","accent":"#f59e0b","good":"#34d399","bad":"#fb7185","ink":"#fff7df"},"texture":"chunky shapes, soft shadows, tabletop lighting","baseScore":9},{"id":"pixel-neon","label":"Pixel Neon","bestFor":["computer","virus","racing","cyber","night"],"colors":{"backgroundA":"#07111f","backgroundB":"#111827","accent":"#22d3ee","good":"#a3e635","bad":"#fb7185","ink":"#f8fafc"},"texture":"grid, scanlines, square particles, neon glow","baseScore":9},{"id":"paper-cut","label":"Paper Cut","bestFor":["adventure","forest","book","map","cozy"],"colors":{"backgroundA":"#3f2a1d","backgroundB":"#b87942","accent":"#f2d28d","good":"#60a5fa","bad":"#ef4444","ink":"#fff6d4"},"texture":"layered silhouettes, warm paper, flat shadows","baseScore":8},{"id":"occult-archive","label":"Occult Archive","bestFor":["fortune","divination","oracle","tarot","astrology","bazi","mystery","premium","dark","算命","占卜","塔罗","星盘","八字","命理"],"colors":{"backgroundA":"#130f0d","backgroundB":"#211916","accent":"#c9a35b","good":"#87a978","bad":"#9f3937","ink":"#eadfc8"},"texture":"dark paper, brass ruling, restrained ritual UI, archival serif labels","baseScore":11},{"id":"noir-ledger","label":"Noir Ledger","bestFor":["detective","mystery","case","risk","consulting","premium","推理","侦探","案件","咨询"],"colors":{"backgroundA":"#101315","backgroundB":"#1f2527","accent":"#d2b36f","good":"#8db596","bad":"#b7524b","ink":"#ece4d6"},"texture":"black case files, thin gold dividers, evidence-board restraint","baseScore":10},{"id":"clinical-ritual","label":"Clinical Ritual","bestFor":["diagnosis","consulting","analysis","premium","serious","诊断","咨询","分析"],"colors":{"backgroundA":"#121817","backgroundB":"#26302d","accent":"#d6b15f","good":"#7fb39d","bad":"#b55f5a","ink":"#edf0e8"},"texture":"diagnostic panels, quiet ritual marks, sober consulting dashboard","baseScore":9}],"mechanicKits":[{"id":"upgrade-cards","label":"Upgrade Cards","bestFor":["horde-survival","battle","roguelite"],"summary":"XP fills a bar, level-up pauses the game, player picks one of three upgrades.","baseScore":10},{"id":"enemy-variety","label":"Enemy Variety","bestFor":["horde-survival","chaos","survive"],"summary":"Enemy set includes fast, tank, charger, splitter, and elite behavior.","baseScore":9},{"id":"risk-reward","label":"Risk Reward","bestFor":["runner-lane","racing","treasure","score"],"summary":"High-value rewards appear near danger or in risky lanes.","baseScore":8},{"id":"combo-streak","label":"Combo Streak","bestFor":["score","arcade","racing","survive"],"summary":"Repeated success raises a multiplier until the player is hit.","baseScore":7},{"id":"trust-pressure","label":"Trust Pressure","bestFor":["case-deduction","fortune","consulting","diagnosis","算命","占卜"],"summary":"A trust meter drains over time and drops on weak or wrong interpretations.","baseScore":10},{"id":"contradiction-mark","label":"Contradiction Mark","bestFor":["case-deduction","mystery","detective","risk","推理","侦探"],"summary":"Wrong readings create a visible penalty, time loss, shake, and red feedback.","baseScore":9},{"id":"tower-placement","label":"Tower Placement","bestFor":["tower-defense","strategy","guard","protect","塔防","守护"],"summary":"Player spends currency to place towers on fixed build nodes.","baseScore":10},{"id":"wave-defense","label":"Wave Defense","bestFor":["tower-defense","siege","base","defense","塔防","防守"],"summary":"Enemies arrive in timed waves and damage the base if they cross the route.","baseScore":10},{"id":"card-energy","label":"Card Energy","bestFor":["deck-builder","card","strategy","卡牌","构筑"],"summary":"Each turn gives limited energy that constrains which cards can be played.","baseScore":10},{"id":"card-combo","label":"Card Combo","bestFor":["deck-builder","combo","negotiation","卡牌","连携"],"summary":"Playing compatible cards in sequence creates extra damage, shield, or score.","baseScore":9},{"id":"match-chain","label":"Match Chain","bestFor":["match-puzzle","puzzle","消除","连锁"],"summary":"Clearing large groups creates score chains and floating feedback.","baseScore":10},{"id":"board-clear","label":"Board Clear","bestFor":["match-puzzle","tiles","board","消除"],"summary":"The board refills after cleared tiles and rewards sustained cleanup.","baseScore":9},{"id":"arc-aim","label":"Arc Aim","bestFor":["physics-arc","throw","launch","弹射","投掷"],"summary":"Pointer drag sets launch direction and power for a projectile.","baseScore":10},{"id":"limited-shots","label":"Limited Shots","bestFor":["physics-arc","precision","rescue","弹射"],"summary":"Limited shots make each release a risk/reward decision.","baseScore":9},{"id":"beat-timing","label":"Beat Timing","bestFor":["rhythm-tap","music","节奏","音乐"],"summary":"Tap close to the hit line for perfect/good/miss timing results.","baseScore":10},{"id":"platform-jump","label":"Platform Jump","bestFor":["platform-climb","jump","平台","跳跃"],"summary":"Player moves and jumps between platforms while avoiding hazards.","baseScore":10},{"id":"rising-pressure","label":"Rising Pressure","bestFor":["platform-climb","climb","爬塔","冒险"],"summary":"Platforms scroll and a bottom danger zone pressures upward movement.","baseScore":9},{"id":"production-loop","label":"Production Loop","bestFor":["idle-automation","factory","business","经营","自动化"],"summary":"Stations produce currency over time and become stronger through upgrades.","baseScore":10},{"id":"bottleneck-upgrade","label":"Bottleneck Upgrade","bestFor":["idle-automation","upgrade","放置","经营"],"summary":"Random bottlenecks slow production until the player spends to clear them.","baseScore":9},{"id":"grid-move","label":"Grid Move","bestFor":["grid-tactics","strategy","战术","棋盘"],"summary":"Player moves one tile per turn on a compact grid.","baseScore":10},{"id":"area-control","label":"Area Control","bestFor":["grid-tactics","contain","围堵","策略"],"summary":"Tiles can be sealed or controlled to slow enemy spread.","baseScore":9},{"id":"merge-grow","label":"Merge Grow","bestFor":["merge-drop","merge","2048","合成","数字"],"summary":"Dropping equal tiles merges them into a higher value with chain scoring.","baseScore":10},{"id":"tile-tray","label":"Tile Tray","bestFor":["tile-match","match","三消","羊了个羊"],"summary":"Tap stacked tiles into a seven-slot tray; three of a kind clear.","baseScore":10},{"id":"paddle-bounce","label":"Paddle Bounce","bestFor":["brick-breaker","arcade","打砖块","弹球"],"summary":"A pointer paddle bounces a ball to break brick rows.","baseScore":10},{"id":"tap-flap","label":"Tap Flap","bestFor":["flappy-gap","flappy","飞","鸟"],"summary":"Tap to flap a flyer through scrolling pipe gaps.","baseScore":10}],"enemyKits":[{"id":"sleepy-swarm","label":"Sleepy Swarm","bestFor":["coffee","office","tired","horde-survival"],"types":["foamling","drowsy","jitter","mug","charger","splitter","eliteAlarm"],"baseScore":10},{"id":"traffic-pack","label":"Traffic Pack","bestFor":["racing","runner-lane","highway"],"types":["slowCar","oilSlick","cone","nitroGate","rivalCar"],"baseScore":9},{"id":"glitch-pack","label":"Glitch Pack","bestFor":["computer","virus","cyber"],"types":["bug","popup","lagSpike","corruptBlock","firewallElite"],"baseScore":9},{"id":"ambiguity-pack","label":"Ambiguity Pack","bestFor":["fortune","divination","case-deduction","mystery","detective","consulting","diagnosis","算命","占卜","推理","侦探"],"types":["ambiguity","contradiction","lowTrust","falseLead","timePressure"],"baseScore":10},{"id":"siege-pack","label":"Siege Pack","bestFor":["tower","defense","tower-defense","guard","protect","base","cafe","塔防","防守","守护","保卫"],"types":["runner","tank","shield","swarm","elite"],"baseScore":10},{"id":"dilemma-pack","label":"Dilemma Pack","bestFor":["deck","card","business","negotiation","strategy","卡牌","构筑","商业","谈判"],"types":["objection","debt","doubt","fatigue","opportunity"],"baseScore":10},{"id":"clutter-pack","label":"Clutter Pack","bestFor":["match","puzzle","clear","coffee","消除","咖啡豆"],"types":["bean","cream","sugar","blocker","bonus"],"baseScore":9},{"id":"obstacle-pack","label":"Obstacle Pack","bestFor":["physics","arc","throw","rescue","弹射","投掷","救援"],"types":["crate","shield","movingTarget","bouncePad","hazard"],"baseScore":9},{"id":"beat-pack","label":"Beat Pack","bestFor":["rhythm","music","beat","tap","节奏","音乐"],"types":["tap","hold","double","early","break"],"baseScore":9},{"id":"hazard-pack","label":"Hazard Pack","bestFor":["platform","jump","climb","adventure","平台","跳跃","爬塔"],"types":["spike","crumble","wind","gap","patrol"],"baseScore":9},{"id":"bottleneck-pack","label":"Bottleneck Pack","bestFor":["idle","automation","factory","business","经营","自动化","咖啡店"],"types":["queue","brokenMachine","rushOrder","waste","upgradeGate"],"baseScore":9},{"id":"tactic-pack","label":"Tactic Pack","bestFor":["grid","tactics","virus","contain","战术","棋盘","围堵","病毒"],"types":["blocker","chaser","splitter","zone","elite"],"baseScore":9}],"mappings":[{"keywords":["coffee","bean","espresso","cafe","latte","咖啡","咖啡豆","拿铁"],"roles":{"player":"coffee bean hero","objective":"survive the caffeine rush","threat":"sleepy blobs","reward":"roasted xp beans","special":"espresso burst","environment":"tabletop cafe arena"},"preferredPlayKits":["horde-survival"],"preferredStyleKits":["toy-diorama"],"preferredEnemySets":["sleepy-swarm"],"maturityLevel":"arcade","coreExperience":"survive caffeine pressure, collect upgrades, and push a busy arena score loop"},{"keywords":["race","racing","car","drift","speed","highway","赛车","汽车","漂移","公路","竞速"],"roles":{"player":"neon race car","objective":"drive as far as possible","threat":"traffic and road hazards","reward":"nitro orbs","special":"boost gate","environment":"neon highway"},"preferredPlayKits":["runner-lane"],"preferredStyleKits":["pixel-neon"],"preferredEnemySets":["traffic-pack"],"maturityLevel":"arcade","coreExperience":"speed, lane risk, near misses, and visible score pressure"},{"keywords":["computer","virus","code","hack","bug","电脑","病毒","代码","黑客","漏洞"],"roles":{"player":"repair cursor","objective":"clean the system","threat":"glitches","reward":"data packets","special":"firewall patch","environment":"neon terminal"},"preferredPlayKits":["horde-survival"],"preferredStyleKits":["pixel-neon"],"preferredEnemySets":["glitch-pack"],"maturityLevel":"arcade","coreExperience":"contain spreading glitches, clean pressure, and upgrade against system chaos"},{"keywords":["fortune","divination","oracle","tarot","astrology","bazi","fate","算命","占卜","塔罗","星盘","八字","命理","运势"],"roles":{"player":"archive reader","objective":"seal the strongest interpretation","threat":"ambiguity and lost trust","reward":"clear insight","special":"contradiction mark","environment":"occult archive"},"preferredPlayKits":["case-deduction"],"preferredStyleKits":["occult-archive"],"preferredEnemySets":["ambiguity-pack"],"maturityLevel":"premium","coreExperience":"interpret ambiguous evidence under trust pressure and choose the most defensible reading"},{"keywords":["detective","mystery","case","clue","investigation","risk","consulting","diagnosis","推理","侦探","案件","线索","调查","咨询","诊断"],"roles":{"player":"case analyst","objective":"resolve the case with enough certainty","threat":"false leads and contradictions","reward":"confirmed finding","special":"evidence ledger","environment":"noir case room"},"preferredPlayKits":["case-deduction"],"preferredStyleKits":["noir-ledger","clinical-ritual"],"preferredEnemySets":["ambiguity-pack"],"maturityLevel":"premium","coreExperience":"read clues, manage uncertainty, and defend an interpretation before trust collapses"},{"keywords":["tower","defense","guard","protect","siege","base","cafe defense","塔防","防守","守护","保卫","基地","咖啡店"],"roles":{"player":"defense planner","objective":"hold the route until the final wave","threat":"incoming siege units","reward":"build coins","special":"focus tower","environment":"paper defense map"},"preferredPlayKits":["tower-defense"],"preferredStyleKits":["paper-cut"],"preferredEnemySets":["siege-pack"],"maturityLevel":"arcade","coreExperience":"place defenses, read attack lanes, and survive escalating waves"},{"keywords":["card","deck","deckbuilder","deck-builder","business negotiation","卡牌","构筑","牌组","商业谈判","谈判卡牌"],"roles":{"player":"deal strategist","objective":"resolve the negotiation before pressure wins","threat":"objections and debt","reward":"leverage cards","special":"combo close","environment":"quiet strategy table"},"preferredPlayKits":["deck-builder"],"preferredStyleKits":["clinical-ritual"],"preferredEnemySets":["dilemma-pack"],"maturityLevel":"premium","coreExperience":"build a hand, spend scarce energy, and chain cards into a decisive outcome"},{"keywords":["match","puzzle","tile","clear","coffee match","咖啡豆消除游戏","咖啡豆消除","消除","连线","方块"],"roles":{"player":"pattern cleaner","objective":"clear the board with high-value chains","threat":"clutter blockers","reward":"chain bonus","special":"board sweep","environment":"tabletop puzzle tray"},"preferredPlayKits":["match-puzzle"],"preferredStyleKits":["toy-diorama"],"preferredEnemySets":["clutter-pack"],"maturityLevel":"casual","coreExperience":"spot matching groups, clear clutter, and build satisfying chain reactions"},{"keywords":["throw","arc","physics","slingshot","rescue","launch","投掷","弹射","救援","发射","物理"],"roles":{"player":"rescue launcher","objective":"hit the target with limited shots","threat":"obstacles and wasted arcs","reward":"clean hit bonus","special":"bank shot","environment":"paper rescue range"},"preferredPlayKits":["physics-arc"],"preferredStyleKits":["paper-cut"],"preferredEnemySets":["obstacle-pack"],"maturityLevel":"arcade","coreExperience":"read the arc, commit to a launch, and solve targets with limited shots"},{"keywords":["rhythm","music","beat","tap","dance","timing","音乐节奏","节奏点击","节奏","音乐","点击"],"roles":{"player":"beat keeper","objective":"hit notes near the timing line","threat":"missed beats","reward":"perfect streak","special":"sync burst","environment":"neon beat lane"},"preferredPlayKits":["rhythm-tap"],"preferredStyleKits":["pixel-neon"],"preferredEnemySets":["beat-pack"],"maturityLevel":"arcade","coreExperience":"tap on timing, protect the streak, and ride escalating beat density"},{"keywords":["platform","jump","climb","adventure climb","tower climb","平台跳跃","爬塔平台","平台","跳跃","爬塔","攀爬","冒险"],"roles":{"player":"climber","objective":"keep climbing before the danger rises","threat":"gaps and hazards","reward":"height bonus","special":"safe landing","environment":"layered climb shaft"},"preferredPlayKits":["platform-climb"],"preferredStyleKits":["paper-cut"],"preferredEnemySets":["hazard-pack"],"maturityLevel":"arcade","coreExperience":"jump between platforms, land cleanly, and stay ahead of rising pressure"},{"keywords":["idle","automation","factory","business automation","cafe automation","咖啡店经营自动化","咖啡店经营","放置","自动化","经营","工厂"],"roles":{"player":"operations planner","objective":"automate production and clear bottlenecks","threat":"queues and broken machines","reward":"production profit","special":"automation surge","environment":"compact cafe line"},"preferredPlayKits":["idle-automation"],"preferredStyleKits":["toy-diorama"],"preferredEnemySets":["bottleneck-pack"],"maturityLevel":"casual","coreExperience":"upgrade stations, watch production compound, and remove bottlenecks"},{"keywords":["grid","tactics","strategy board","contain virus","病毒围堵","战术棋盘","围堵","战术","棋盘","格子"],"roles":{"player":"containment unit","objective":"seal threats before they overrun the grid","threat":"spreading virus units","reward":"secured tiles","special":"zone lock","environment":"neon containment grid"},"preferredPlayKits":["grid-tactics"],"preferredStyleKits":["pixel-neon"],"preferredEnemySets":["tactic-pack"],"maturityLevel":"arcade","coreExperience":"move turn by turn, control space, and contain spreading threats"},{"keywords":["merge","2048","watermelon","fruit merge","合成","合成大西瓜","数字合并","西瓜","合体"],"roles":{"player":"merge planner","objective":"merge equal tiles into bigger numbers","threat":"a filling board","reward":"chain merges","special":"big-number burst","environment":"drop board"},"preferredPlayKits":["merge-drop"],"preferredStyleKits":["toy-diorama"],"preferredEnemySets":["clutter-pack"],"maturityLevel":"casual","coreExperience":"drop tiles, merge equals, and chase a satisfying high-number combo"},{"keywords":["羊了个羊","三消","三牌消","连连看","叠叠乐","tile match","sheep","麻将消"],"roles":{"player":"tile sorter","objective":"clear every tile in threes before the tray fills","threat":"a filling slot tray","reward":"triple clears","special":"tray combo","environment":"layered tile heap"},"preferredPlayKits":["tile-match"],"preferredStyleKits":["toy-diorama"],"preferredEnemySets":["clutter-pack"],"maturityLevel":"casual","coreExperience":"tap free tiles into a seven-slot tray and clear triples before it overflows"},{"keywords":["brick","breakout","arkanoid","paddle","打砖块","弹球","碰碰球","接球"],"roles":{"player":"paddle pilot","objective":"break every brick without dropping the ball","threat":"a falling ball","reward":"combo breaks","special":"clear bonus","environment":"neon brick field"},"preferredPlayKits":["brick-breaker"],"preferredStyleKits":["pixel-neon"],"preferredEnemySets":["glitch-pack"],"maturityLevel":"arcade","coreExperience":"steer the paddle, keep the ball alive, and clear escalating brick walls"},{"keywords":["flappy","flappy bird","fly","一键飞","像素鸟","小鸟","飞行","穿越"],"roles":{"player":"tap flyer","objective":"fly through as many gaps as possible","threat":"tight pipe gaps","reward":"clean passes","special":"near miss","environment":"scrolling sky"},"preferredPlayKits":["flappy-gap"],"preferredStyleKits":["pixel-neon"],"preferredEnemySets":["glitch-pack"],"maturityLevel":"arcade","coreExperience":"one-tap timing through scrolling gaps with a rising score streak"}],"strings":{"en":{"common":{"start":"START","restart":"RESTART","score":"Score","best":"Best","time":"{n}s","gameOver":"GAME OVER","hp":"HP","combo":"Combo","level":"Level","wave":"Wave","coins":"Coins","lives":"Lives","turn":"Turn"},"horde-survival":{"levelUp":"LEVEL UP","chooseCard":"Choose one card","upgrades":{"shots":["Multishot","Add one auto bullet"],"speed":["Move Speed","Move faster"],"fire":["Fire Rate","Shoot more often"],"magnet":["Magnet","Wider pickup pull"],"heart":["Vitality","Recover one life"]}},"case-deduction":{"instruction":"Drag evidence into the ledger, then choose the strongest reading before trust runs out.","evidence":"Evidence","ledger":"Ledger","readings":"Readings","trust":"Trust","certainty":"Certainty","filed":"Evidence filed","filedSlot":"filed","dropSlot":"drop","tooLittleEvidence":"Too little evidence","solved":"Pattern confirmed","failed":"Weak reading","readingSealed":"Reading sealed","contradiction":"Contradiction marked","cases":[{"client":"Case 01 / merchant","question":"A quiet loss follows public success.","answer":"career","clues":[{"id":"ledger","title":"late ledger","text":"Profit rose, sleep fell.","weight":{"career":2,"money":1}},{"id":"door","title":"closed door","text":"Two offers arrived together.","weight":{"career":2,"relation":-1}},{"id":"ash","title":"warm ash","text":"An old promise still burns.","weight":{"relation":2,"money":-1}}]},{"client":"Case 02 / traveler","question":"A return is desired, but the road resists.","answer":"travel","clues":[{"id":"map","title":"creased map","text":"The route bends back twice.","weight":{"travel":2,"relation":1}},{"id":"thread","title":"red thread","text":"A name was kept hidden.","weight":{"relation":2,"travel":-1}},{"id":"ticket","title":"unused ticket","text":"Delay protects the outcome.","weight":{"travel":2,"money":-1}}]},{"client":"Case 03 / heir","question":"The house is divided by an unseen debt.","answer":"money","clues":[{"id":"seal","title":"broken seal","text":"An agreement was opened early.","weight":{"money":2,"relation":-1}},{"id":"cup","title":"shared cup","text":"Kindness hides calculation.","weight":{"relation":2,"money":1}},{"id":"window","title":"north window","text":"The safest path is restraint.","weight":{"money":2,"career":-1}}]}],"hypotheses":[{"id":"career","label":"Career Pivot","desc":"Change the role, not the ambition."},{"id":"relation","label":"Hidden Bond","desc":"The true force is obligation."},{"id":"money","label":"Debt Pattern","desc":"Value is leaking through delay."},{"id":"travel","label":"Delayed Return","desc":"Movement must wait for clarity."}]},"deck-builder":{"energy":"Energy","shield":"Shield","demand":"Demand","endTurn":"End turn","cost":"Cost","notEnough":"Not enough energy","dealClosed":"Deal closed","blocked":"Blocked","cards":{"pitch":"Pitch","proof":"Proof","leverage":"Leverage","focus":"Focus","close":"Close"},"types":{"attack":"Attack","shield":"Shield","energy":"Energy"}},"match-puzzle":{},"physics-arc":{"shots":"Shots","stage":"Stage"},"rhythm-tap":{"perfect":"Perfect","good":"Good","miss":"Miss","tap":"TAP"},"platform-climb":{"jumpMove":"Jump / Move"},"idle-automation":{"cash":"Cash","rate":"Rate","upgrade":"Upgrade","bottleneck":"Clear bottleneck","bottleneckCleared":"Bottleneck cleared","needCash":"Need cash","upgraded":"upgraded","stations":["Input","Process","Output"]},"grid-tactics":{"locks":"Locks","breach":"Breach"},"runner-lane":{},"tower-defense":{"up":"UP"},"merge-drop":{},"tile-match":{},"brick-breaker":{},"flappy-gap":{}},"zh":{"common":{"start":"开始","restart":"重开","score":"分数","best":"最高","time":"{n}秒","gameOver":"结束","hp":"生命","combo":"连击","level":"等级","wave":"波次","coins":"金币","lives":"生命","turn":"回合"},"horde-survival":{"levelUp":"升级","chooseCard":"选择一张卡","upgrades":{"shots":["多重弹","自动子弹加一"],"speed":["移速","移动更快"],"fire":["射速","攻击更频繁"],"magnet":["磁吸","扩大拾取范围"],"heart":["生命","恢复一点生命"]}},"case-deduction":{"instruction":"把证据拖入档案，在信任耗尽前选出最强的解读。","evidence":"证据","ledger":"档案","readings":"解读","trust":"信任","certainty":"确定度","filed":"证据已归档","filedSlot":"已归档","dropSlot":"放入","tooLittleEvidence":"证据不足","solved":"判断成立","failed":"判断偏弱","readingSealed":"解读确认","contradiction":"矛盾已标记","cases":[{"client":"案一 / 商人","question":"风光背后，暗自折损。","answer":"career","clues":[{"id":"ledger","title":"迟到的账本","text":"利润见涨，睡眠下降。","weight":{"career":2,"money":1}},{"id":"door","title":"关上的门","text":"两个邀约同时到来。","weight":{"career":2,"relation":-1}},{"id":"ash","title":"余温的灰","text":"一个旧承诺仍在灼烧。","weight":{"relation":2,"money":-1}}]},{"client":"案二 / 旅人","question":"归心已起，归途却阻。","answer":"travel","clues":[{"id":"map","title":"折痕的地图","text":"路线两度折返。","weight":{"travel":2,"relation":1}},{"id":"thread","title":"红线","text":"有个名字被藏了起来。","weight":{"relation":2,"travel":-1}},{"id":"ticket","title":"未用的票","text":"延迟反而保全结局。","weight":{"travel":2,"money":-1}}]},{"client":"案三 / 继承人","question":"一笔看不见的债，裂开了这个家。","answer":"money","clues":[{"id":"seal","title":"破损的封印","text":"一纸约定被提前拆开。","weight":{"money":2,"relation":-1}},{"id":"cup","title":"共饮的杯","text":"善意里藏着算计。","weight":{"relation":2,"money":1}},{"id":"window","title":"朝北的窗","text":"最稳的路是克制。","weight":{"money":2,"career":-1}}]}],"hypotheses":[{"id":"career","label":"事业转向","desc":"换的是位置，不是野心。"},{"id":"relation","label":"隐秘羁绊","desc":"真正的牵引是亏欠。"},{"id":"money","label":"债务模式","desc":"价值正从拖延中流失。"},{"id":"travel","label":"延迟归程","desc":"行动必须等清晰。"}]},"deck-builder":{"energy":"能量","shield":"护盾","demand":"压力","endTurn":"结束回合","cost":"费用","notEnough":"能量不足","dealClosed":"达成成交","blocked":"已挡下","cards":{"pitch":"提案","proof":"佐证","leverage":"筹码","focus":"专注","close":"成交"},"types":{"attack":"攻击","shield":"护盾","energy":"能量"}},"match-puzzle":{},"physics-arc":{"shots":"弹数","stage":"阶段"},"rhythm-tap":{"perfect":"完美","good":"命中","miss":"失误","tap":"点击"},"platform-climb":{"jumpMove":"跳跃 / 移动"},"idle-automation":{"cash":"现金","rate":"产能","upgrade":"升级","bottleneck":"清理瓶颈","bottleneckCleared":"瓶颈已清","needCash":"现金不足","upgraded":"已升级","stations":["进料","加工","出货"]},"grid-tactics":{"locks":"锁定","breach":"被突破"},"runner-lane":{},"tower-defense":{"up":"升级"},"merge-drop":{},"tile-match":{},"brick-breaker":{},"flappy-gap":{}}}},"templates":{"brick-breaker.html":"<!doctype html>\n<html lang=\"{{LANG}}\"><head><meta charset=\"utf-8\"><meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\"><title>{{TITLE}}</title><link rel=\"icon\" href=\"data:,\"><style>\nhtml,body{margin:0;width:100%;height:100%;overflow:hidden;background:{{BACKGROUND_A}};color:{{INK}};font-family:system-ui,-apple-system,BlinkMacSystemFont,\"Segoe UI\",sans-serif;touch-action:none;user-select:none}canvas{display:block;width:100vw;height:100vh;touch-action:none}\n</style></head><body>\n<!--\nGAME_SPEC_ID: {{ID}}\nGAME_KIT: brick-breaker\nSTYLE_KIT: {{STYLE_KIT}}\nMECHANICS: {{MECHANICS}}\nENEMY_SET: {{ENEMY_SET}}\n-->\n<canvas id=\"game\" aria-label=\"{{TITLE}}\"></canvas><script>\n(() => {\n\"use strict\";\nconst SPEC = {{SPEC_JSON}},S=SPEC.strings,canvas=document.getElementById(\"game\"),ctx=canvas.getContext(\"2d\"),READY=\"READY\",PLAYING=\"PLAYING\",OVER=\"OVER\",FIXED_DT=1/60;\nconst p=SPEC.palette,ROWCOL=[p.bad,p.accent,\"#f59e0b\",\"#60a5fa\",p.good],particles=[],floats=[];\nlet width=0,height=0,dpr=1,state=READY,last=0,acc=0,score=0,best=Number(localStorage.getItem(\"tgg-\"+SPEC.id+\"-best\")||0),timeLeft=SPEC.duration,lives=3,level=1,combo=0,shake=0,flash=0;\nconst paddle={x:0,y:0,w:120,h:16},ball={x:0,y:0,vx:0,vy:0,r:9,stuck:true},bricks=[];\nfunction resize(){dpr=Math.max(1,Math.min(2,devicePixelRatio||1));width=innerWidth;height=innerHeight;canvas.width=Math.floor(width*dpr);canvas.height=Math.floor(height*dpr);canvas.style.width=width+\"px\";canvas.style.height=height+\"px\";ctx.setTransform(dpr,0,0,dpr,0,0);paddle.w=Math.min(150,width*.26);paddle.y=height-70;if(ball.stuck){paddle.x=width/2;resetBall()}}\nfunction reset(){state=PLAYING;score=0;timeLeft=SPEC.duration;lives=3;level=1;combo=0;particles.length=floats.length=0;buildBricks();resetBall()}\nfunction buildBricks(){bricks.length=0;const cols=Math.max(5,Math.min(9,Math.floor(width/86))),rows=Math.min(5,3+Math.floor(level/2)),gap=8,bw=(Math.min(width*.92,760)-gap*(cols-1))/cols,bh=26,x0=(width-(bw*cols+gap*(cols-1)))/2,y0=Math.max(96,height*.14);for(let r=0;r<rows;r++)for(let c=0;c<cols;c++)bricks.push({x:x0+c*(bw+gap),y:y0+r*(bh+gap),w:bw,h:bh,hp:1+Math.floor((rows-r)/2),max:1+Math.floor((rows-r)/2),color:ROWCOL[r%ROWCOL.length],alive:true})}\nfunction resetBall(){ball.stuck=true;ball.x=paddle.x;ball.y=paddle.y-ball.r-2;ball.vx=0;ball.vy=0}\nfunction launch(){ball.stuck=false;const sp=380*SPEC.tuning.speed;ball.vx=sp*.5*(Math.random()<.5?-1:1);ball.vy=-sp}\nfunction pointerdown(e){e.preventDefault();if(state===READY||state===OVER){reset();return}paddle.x=clamp(e.clientX,paddle.w/2,width-paddle.w/2);if(ball.stuck)launch()}\nfunction pointermove(e){e.preventDefault();if(state===PLAYING)paddle.x=clamp(e.clientX,paddle.w/2,width-paddle.w/2)}\nfunction pointerup(e){e.preventDefault()}\ncanvas.addEventListener(\"pointerdown\",pointerdown);canvas.addEventListener(\"pointermove\",pointermove);canvas.addEventListener(\"pointerup\",pointerup);canvas.addEventListener(\"pointercancel\",pointerup);addEventListener(\"resize\",resize);\nfunction update(dt){if(state!==PLAYING){updateFx(dt);return}timeLeft-=dt;if(timeLeft<=0)over();if(ball.stuck){ball.x=paddle.x;ball.y=paddle.y-ball.r-2}else{ball.x+=ball.vx*dt;ball.y+=ball.vy*dt;if(ball.x<ball.r){ball.x=ball.r;ball.vx=Math.abs(ball.vx)}if(ball.x>width-ball.r){ball.x=width-ball.r;ball.vx=-Math.abs(ball.vx)}if(ball.y<ball.r){ball.y=ball.r;ball.vy=Math.abs(ball.vy)}if(ball.y-ball.r>paddle.y-paddle.h&&ball.y<paddle.y+30&&Math.abs(ball.x-paddle.x)<paddle.w/2+ball.r&&ball.vy>0){const rel=(ball.x-paddle.x)/(paddle.w/2),sp=Math.hypot(ball.vx,ball.vy);const ang=rel*1.05;ball.vx=Math.sin(ang)*sp;ball.vy=-Math.abs(Math.cos(ang)*sp);ball.y=paddle.y-paddle.h-ball.r;combo=0}for(const b of bricks){if(!b.alive)continue;if(ball.x+ball.r>b.x&&ball.x-ball.r<b.x+b.w&&ball.y+ball.r>b.y&&ball.y-ball.r<b.y+b.h){const px=Math.min(ball.x+ball.r-b.x,b.x+b.w-(ball.x-ball.r)),py=Math.min(ball.y+ball.r-b.y,b.y+b.h-(ball.y-ball.r));if(px<py){ball.vx=-ball.vx}else{ball.vy=-ball.vy}b.hp--;burst(ball.x,ball.y,b.color,8);if(b.hp<=0){b.alive=false;combo++;const g=Math.round(10*(1+Math.floor(combo/3))*SPEC.tuning.scoreMultiplier);score+=g;addFloat(\"+\"+g,b.x+b.w/2,b.y,p.good)}break}}if(ball.y-ball.r>height){lives--;shake=.3;flash=.4;combo=0;if(lives<=0)over();else resetBall()}if(bricks.every(b=>!b.alive)){level++;score+=50;buildBricks();resetBall()}}shake=Math.max(0,shake-dt*3);flash=Math.max(0,flash-dt*3);updateFx(dt)}\nfunction over(){state=OVER;best=Math.max(best,score);localStorage.setItem(\"tgg-\"+SPEC.id+\"-best\",String(best))}\nfunction updateFx(dt){for(const q of particles){q.x+=q.vx*dt;q.y+=q.vy*dt;q.life-=dt}for(const f of floats){f.y-=42*dt;f.life-=dt}trim(particles,q=>q.life<=0);trim(floats,f=>f.life<=0)}\nfunction render(){ctx.save();if(shake)ctx.translate((Math.random()-.5)*shake*16,(Math.random()-.5)*shake*16);bg();drawBricks();drawPaddle();drawBall();fx();hud();if(flash){ctx.fillStyle=alpha(p.bad,flash*.22);ctx.fillRect(0,0,width,height)}if(state!==PLAYING)overlay();ctx.restore()}\nfunction bg(){const g=ctx.createLinearGradient(0,0,0,height);g.addColorStop(0,p.backgroundA);g.addColorStop(1,p.backgroundB);ctx.fillStyle=g;ctx.fillRect(0,0,width,height);const v=ctx.createRadialGradient(width/2,height*.45,height*.2,width/2,height*.5,height*.95);v.addColorStop(0,\"rgba(0,0,0,0)\");v.addColorStop(1,\"rgba(0,0,0,.42)\");ctx.fillStyle=v;ctx.fillRect(0,0,width,height)}\nfunction drawBricks(){for(const b of bricks){if(!b.alive)continue;const c=b.color,t=b.hp/b.max;ctx.fillStyle=\"rgba(0,0,0,.22)\";ctx.beginPath();ctx.roundRect(b.x+1,b.y+2,b.w,b.h,7);ctx.fill();const g=ctx.createLinearGradient(0,b.y,0,b.y+b.h);g.addColorStop(0,lighten(c,.4*t+.1));g.addColorStop(1,c);ctx.fillStyle=g;ctx.beginPath();ctx.roundRect(b.x,b.y,b.w,b.h,7);ctx.fill();ctx.fillStyle=\"rgba(255,255,255,.18)\";ctx.beginPath();ctx.roundRect(b.x+4,b.y+3,b.w-8,5,3);ctx.fill();ctx.strokeStyle=alpha(p.ink,.25);ctx.lineWidth=1.5;ctx.stroke()}}\nfunction drawPaddle(){const x=paddle.x,y=paddle.y;ctx.fillStyle=\"rgba(0,0,0,.3)\";ctx.beginPath();ctx.roundRect(x-paddle.w/2,y+3,paddle.w,paddle.h,9);ctx.fill();const g=ctx.createLinearGradient(0,y,0,y+paddle.h);g.addColorStop(0,lighten(p.accent,.5));g.addColorStop(1,p.accent);ctx.save();ctx.shadowColor=p.accent;ctx.shadowBlur=14;ctx.fillStyle=g;ctx.beginPath();ctx.roundRect(x-paddle.w/2,y,paddle.w,paddle.h,9);ctx.fill();ctx.restore();ctx.strokeStyle=p.ink;ctx.lineWidth=2;ctx.stroke()}\nfunction drawBall(){ctx.save();ctx.shadowColor=p.ink;ctx.shadowBlur=12;const g=ctx.createRadialGradient(ball.x-3,ball.y-3,2,ball.x,ball.y,ball.r);g.addColorStop(0,\"#fff\");g.addColorStop(1,p.ink);ctx.fillStyle=g;ctx.beginPath();ctx.arc(ball.x,ball.y,ball.r,0,Math.PI*2);ctx.fill();ctx.restore()}\nfunction fx(){for(const q of particles){ctx.globalAlpha=Math.max(0,q.life*2);ctx.fillStyle=q.color;ctx.fillRect(q.x,q.y,q.size,q.size)}ctx.globalAlpha=1;for(const f of floats){ctx.globalAlpha=Math.max(0,f.life);text(f.text,f.x,f.y,16,f.color,\"center\")}ctx.globalAlpha=1}\nfunction hud(){if(state===READY)return;text(SPEC.title,20,30,21,p.ink,\"left\");text(S.score+\" \"+score+\"  \"+S.best+\" \"+best,20,58,13,p.ink,\"left\");text(S.lives+\" \"+lives+\"  \"+S.level+\" \"+level,width-20,32,15,p.ink,\"right\");text(S.time.replace(\"{n}\",Math.ceil(timeLeft)),width-20,60,20,timeLeft<10?p.bad:p.ink,\"right\")}\nfunction overlay(){ctx.fillStyle=\"rgba(0,0,0,.66)\";ctx.fillRect(0,0,width,height);text(SPEC.title,width/2,height*.4,34,p.ink,\"center\");text(SPEC.roles.objective,width/2,height*.4+40,15,p.ink,\"center\");text(state===OVER?S.restart:S.start,width/2,height*.4+92,24,p.accent,\"center\");if(state===OVER)text(S.score+\" \"+score+\"  \"+S.best+\" \"+best,width/2,height*.4+128,15,p.ink,\"center\")}\nfunction burst(x,y,c,n){for(let i=0;i<n;i++){const a=Math.random()*Math.PI*2,s=40+Math.random()*120;particles.push({x,y,vx:Math.cos(a)*s,vy:Math.sin(a)*s,life:.5,size:3+Math.random()*4,color:c})}}\nfunction addFloat(t,x,y,c){floats.push({text:t,x,y,life:.9,color:c})}\nfunction trim(a,pred){for(let i=a.length-1;i>=0;i--)if(pred(a[i]))a.splice(i,1)}\nfunction clamp(v,a,b){return Math.max(a,Math.min(b,v))}\nfunction alpha(hex,a){const n=parseInt(hex.slice(1),16);return`rgba(${n>>16&255},${n>>8&255},${n&255},${a})`}\nfunction lighten(hex,t){const n=parseInt(hex.slice(1),16),r=n>>16&255,g=n>>8&255,b=n&255,m=v=>Math.round(v+(255-v)*t);return`rgb(${m(r)},${m(g)},${m(b)})`}\nfunction text(t,x,y,s,c,a){ctx.fillStyle=c;ctx.font=`900 ${s}px system-ui`;ctx.textAlign=a;ctx.textBaseline=\"middle\";ctx.fillText(t,x,y)}\nfunction tick(now){if(!last)last=now;const frame=Math.min(.05,(now-last)/1000);last=now;acc+=frame;while(acc>=FIXED_DT){update(FIXED_DT);acc-=FIXED_DT}render();requestAnimationFrame(tick)}\nresize();requestAnimationFrame(tick);\n})();\n</script></body></html>\n","case-deduction.html":"<!doctype html>\n<html lang=\"{{LANG}}\">\n<head>\n<meta charset=\"utf-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>{{TITLE}}</title>\n<link rel=\"icon\" href=\"data:,\">\n<style>\nhtml,body{margin:0;width:100%;height:100%;overflow:hidden;background:{{BACKGROUND_A}};color:{{INK}};font-family:Georgia,\"Times New Roman\",serif;touch-action: none;user-select:none}\ncanvas{display:block;width:100vw;height:100vh;touch-action: none}\n</style>\n</head>\n<body>\n<!--\nGAME_INFERENCE\nGAME_SPEC_ID: {{ID}}\nGAME_KIT: case-deduction\nSTYLE_KIT: {{STYLE_KIT}}\nMECHANICS: {{MECHANICS}}\nENEMY_SET: {{ENEMY_SET}}\nSECONDARY_TWIST: trust-pressure\n-->\n<canvas id=\"game\" aria-label=\"{{TITLE}}\"></canvas>\n<script>\n(() => {\n  \"use strict\";\n\n  const SPEC = {{SPEC_JSON}};\n  const S = SPEC.strings;\n  const canvas = document.getElementById(\"game\");\n  const ctx = canvas.getContext(\"2d\");\n  const STATE_READY = \"READY\";\n  const STATE_PLAYING = \"PLAYING\";\n  const STATE_OVER = \"OVER\";\n  const GAME_SECONDS = SPEC.duration;\n  const FIXED_DT = 1 / 60;\n\n  const THEME = {\n    title: SPEC.title,\n    instruction: S.instruction,\n    bg: SPEC.palette.backgroundA,\n    panel: SPEC.palette.backgroundB,\n    paper: SPEC.palette.backgroundB,\n    paper2: \"#47362b\",\n    line: \"#8b6f4e\",\n    ink: SPEC.palette.ink,\n    dim: \"#a99574\",\n    accent: SPEC.palette.accent,\n    red: SPEC.palette.bad,\n    green: SPEC.palette.good\n  };\n\n  // Case fiction + readings are localized: see kits/strings/{en,zh}.json case-deduction.cases / .hypotheses\n  const cases = S.cases;\n  const hypotheses = S.hypotheses;\n\n  let width = 0, height = 0, dpr = 1;\n  let state = STATE_READY;\n  let lastTime = 0, accumulator = 0;\n  let score = 0, best = readBest(), timeLeft = GAME_SECONDS, trust = 70, certainty = 0;\n  let caseIndex = 0, currentCase = cases[0], selectedClues = [], hypothesisScores = {}, dragging = null;\n  let shake = 0, flash = 0, overlayEase = 0, notice = \"\", noticeTime = 0;\n  const clueRects = [], ledgerRects = [], hypothesisRects = [], particles = [], lines = [];\n\n  function readBest() {\n    const raw = localStorage.getItem(\"tgg-\" + SPEC.id + \"-best\");\n    return raw ? Number(raw) || 0 : 0;\n  }\n\n  function writeBest() {\n    if (score > best) {\n      best = score;\n      localStorage.setItem(\"tgg-\" + SPEC.id + \"-best\", String(best));\n    }\n  }\n\n  function resizeCanvas() {\n    dpr = Math.max(1, Math.min(2, window.devicePixelRatio || 1));\n    width = window.innerWidth;\n    height = window.innerHeight;\n    canvas.width = Math.floor(width * dpr);\n    canvas.height = Math.floor(height * dpr);\n    canvas.style.width = width + \"px\";\n    canvas.style.height = height + \"px\";\n    ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n    layout();\n  }\n\n  function layout() {\n    clueRects.length = 0;\n    ledgerRects.length = 0;\n    hypothesisRects.length = 0;\n    const sideW = Math.min(270, width * 0.28);\n    const top = Math.max(124, height * 0.18);\n    for (let i = 0; i < 3; i += 1) {\n      clueRects.push({ x: 28, y: top + i * 118, w: sideW, h: 92, clue: currentCase.clues[i] });\n      ledgerRects.push({ x: width * 0.42 + i * Math.min(112, width * 0.11), y: height * 0.48, w: 90, h: 78, clue: selectedClues[i] || null });\n    }\n    const hW = Math.min(210, width * 0.22);\n    const startX = width - hW - 28;\n    for (let i = 0; i < hypotheses.length; i += 1) {\n      hypothesisRects.push({ x: startX, y: top + i * 92, w: hW, h: 72, hypothesis: hypotheses[i] });\n    }\n  }\n\n  function resetGame() {\n    state = STATE_PLAYING;\n    score = 0;\n    timeLeft = GAME_SECONDS;\n    trust = 70;\n    certainty = 0;\n    caseIndex = 0;\n    particles.length = 0;\n    lines.length = 0;\n    notice = \"\";\n    noticeTime = 0;\n    loadCase(0);\n  }\n\n  function loadCase(index) {\n    currentCase = cases[index % cases.length];\n    selectedClues = [];\n    hypothesisScores = Object.fromEntries(hypotheses.map(h => [h.id, 0]));\n    certainty = 0;\n    dragging = null;\n    layout();\n  }\n\n  function onPointerDown(event) {\n    event.preventDefault();\n    const x = event.clientX;\n    const y = event.clientY;\n    if (state === STATE_READY || state === STATE_OVER) {\n      resetGame();\n      return;\n    }\n    if (state !== STATE_PLAYING) return;\n\n    for (const rect of clueRects) {\n      if (inside(x, y, rect) && !selectedClues.includes(rect.clue)) {\n        dragging = { clue: rect.clue, x, y, from: rect };\n        return;\n      }\n    }\n    for (const rect of hypothesisRects) {\n      if (inside(x, y, rect)) {\n        chooseHypothesis(rect.hypothesis);\n        return;\n      }\n    }\n  }\n\n  function onPointerMove(event) {\n    event.preventDefault();\n    if (dragging) {\n      dragging.x = event.clientX;\n      dragging.y = event.clientY;\n    }\n  }\n\n  function onPointerUp(event) {\n    event.preventDefault();\n    if (!dragging || state !== STATE_PLAYING) {\n      dragging = null;\n      return;\n    }\n    const hitLedger = ledgerRects.some(rect => inside(dragging.x, dragging.y, rect));\n    if (hitLedger) addClue(dragging.clue, dragging.x, dragging.y);\n    dragging = null;\n  }\n\n  canvas.addEventListener(\"pointerdown\", onPointerDown);\n  canvas.addEventListener(\"pointermove\", onPointerMove);\n  canvas.addEventListener(\"pointerup\", onPointerUp);\n  canvas.addEventListener(\"pointercancel\", onPointerUp);\n  window.addEventListener(\"resize\", resizeCanvas);\n\n  function addClue(clue, x, y) {\n    if (selectedClues.includes(clue)) return;\n    selectedClues.push(clue);\n    for (const [key, value] of Object.entries(clue.weight)) {\n      hypothesisScores[key] = (hypothesisScores[key] || 0) + value;\n    }\n    certainty = Math.min(100, certainty + 24 + Math.max(0, clue.weight[currentCase.answer] || 0) * 8);\n    score += 8;\n    notice = S.filed;\n    noticeTime = 1;\n    addLine(clue.id);\n    burst(x, y, THEME.accent, 14);\n    layout();\n  }\n\n  function chooseHypothesis(hypothesis) {\n    if (selectedClues.length < 2) {\n      trust = Math.max(0, trust - 8);\n      flash = 0.35;\n      shake = 0.2;\n      notice = S.tooLittleEvidence;\n      noticeTime = 1.1;\n      return;\n    }\n    const right = hypothesis.id === currentCase.answer;\n    const confidence = Math.max(0, hypothesisScores[hypothesis.id] || 0);\n    if (right) {\n      const gain = 55 + confidence * 12 + Math.ceil(trust / 5);\n      score += gain;\n      trust = Math.min(100, trust + 12);\n      certainty = 100;\n      notice = S.readingSealed + \" +\" + gain;\n      noticeTime = 1.2;\n      burst(width * 0.52, height * 0.37, THEME.green, 34);\n      caseIndex += 1;\n      setTimeout(() => { if (state === STATE_PLAYING) loadCase(caseIndex); }, 420);\n    } else {\n      timeLeft = Math.max(0, timeLeft - 5);\n      trust = Math.max(0, trust - 18);\n      certainty = Math.max(0, certainty - 18);\n      flash = 0.5;\n      shake = 0.32;\n      notice = S.contradiction;\n      noticeTime = 1.2;\n      burst(width * 0.52, height * 0.37, THEME.red, 22);\n    }\n  }\n\n  function update(dt) {\n    if (state !== STATE_PLAYING) {\n      overlayEase = Math.min(1, overlayEase + dt * 2.6);\n      updateFx(dt);\n      return;\n    }\n    timeLeft -= dt;\n    trust = Math.max(0, trust - dt * 1.2);\n    if (timeLeft <= 0 || trust <= 0) {\n      timeLeft = Math.max(0, timeLeft);\n      state = STATE_OVER;\n      writeBest();\n    }\n    shake = Math.max(0, shake - dt * 3);\n    flash = Math.max(0, flash - dt * 2.4);\n    noticeTime = Math.max(0, noticeTime - dt);\n    updateFx(dt);\n  }\n\n  function updateFx(dt) {\n    for (let i = particles.length - 1; i >= 0; i -= 1) {\n      const p = particles[i];\n      p.x += p.vx * dt;\n      p.y += p.vy * dt;\n      p.life -= dt;\n      if (p.life <= 0) particles.splice(i, 1);\n    }\n  }\n\n  function render() {\n    ctx.save();\n    if (shake > 0) ctx.translate((Math.random() - 0.5) * shake * 16, (Math.random() - 0.5) * shake * 16);\n    drawBackground();\n    drawHud();\n    drawCase();\n    drawClues();\n    drawLedger();\n    drawHypotheses();\n    drawFx();\n    if (dragging) drawClueCard({ x: dragging.x - 92, y: dragging.y - 36, w: 184, h: 74, clue: dragging.clue }, true);\n    if (flash > 0) {\n      ctx.fillStyle = `rgba(159,57,55,${flash * 0.22})`;\n      ctx.fillRect(0, 0, width, height);\n    }\n    if (state !== STATE_PLAYING) drawOverlay();\n    ctx.restore();\n  }\n\n  function drawBackground() {\n    ctx.fillStyle = THEME.bg;\n    ctx.fillRect(0, 0, width, height);\n    ctx.strokeStyle = \"rgba(201,163,91,0.09)\";\n    ctx.lineWidth = 1;\n    for (let x = 0; x < width; x += 38) {\n      ctx.beginPath();\n      ctx.moveTo(x, 0);\n      ctx.lineTo(x, height);\n      ctx.stroke();\n    }\n    for (let y = 0; y < height; y += 38) {\n      ctx.beginPath();\n      ctx.moveTo(0, y);\n      ctx.lineTo(width, y);\n      ctx.stroke();\n    }\n    const vig = ctx.createRadialGradient(width / 2, height * 0.45, height * 0.2, width / 2, height * 0.5, height * 0.98);\n    vig.addColorStop(0, \"rgba(0,0,0,0)\"); vig.addColorStop(1, \"rgba(0,0,0,0.5)\");\n    ctx.fillStyle = vig; ctx.fillRect(0, 0, width, height);\n    ctx.fillStyle = \"rgba(33,25,22,0.90)\";\n    roundRect(width * 0.34, height * 0.16, width * 0.34, height * 0.62, 18, true, true);\n    ctx.strokeStyle = \"rgba(201,163,91,0.42)\";\n    ctx.stroke();\n  }\n\n  function drawHud() {\n    drawText(THEME.title, 22, 30, 24, THEME.ink, \"left\", \"serif\");\n    drawText(S.score + \" \" + score + \"   \" + S.best + \" \" + best, 22, 58, 13, THEME.dim, \"left\", \"serif\");\n    drawMeter(22, 78, 176, 8, trust / 100, THEME.green, S.trust);\n    drawMeter(width - 210, 78, 176, 8, certainty / 100, THEME.accent, S.certainty);\n    drawText(S.time.replace(\"{n}\", Math.ceil(timeLeft)), width - 22, 30, 24, timeLeft < 12 ? THEME.red : THEME.ink, \"right\", \"serif\");\n  }\n\n  function drawCase() {\n    const x = width * 0.37, y = height * 0.19, w = width * 0.28, h = height * 0.22;\n    ctx.fillStyle = \"rgba(48,37,31,0.88)\";\n    roundRect(x, y, w, h, 14, true, true);\n    ctx.strokeStyle = \"rgba(201,163,91,0.54)\";\n    ctx.stroke();\n    drawText(currentCase.client, x + 18, y + 26, 13, THEME.dim, \"left\", \"serif\");\n    wrapText(currentCase.question, x + 18, y + 62, w - 36, 22, THEME.ink, 19);\n    if (noticeTime > 0) drawText(notice, x + w / 2, y + h - 26, 15, notice === S.contradiction || notice === S.tooLittleEvidence ? THEME.red : THEME.accent, \"center\", \"serif\");\n  }\n\n  function drawClues() {\n    drawText(S.evidence, 28, clueRects[0].y - 18, 15, THEME.dim, \"left\", \"serif\");\n    for (const rect of clueRects) drawClueCard(rect, false);\n  }\n\n  function drawClueCard(rect, lifted) {\n    const clue = rect.clue;\n    const used = selectedClues.includes(clue);\n    ctx.save();\n    ctx.globalAlpha = used && !lifted ? 0.36 : 1;\n    ctx.fillStyle = lifted ? \"rgba(71,54,43,0.98)\" : \"rgba(48,37,31,0.92)\";\n    roundRect(rect.x, rect.y, rect.w, rect.h, 12, true, true);\n    ctx.strokeStyle = lifted ? THEME.accent : \"rgba(139,111,78,0.75)\";\n    ctx.lineWidth = lifted ? 3 : 1.5;\n    ctx.stroke();\n    drawText(clue.title, rect.x + 14, rect.y + 24, 15, THEME.ink, \"left\", \"serif\");\n    wrapText(clue.text, rect.x + 14, rect.y + 50, rect.w - 28, 18, THEME.dim, 13);\n    ctx.restore();\n  }\n\n  function drawLedger() {\n    drawText(S.ledger, width * 0.43, height * 0.46, 15, THEME.dim, \"left\", \"serif\");\n    for (const rect of ledgerRects) {\n      ctx.fillStyle = \"rgba(19,15,13,0.76)\";\n      roundRect(rect.x, rect.y, rect.w, rect.h, 10, true, true);\n      ctx.strokeStyle = \"rgba(201,163,91,0.38)\";\n      ctx.stroke();\n      if (rect.clue) {\n        drawText(rect.clue.title, rect.x + rect.w / 2, rect.y + 30, 12, THEME.ink, \"center\", \"serif\");\n        drawText(S.filedSlot, rect.x + rect.w / 2, rect.y + 54, 11, THEME.accent, \"center\", \"serif\");\n      } else {\n        drawText(S.dropSlot, rect.x + rect.w / 2, rect.y + 34, 11, THEME.dim, \"center\", \"serif\");\n      }\n    }\n    ctx.strokeStyle = \"rgba(159,57,55,0.44)\";\n    for (const line of lines) {\n      ctx.beginPath();\n      ctx.moveTo(line.x1, line.y1);\n      ctx.lineTo(line.x2, line.y2);\n      ctx.stroke();\n    }\n  }\n\n  function drawHypotheses() {\n    drawText(S.readings, hypothesisRects[0].x, hypothesisRects[0].y - 18, 15, THEME.dim, \"left\", \"serif\");\n    for (const rect of hypothesisRects) {\n      const h = rect.hypothesis;\n      const scoreHint = hypothesisScores[h.id] || 0;\n      ctx.fillStyle = \"rgba(48,37,31,0.92)\";\n      roundRect(rect.x, rect.y, rect.w, rect.h, 12, true, true);\n      ctx.strokeStyle = scoreHint > 0 ? THEME.accent : \"rgba(139,111,78,0.62)\";\n      ctx.lineWidth = scoreHint > 0 ? 2.5 : 1.2;\n      ctx.stroke();\n      drawText(h.label, rect.x + 14, rect.y + 22, 14, THEME.ink, \"left\", \"serif\");\n      drawText(h.desc, rect.x + 14, rect.y + 46, 11, THEME.dim, \"left\", \"serif\");\n    }\n  }\n\n  function drawFx() {\n    for (const p of particles) {\n      ctx.globalAlpha = Math.max(0, p.life * 2);\n      ctx.fillStyle = p.color;\n      ctx.fillRect(p.x - p.size / 2, p.y - p.size / 2, p.size, p.size);\n    }\n    ctx.globalAlpha = 1;\n  }\n\n  function drawOverlay() {\n    ctx.fillStyle = \"rgba(19,15,13,0.82)\";\n    ctx.fillRect(0, 0, width, height);\n    const y = height * (0.5 + (1 - overlayEase) * 0.04);\n    drawText(THEME.title, width / 2, y - 70, 38, THEME.ink, \"center\", \"serif\");\n    wrapText(THEME.instruction, width / 2 - Math.min(330, width * 0.38), y - 24, Math.min(660, width * 0.76), 24, THEME.dim, 17, \"center\");\n    drawText(state === STATE_OVER ? S.restart : S.start, width / 2, y + 54, 24, THEME.accent, \"center\", \"serif\");\n    if (state === STATE_OVER) drawText(S.score + \" \" + score + \"   \" + S.best + \" \" + best, width / 2, y + 92, 15, THEME.ink, \"center\", \"serif\");\n  }\n\n  function drawMeter(x, y, w, h, value, color, label) {\n    drawText(label, x, y - 10, 11, THEME.dim, \"left\", \"serif\");\n    ctx.fillStyle = \"rgba(48,37,31,0.9)\";\n    ctx.fillRect(x, y, w, h);\n    ctx.fillStyle = color;\n    ctx.fillRect(x, y, w * Math.max(0, Math.min(1, value)), h);\n  }\n\n  function burst(x, y, color, count) {\n    for (let i = 0; i < count; i += 1) {\n      const a = Math.random() * Math.PI * 2;\n      const s = 45 + Math.random() * 130;\n      particles.push({ x, y, vx: Math.cos(a) * s, vy: Math.sin(a) * s, life: 0.48, size: 2 + Math.random() * 5, color });\n    }\n  }\n\n  function addLine(clueId) {\n    const source = clueRects.find(rect => rect.clue.id === clueId);\n    const target = ledgerRects[Math.max(0, selectedClues.length - 1)];\n    if (!source || !target) return;\n    lines.push({ x1: source.x + source.w, y1: source.y + source.h / 2, x2: target.x + target.w / 2, y2: target.y + target.h / 2 });\n  }\n\n  function inside(x, y, rect) {\n    return x >= rect.x && x <= rect.x + rect.w && y >= rect.y && y <= rect.y + rect.h;\n  }\n\n  function drawText(text, x, y, size, color, align, family) {\n    ctx.fillStyle = color;\n    ctx.font = `700 ${size}px ${family === \"serif\" ? \"Georgia, serif\" : \"system-ui, sans-serif\"}`;\n    ctx.textAlign = align;\n    ctx.textBaseline = \"middle\";\n    ctx.fillText(text, x, y);\n  }\n\n  function wrapText(text, x, y, maxWidth, lineHeight, color, size, align = \"left\") {\n    ctx.fillStyle = color;\n    ctx.font = `700 ${size}px Georgia, serif`;\n    ctx.textAlign = align;\n    ctx.textBaseline = \"middle\";\n    const drawX = align === \"center\" ? x + maxWidth / 2 : x;\n    const byChar = text.indexOf(\" \") === -1; // CJK has no spaces -> wrap per character\n    const tokens = byChar ? Array.from(text) : text.split(\" \");\n    const joiner = byChar ? \"\" : \" \";\n    let line = \"\";\n    let yy = y;\n    for (const token of tokens) {\n      const test = line ? line + joiner + token : token;\n      if (ctx.measureText(test).width > maxWidth && line) {\n        ctx.fillText(line, drawX, yy);\n        line = token;\n        yy += lineHeight;\n      } else {\n        line = test;\n      }\n    }\n    if (line) ctx.fillText(line, drawX, yy);\n  }\n\n  function roundRect(x, y, w, h, r, fill, stroke) {\n    ctx.beginPath();\n    ctx.roundRect(x, y, w, h, r);\n    if (fill) ctx.fill();\n    if (stroke) ctx.stroke();\n  }\n\n  function tick(now) {\n    if (!lastTime) lastTime = now;\n    const frame = Math.min(0.05, (now - lastTime) / 1000);\n    lastTime = now;\n    accumulator += frame;\n    while (accumulator >= FIXED_DT) {\n      update(FIXED_DT);\n      accumulator -= FIXED_DT;\n    }\n    render();\n    requestAnimationFrame(tick);\n  }\n\n  resizeCanvas();\n  requestAnimationFrame(tick);\n})();\n</script>\n</body>\n</html>\n","deck-builder.html":"<!doctype html>\n<html lang=\"{{LANG}}\"><head><meta charset=\"utf-8\"><meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\"><title>{{TITLE}}</title><link rel=\"icon\" href=\"data:,\"><style>\nhtml,body{margin:0;width:100%;height:100%;overflow:hidden;background:{{BACKGROUND_A}};color:{{INK}};font-family:system-ui,-apple-system,BlinkMacSystemFont,\"Segoe UI\",sans-serif;touch-action:none;user-select:none}canvas{display:block;width:100vw;height:100vh;touch-action:none}\n</style></head><body>\n<!--\nGAME_SPEC_ID: {{ID}}\nGAME_KIT: deck-builder\nSTYLE_KIT: {{STYLE_KIT}}\nMECHANICS: {{MECHANICS}}\nENEMY_SET: {{ENEMY_SET}}\n-->\n<canvas id=\"game\" aria-label=\"{{TITLE}}\"></canvas><script>\n(() => {\n\"use strict\";\nconst SPEC = {{SPEC_JSON}},S=SPEC.strings,canvas=document.getElementById(\"game\"),ctx=canvas.getContext(\"2d\");\nconst READY=\"READY\",PLAYING=\"PLAYING\",OVER=\"OVER\",FIXED_DT=1/60;\nlet width=0,height=0,dpr=1,state=READY,last=0,acc=0,score=0,best=Number(localStorage.getItem(\"tgg-\"+SPEC.id+\"-best\")||0),timeLeft=SPEC.duration;\nlet energy=3,turn=1,playerHp=34,shield=0,enemyHp=52,enemyIntent=7,combo=0,shake=0,flash=0,notice=\"\",noticeTime=0;\nconst p=SPEC.palette,hand=[],cardRects=[],particles=[],floats=[];\nconst deck=[\n {id:\"pitch\",cost:1,type:\"attack\",value:7},\n {id:\"proof\",cost:1,type:\"shield\",value:6},\n {id:\"leverage\",cost:2,type:\"attack\",value:13},\n {id:\"focus\",cost:0,type:\"energy\",value:1},\n {id:\"close\",cost:3,type:\"attack\",value:22}\n];\nconst cardName=c=>(S.cards&&S.cards[c.id])||c.id;\nconst typeName=t=>(S.types&&S.types[t])||t;\nfunction resize(){dpr=Math.max(1,Math.min(2,devicePixelRatio||1));width=innerWidth;height=innerHeight;canvas.width=Math.floor(width*dpr);canvas.height=Math.floor(height*dpr);canvas.style.width=width+\"px\";canvas.style.height=height+\"px\";ctx.setTransform(dpr,0,0,dpr,0,0);layout();}\nfunction layout(){cardRects.length=0;const w=Math.min(142,width*.18),gap=14,total=hand.length*w+(hand.length-1)*gap,x0=(width-total)/2,y=height-155;for(let i=0;i<hand.length;i++)cardRects.push({x:x0+i*(w+gap),y,w,h:120,card:hand[i]});}\nfunction reset(){state=PLAYING;score=0;timeLeft=SPEC.duration;turn=1;playerHp=34;shield=0;enemyHp=52;combo=0;notice=\"\";particles.length=floats.length=0;newTurn();}\nfunction newTurn(){energy=3+Math.floor(turn/3);enemyIntent=7+Math.floor(turn*1.3);hand.length=0;for(let i=0;i<4;i++)hand.push({...deck[Math.floor(Math.random()*deck.length)]});layout();}\nfunction pointerdown(e){e.preventDefault();if(state===READY||state===OVER){reset();return}for(const r of cardRects)if(inside(e.clientX,e.clientY,r)){playCard(r.card,r.x+r.w/2,r.y+40);return}if(e.clientY<height*.32)endTurn();}\nfunction pointermove(e){e.preventDefault()}function pointerup(e){e.preventDefault()}\ncanvas.addEventListener(\"pointerdown\",pointerdown);canvas.addEventListener(\"pointermove\",pointermove);canvas.addEventListener(\"pointerup\",pointerup);canvas.addEventListener(\"pointercancel\",pointerup);addEventListener(\"resize\",resize);\nfunction playCard(card,x,y){if(card.cost>energy){flash=.25;notice=S.notEnough;noticeTime=1;return}energy-=card.cost;const idx=hand.indexOf(card);if(idx>=0)hand.splice(idx,1);if(card.type===\"attack\"){const dmg=Math.round((card.value+combo*2)*SPEC.tuning.scoreMultiplier);enemyHp-=dmg;score+=dmg;combo++;addFloat(\"+\"+dmg,x,y,p.good);burst(x,y,p.accent,18)}else if(card.type===\"shield\"){shield+=card.value;combo=0;addFloat(S.shield,x,y,p.ink);burst(x,y,p.good,12)}else{energy+=card.value;addFloat(\"+\"+S.energy,x,y,p.accent);burst(x,y,p.accent,10)}if(enemyHp<=0){score+=80;enemyHp=48+turn*6;notice=S.dealClosed;noticeTime=1.1;turn++;newTurn()}layout();}\nfunction endTurn(){const damage=Math.max(0,enemyIntent-shield);playerHp-=damage;shield=0;combo=0;turn++;notice=damage?S.demand+\" -\"+damage:S.blocked;noticeTime=1;shake=damage?.25:0;newTurn();if(playerHp<=0)over();}\nfunction update(dt){if(state!==PLAYING){updateFx(dt);return}timeLeft-=dt;if(timeLeft<=0)over();noticeTime=Math.max(0,noticeTime-dt);shake=Math.max(0,shake-dt*3);flash=Math.max(0,flash-dt*3);updateFx(dt)}\nfunction over(){state=OVER;best=Math.max(best,score);localStorage.setItem(\"tgg-\"+SPEC.id+\"-best\",String(best))}\nfunction updateFx(dt){for(const q of particles){q.x+=q.vx*dt;q.y+=q.vy*dt;q.life-=dt}for(const f of floats){f.y-=40*dt;f.life-=dt}trim(particles,q=>q.life<=0);trim(floats,f=>f.life<=0)}\nfunction render(){ctx.save();if(shake)ctx.translate((Math.random()-.5)*shake*18,(Math.random()-.5)*shake*18);bg();arena();cards();fx();hud();if(flash){ctx.fillStyle=alpha(p.bad,flash*.25);ctx.fillRect(0,0,width,height)}if(state!==PLAYING)overlay();ctx.restore()}\nfunction bg(){const g=ctx.createLinearGradient(0,0,0,height);g.addColorStop(0,p.backgroundA);g.addColorStop(1,p.backgroundB);ctx.fillStyle=g;ctx.fillRect(0,0,width,height);const v=ctx.createRadialGradient(width/2,height*.42,height*.2,width/2,height*.5,height*.95);v.addColorStop(0,\"rgba(0,0,0,0)\");v.addColorStop(1,\"rgba(0,0,0,.42)\");ctx.fillStyle=v;ctx.fillRect(0,0,width,height)}\nfunction arena(){const ax=width*.16,ay=height*.16,aw=width*.68,ah=height*.34;const g=ctx.createLinearGradient(0,ay,0,ay+ah);g.addColorStop(0,alpha(p.bad,.1));g.addColorStop(1,alpha(p.ink,.05));ctx.fillStyle=g;ctx.beginPath();ctx.roundRect(ax,ay,aw,ah,18);ctx.fill();ctx.strokeStyle=alpha(p.bad,.4);ctx.lineWidth=2;ctx.stroke();ctx.save();ctx.shadowColor=p.bad;ctx.shadowBlur=12;text(S.demand+\" \"+Math.max(0,Math.ceil(enemyHp)),width/2,height*.26,30,p.bad,\"center\");ctx.restore();text(S.turn+\" \"+enemyIntent,width/2,height*.34,16,p.ink,\"center\");if(noticeTime>0)text(notice,width/2,height*.44,17,p.accent,\"center\")}\nfunction cards(){for(const r of cardRects){const c=r.card,col=c.type===\"attack\"?p.bad:c.type===\"shield\"?p.good:p.accent;ctx.fillStyle=\"rgba(0,0,0,.25)\";ctx.beginPath();ctx.roundRect(r.x+2,r.y+3,r.w,r.h,12);ctx.fill();const g=ctx.createLinearGradient(0,r.y,0,r.y+r.h);g.addColorStop(0,alpha(col,.3));g.addColorStop(1,alpha(p.ink,.1));ctx.fillStyle=g;ctx.beginPath();ctx.roundRect(r.x,r.y,r.w,r.h,12);ctx.fill();ctx.strokeStyle=col;ctx.lineWidth=2.5;ctx.stroke();ctx.fillStyle=\"rgba(255,255,255,.12)\";ctx.beginPath();ctx.roundRect(r.x+4,r.y+4,r.w-8,16,7);ctx.fill();text(cardName(c),r.x+r.w/2,r.y+27,16,p.ink,\"center\");ctx.save();ctx.shadowColor=col;ctx.shadowBlur=8;ctx.fillStyle=col;ctx.beginPath();ctx.arc(r.x+r.w/2,r.y+60,15,0,Math.PI*2);ctx.fill();ctx.restore();text(String(c.cost),r.x+r.w/2,r.y+60,15,p.backgroundA,\"center\");text(typeName(c.type)+\" \"+c.value,r.x+r.w/2,r.y+92,13,p.ink,\"center\")}}\nfunction hud(){if(state===READY)return;text(SPEC.title,20,30,22,p.ink,\"left\");text(S.score+\" \"+score+\" \"+S.best+\" \"+best,20,58,13,p.ink,\"left\");text(S.hp+\" \"+playerHp+\" \"+S.shield+\" \"+shield+\" \"+S.energy+\" \"+energy,width-20,32,15,p.ink,\"right\");text(S.time.replace(\"{n}\",Math.ceil(timeLeft)),width-20,60,22,timeLeft<10?p.bad:p.ink,\"right\")}\nfunction fx(){for(const q of particles){ctx.globalAlpha=q.life*2;ctx.fillStyle=q.color;ctx.fillRect(q.x,q.y,q.size,q.size)}ctx.globalAlpha=1;for(const f of floats){ctx.globalAlpha=f.life;text(f.text,f.x,f.y,16,f.color,\"center\")}ctx.globalAlpha=1}\nfunction overlay(){ctx.fillStyle=\"rgba(0,0,0,.68)\";ctx.fillRect(0,0,width,height);text(SPEC.title,width/2,height*.44,36,p.ink,\"center\");text(SPEC.roles.objective,width/2,height*.44+42,15,p.ink,\"center\");text(state===OVER?S.restart:S.start,width/2,height*.44+96,25,p.accent,\"center\");if(state===OVER)text(S.score+\" \"+score+\" \"+S.best+\" \"+best,width/2,height*.44+132,15,p.ink,\"center\")}\nfunction inside(x,y,r){return x>=r.x&&x<=r.x+r.w&&y>=r.y&&y<=r.y+r.h}function burst(x,y,color,n){for(let i=0;i<n;i++){const a=Math.random()*Math.PI*2,s=40+Math.random()*110;particles.push({x,y,vx:Math.cos(a)*s,vy:Math.sin(a)*s,life:.5,size:3+Math.random()*5,color})}}function addFloat(t,x,y,c){floats.push({text:t,x,y,life:.9,color:c})}function trim(a,pred){for(let i=a.length-1;i>=0;i--)if(pred(a[i]))a.splice(i,1)}function alpha(hex,a){const n=parseInt(hex.slice(1),16);return`rgba(${n>>16&255},${n>>8&255},${n&255},${a})`}function text(t,x,y,s,c,a){ctx.fillStyle=c;ctx.font=`900 ${s}px system-ui`;ctx.textAlign=a;ctx.textBaseline=\"middle\";ctx.fillText(t,x,y)}\nfunction tick(now){if(!last)last=now;const frame=Math.min(.05,(now-last)/1000);last=now;acc+=frame;while(acc>=FIXED_DT){update(FIXED_DT);acc-=FIXED_DT}render();requestAnimationFrame(tick)}\nresize();requestAnimationFrame(tick);\n})();\n</script></body></html>\n","flappy-gap.html":"<!doctype html>\n<html lang=\"{{LANG}}\"><head><meta charset=\"utf-8\"><meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\"><title>{{TITLE}}</title><link rel=\"icon\" href=\"data:,\"><style>\nhtml,body{margin:0;width:100%;height:100%;overflow:hidden;background:{{BACKGROUND_A}};color:{{INK}};font-family:system-ui,-apple-system,BlinkMacSystemFont,\"Segoe UI\",sans-serif;touch-action:none;user-select:none}canvas{display:block;width:100vw;height:100vh;touch-action:none}\n</style></head><body>\n<!--\nGAME_SPEC_ID: {{ID}}\nGAME_KIT: flappy-gap\nSTYLE_KIT: {{STYLE_KIT}}\nMECHANICS: {{MECHANICS}}\nENEMY_SET: {{ENEMY_SET}}\n-->\n<canvas id=\"game\" aria-label=\"{{TITLE}}\"></canvas><script>\n(() => {\n\"use strict\";\nconst SPEC = {{SPEC_JSON}},S=SPEC.strings,canvas=document.getElementById(\"game\"),ctx=canvas.getContext(\"2d\"),READY=\"READY\",PLAYING=\"PLAYING\",OVER=\"OVER\",FIXED_DT=1/60;\nconst p=SPEC.palette,particles=[],floats=[],pipes=[];\nlet width=0,height=0,dpr=1,state=READY,last=0,acc=0,score=0,best=Number(localStorage.getItem(\"tgg-\"+SPEC.id+\"-best\")||0),timeLeft=SPEC.duration,shake=0,flash=0,scroll=0;\nconst bird={x:0,y:0,vy:0,r:18};let speed=160,spawnTimer=0,gap=0,pw=64;\nfunction resize(){dpr=Math.max(1,Math.min(2,devicePixelRatio||1));width=innerWidth;height=innerHeight;canvas.width=Math.floor(width*dpr);canvas.height=Math.floor(height*dpr);canvas.style.width=width+\"px\";canvas.style.height=height+\"px\";ctx.setTransform(dpr,0,0,dpr,0,0);bird.x=width*.3;gap=Math.max(150,height*.3);pw=Math.max(56,width*.09);if(state!==PLAYING)bird.y=height*.45}\nfunction reset(){state=PLAYING;score=0;timeLeft=SPEC.duration;speed=160*SPEC.tuning.speed;bird.y=height*.45;bird.vy=0;pipes.length=particles.length=floats.length=0;spawnTimer=.3;scroll=0}\nfunction flap(){bird.vy=-360}\nfunction pointerdown(e){e.preventDefault();if(state===READY||state===OVER){reset();return}flap()}\nfunction pointermove(e){e.preventDefault()}function pointerup(e){e.preventDefault()}\ncanvas.addEventListener(\"pointerdown\",pointerdown);canvas.addEventListener(\"pointermove\",pointermove);canvas.addEventListener(\"pointerup\",pointerup);canvas.addEventListener(\"pointercancel\",pointerup);addEventListener(\"resize\",resize);\nfunction spawn(){const m=90,gy=m+Math.random()*(height-gap-m*2-60);pipes.push({x:width+pw,gy,scored:false})}\nfunction update(dt){if(state!==PLAYING){updateFx(dt);return}timeLeft-=dt;if(timeLeft<=0)over();speed+=dt*3;scroll+=speed*dt;bird.vy+=1250*dt;bird.y+=bird.vy*dt;spawnTimer-=dt;if(spawnTimer<=0){spawn();spawnTimer=Math.max(.9,(gap+pw+170)/speed)}for(const pi of pipes){pi.x-=speed*dt;if(!pi.scored&&pi.x+pw<bird.x){pi.scored=true;score+=1;addFloat(\"+1\",bird.x,bird.y-30,p.good);burst(bird.x,bird.y,p.accent,8)}if(bird.x+bird.r>pi.x&&bird.x-bird.r<pi.x+pw&&(bird.y-bird.r<pi.gy||bird.y+bird.r>pi.gy+gap))crash()}trim(pipes,pi=>pi.x<-pw-10);if(bird.y+bird.r>height-30||bird.y-bird.r<0)crash();shake=Math.max(0,shake-dt*3);flash=Math.max(0,flash-dt*3);updateFx(dt)}\nfunction crash(){if(state!==PLAYING)return;shake=.4;flash=.5;burst(bird.x,bird.y,p.bad,24);over()}\nfunction over(){state=OVER;best=Math.max(best,score);localStorage.setItem(\"tgg-\"+SPEC.id+\"-best\",String(best))}\nfunction updateFx(dt){for(const q of particles){q.x+=q.vx*dt;q.y+=q.vy*dt;q.vy+=200*dt;q.life-=dt}for(const f of floats){f.y-=42*dt;f.life-=dt}trim(particles,q=>q.life<=0);trim(floats,f=>f.life<=0)}\nfunction render(){ctx.save();if(shake)ctx.translate((Math.random()-.5)*shake*16,(Math.random()-.5)*shake*16);bg();drawPipes();ground();drawBird();fx();hud();if(flash){ctx.fillStyle=alpha(p.bad,flash*.22);ctx.fillRect(0,0,width,height)}if(state!==PLAYING)overlay();ctx.restore()}\nfunction bg(){const g=ctx.createLinearGradient(0,0,0,height);g.addColorStop(0,p.backgroundA);g.addColorStop(1,p.backgroundB);ctx.fillStyle=g;ctx.fillRect(0,0,width,height);ctx.fillStyle=alpha(p.ink,.05);for(let i=0;i<5;i++){const x=((i*260-scroll*.3)%(width+260)+width+260)%(width+260)-130;ctx.beginPath();ctx.arc(x,height*.3+((i*53)%80),46,0,Math.PI*2);ctx.fill()}const v=ctx.createRadialGradient(width/2,height*.45,height*.2,width/2,height*.5,height*.95);v.addColorStop(0,\"rgba(0,0,0,0)\");v.addColorStop(1,\"rgba(0,0,0,.4)\");ctx.fillStyle=v;ctx.fillRect(0,0,width,height)}\nfunction pipe(x,y,h,cap){const g=ctx.createLinearGradient(x,0,x+pw,0);g.addColorStop(0,p.good);g.addColorStop(.5,lighten(p.good,.4));g.addColorStop(1,p.good);ctx.save();ctx.shadowColor=p.good;ctx.shadowBlur=12;ctx.fillStyle=g;ctx.beginPath();ctx.roundRect(x,y,pw,h,6);ctx.fill();ctx.beginPath();ctx.roundRect(x-6,cap,pw+12,18,6);ctx.fill();ctx.restore();ctx.strokeStyle=alpha(p.ink,.3);ctx.lineWidth=2;ctx.stroke()}\nfunction drawPipes(){for(const pi of pipes){pipe(pi.x,0,pi.gy,pi.gy-18);pipe(pi.x,pi.gy+gap,height-(pi.gy+gap),pi.gy+gap)}}\nfunction ground(){ctx.fillStyle=alpha(p.ink,.18);ctx.fillRect(0,height-30,width,30);ctx.fillStyle=alpha(p.ink,.3);for(let i=0;i<width/28+1;i++){const x=((i*28-scroll)%(width+28)+width+28)%(width+28)-28;ctx.fillRect(x,height-30,14,30)}}\nfunction drawBird(){const ang=Math.max(-.5,Math.min(1.1,bird.vy/520));ctx.save();ctx.translate(bird.x,bird.y);ctx.rotate(ang);ctx.fillStyle=\"rgba(0,0,0,.25)\";ctx.beginPath();ctx.ellipse(0,bird.r*.9,bird.r,bird.r*.45,0,0,Math.PI*2);ctx.fill();const g=ctx.createRadialGradient(-bird.r*.3,-bird.r*.3,3,0,0,bird.r);g.addColorStop(0,lighten(p.accent,.5));g.addColorStop(1,p.accent);ctx.shadowColor=p.accent;ctx.shadowBlur=14;ctx.fillStyle=g;ctx.beginPath();ctx.arc(0,0,bird.r,0,Math.PI*2);ctx.fill();ctx.shadowBlur=0;ctx.strokeStyle=p.ink;ctx.lineWidth=2.5;ctx.stroke();ctx.fillStyle=\"#f59e0b\";ctx.beginPath();ctx.moveTo(bird.r*.7,-2);ctx.lineTo(bird.r*1.4,2);ctx.lineTo(bird.r*.7,6);ctx.closePath();ctx.fill();ctx.fillStyle=\"#fff\";ctx.beginPath();ctx.arc(bird.r*.25,-bird.r*.25,bird.r*.32,0,Math.PI*2);ctx.fill();ctx.fillStyle=\"#10131a\";ctx.beginPath();ctx.arc(bird.r*.34,-bird.r*.25,bird.r*.15,0,Math.PI*2);ctx.fill();ctx.restore()}\nfunction fx(){for(const q of particles){ctx.globalAlpha=Math.max(0,q.life*2);ctx.fillStyle=q.color;ctx.fillRect(q.x,q.y,q.size,q.size)}ctx.globalAlpha=1;for(const f of floats){ctx.globalAlpha=Math.max(0,f.life);text(f.text,f.x,f.y,18,f.color,\"center\")}ctx.globalAlpha=1}\nfunction hud(){if(state===READY)return;text(SPEC.title,20,30,21,p.ink,\"left\");text(S.best+\" \"+best,20,58,13,p.ink,\"left\");text(String(score),width/2,80,46,p.ink,\"center\");text(S.time.replace(\"{n}\",Math.ceil(timeLeft)),width-20,32,20,timeLeft<10?p.bad:p.ink,\"right\")}\nfunction overlay(){ctx.fillStyle=\"rgba(0,0,0,.6)\";ctx.fillRect(0,0,width,height);text(SPEC.title,width/2,height*.38,34,p.ink,\"center\");text(SPEC.roles.objective,width/2,height*.38+40,15,p.ink,\"center\");text(state===OVER?S.restart:S.start,width/2,height*.38+92,24,p.accent,\"center\");if(state===OVER)text(S.score+\" \"+score+\"  \"+S.best+\" \"+best,width/2,height*.38+128,15,p.ink,\"center\")}\nfunction burst(x,y,c,n){for(let i=0;i<n;i++){const a=Math.random()*Math.PI*2,s=40+Math.random()*130;particles.push({x,y,vx:Math.cos(a)*s,vy:Math.sin(a)*s,life:.5,size:3+Math.random()*5,color:c})}}\nfunction addFloat(t,x,y,c){floats.push({text:t,x,y,life:.8,color:c})}\nfunction trim(a,pred){for(let i=a.length-1;i>=0;i--)if(pred(a[i]))a.splice(i,1)}\nfunction alpha(hex,a){const n=parseInt(hex.slice(1),16);return`rgba(${n>>16&255},${n>>8&255},${n&255},${a})`}\nfunction lighten(hex,t){const n=parseInt(hex.slice(1),16),r=n>>16&255,g=n>>8&255,b=n&255,m=v=>Math.round(v+(255-v)*t);return`rgb(${m(r)},${m(g)},${m(b)})`}\nfunction text(t,x,y,s,c,a){ctx.fillStyle=c;ctx.font=`900 ${s}px system-ui`;ctx.textAlign=a;ctx.textBaseline=\"middle\";ctx.fillText(t,x,y)}\nfunction tick(now){if(!last)last=now;const frame=Math.min(.05,(now-last)/1000);last=now;acc+=frame;while(acc>=FIXED_DT){update(FIXED_DT);acc-=FIXED_DT}render();requestAnimationFrame(tick)}\nresize();requestAnimationFrame(tick);\n})();\n</script></body></html>\n","grid-tactics.html":"<!doctype html>\n<html lang=\"{{LANG}}\"><head><meta charset=\"utf-8\"><meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\"><title>{{TITLE}}</title><link rel=\"icon\" href=\"data:,\"><style>\nhtml,body{margin:0;width:100%;height:100%;overflow:hidden;background:{{BACKGROUND_A}};color:{{INK}};font-family:system-ui,-apple-system,BlinkMacSystemFont,\"Segoe UI\",sans-serif;touch-action:none;user-select:none}canvas{display:block;width:100vw;height:100vh;touch-action:none}\n</style></head><body>\n<!--\nGAME_SPEC_ID: {{ID}}\nGAME_KIT: grid-tactics\nSTYLE_KIT: {{STYLE_KIT}}\nMECHANICS: {{MECHANICS}}\nENEMY_SET: {{ENEMY_SET}}\n-->\n<canvas id=\"game\" aria-label=\"{{TITLE}}\"></canvas><script>\n(() => {\n\"use strict\";\nconst SPEC = {{SPEC_JSON}},S=SPEC.strings,canvas=document.getElementById(\"game\"),ctx=canvas.getContext(\"2d\"),READY=\"READY\",PLAYING=\"PLAYING\",OVER=\"OVER\",FIXED_DT=1/60;\nlet width=0,height=0,dpr=1,state=READY,last=0,acc=0,score=0,best=Number(localStorage.getItem(\"tgg-\"+SPEC.id+\"-best\")||0),timeLeft=SPEC.duration,turn=1,locks=4,flash=0,notice=\"\",noticeTime=0;\nconst p=SPEC.palette,N=5,blocked=new Set(),secured=new Set(),enemies=[],particles=[],floats=[];let tile=74,ox=0,oy=0,player={x:2,y:4};\nfunction resize(){dpr=Math.max(1,Math.min(2,devicePixelRatio||1));width=innerWidth;height=innerHeight;canvas.width=Math.floor(width*dpr);canvas.height=Math.floor(height*dpr);canvas.style.width=width+\"px\";canvas.style.height=height+\"px\";ctx.setTransform(dpr,0,0,dpr,0,0);tile=Math.min(88,Math.floor(Math.min(width*.7,height*.68)/N));ox=(width-tile*N)/2;oy=(height-tile*N)/2+22}\nfunction reset(){state=PLAYING;score=0;timeLeft=SPEC.duration;turn=1;locks=4;player={x:2,y:4};blocked.clear();secured.clear();enemies.length=particles.length=floats.length=0;spawnEnemy();spawnEnemy()}\nfunction pointerdown(e){e.preventDefault();if(state===READY||state===OVER){reset();return}const gx=Math.floor((e.clientX-ox)/tile),gy=Math.floor((e.clientY-oy)/tile);if(gx<0||gx>=N||gy<0||gy>=N)return;const key=gx+\",\"+gy;if(Math.abs(gx-player.x)+Math.abs(gy-player.y)===1&&!blocked.has(key)){player.x=gx;player.y=gy;secured.add(key);score+=8;enemyTurn()}else if(locks>0&&!blocked.has(key)&&!(gx===player.x&&gy===player.y)){blocked.add(key);locks--;score+=12;burst(ox+gx*tile+tile/2,oy+gy*tile+tile/2,p.accent,12);enemyTurn()}else{flash=.18}}\nfunction pointermove(e){e.preventDefault()}function pointerup(e){e.preventDefault()}\ncanvas.addEventListener(\"pointerdown\",pointerdown);canvas.addEventListener(\"pointermove\",pointermove);canvas.addEventListener(\"pointerup\",pointerup);canvas.addEventListener(\"pointercancel\",pointerup);addEventListener(\"resize\",resize);\nfunction enemyTurn(){turn++;if(turn%3===0)spawnEnemy();for(const e of enemies){if(e.dead)continue;const dx=Math.sign(player.x-e.x),dy=Math.sign(player.y-e.y);const nx=e.x+(Math.abs(player.x-e.x)>Math.abs(player.y-e.y)?dx:0),ny=e.y+(Math.abs(player.y-e.y)>=Math.abs(player.x-e.x)?dy:0);const key=nx+\",\"+ny;if(!blocked.has(key)){e.x=nx;e.y=ny}if(e.x===player.x&&e.y===player.y){flash=.35;locks=Math.max(0,locks-1);notice=S.breach;noticeTime=.8;e.dead=true}}trim(enemies,e=>e.dead);if(enemies.length>7||locks<=0&&enemies.some(e=>Math.abs(e.x-player.x)+Math.abs(e.y-player.y)<=1))over()}\nfunction spawnEnemy(){const x=Math.floor(Math.random()*N);enemies.push({x,y:0,dead:false})}\nfunction update(dt){if(state!==PLAYING){updateFx(dt);return}timeLeft-=dt;if(timeLeft<=0)over();flash=Math.max(0,flash-dt*3);noticeTime=Math.max(0,noticeTime-dt);updateFx(dt)}\nfunction over(){state=OVER;best=Math.max(best,score);localStorage.setItem(\"tgg-\"+SPEC.id+\"-best\",String(best))}\nfunction updateFx(dt){for(const q of particles){q.x+=q.vx*dt;q.y+=q.vy*dt;q.life-=dt}for(const f of floats){f.y-=36*dt;f.life-=dt}trim(particles,q=>q.life<=0);trim(floats,f=>f.life<=0)}\nfunction render(){ctx.save();bg();grid();units();fx();hud();if(flash){ctx.fillStyle=alpha(p.bad,flash*.24);ctx.fillRect(0,0,width,height)}if(state!==PLAYING)overlay();ctx.restore()}\nfunction bg(){const bgg=ctx.createLinearGradient(0,0,0,height);bgg.addColorStop(0,p.backgroundA);bgg.addColorStop(1,p.backgroundB);ctx.fillStyle=bgg;ctx.fillRect(0,0,width,height);ctx.strokeStyle=alpha(p.good,.08);for(let x=0;x<width;x+=36){ctx.beginPath();ctx.moveTo(x,0);ctx.lineTo(x,height);ctx.stroke()}const v=ctx.createRadialGradient(width/2,height*.45,height*.2,width/2,height*.5,height*.95);v.addColorStop(0,\"rgba(0,0,0,0)\");v.addColorStop(1,\"rgba(0,0,0,.4)\");ctx.fillStyle=v;ctx.fillRect(0,0,width,height)}\nfunction grid(){for(let y=0;y<N;y++)for(let x=0;x<N;x++){const key=x+\",\"+y;ctx.fillStyle=blocked.has(key)?alpha(p.bad,.35):secured.has(key)?alpha(p.good,.22):alpha(p.ink,.06);ctx.beginPath();ctx.roundRect(ox+x*tile+5,oy+y*tile+5,tile-10,tile-10,10);ctx.fill();ctx.strokeStyle=alpha(p.ink,.24);ctx.stroke()}if(noticeTime>0)text(notice,width/2,oy-28,22,p.bad,\"center\")}\nfunction units(){for(const e of enemies){const cx=ox+e.x*tile+tile/2,cy=oy+e.y*tile+tile/2,r=tile*.3;ctx.fillStyle=\"rgba(0,0,0,.28)\";ctx.beginPath();ctx.ellipse(cx+2,cy+r*.7,r,r*.5,0,0,Math.PI*2);ctx.fill();const g=ctx.createRadialGradient(cx-r*.35,cy-r*.4,r*.15,cx,cy,r*1.2);g.addColorStop(0,lighten(p.bad,.5));g.addColorStop(1,p.bad);ctx.save();ctx.shadowColor=p.bad;ctx.shadowBlur=12;ctx.fillStyle=g;ctx.beginPath();ctx.roundRect(cx-r,cy-r,r*2,r*2,8);ctx.fill();ctx.restore();ctx.strokeStyle=p.ink;ctx.lineWidth=2.5;ctx.stroke();const ang=Math.atan2(player.y-e.y,player.x-e.x),ex=Math.cos(ang),ey=Math.sin(ang),er=r*.22,pr=er*.5;for(const s of[-1,1]){const eyx=cx+s*r*.32,eyy=cy-r*.1;ctx.fillStyle=\"#fff\";ctx.beginPath();ctx.arc(eyx,eyy,er,0,Math.PI*2);ctx.fill();ctx.fillStyle=\"#10131a\";ctx.beginPath();ctx.arc(eyx+ex*pr,eyy+ey*pr,pr,0,Math.PI*2);ctx.fill()}}const px=ox+player.x*tile+tile/2,py=oy+player.y*tile+tile/2,pr=tile*.28;ctx.fillStyle=\"rgba(0,0,0,.28)\";ctx.beginPath();ctx.ellipse(px+2,py+pr*.7,pr,pr*.5,0,0,Math.PI*2);ctx.fill();const pg=ctx.createRadialGradient(px-pr*.35,py-pr*.4,pr*.15,px,py,pr*1.2);pg.addColorStop(0,lighten(p.accent,.55));pg.addColorStop(1,p.accent);ctx.save();ctx.shadowColor=p.accent;ctx.shadowBlur=16;ctx.fillStyle=pg;ctx.beginPath();ctx.arc(px,py,pr,0,Math.PI*2);ctx.fill();ctx.restore();ctx.strokeStyle=p.ink;ctx.lineWidth=3;ctx.stroke()}\nfunction hud(){if(state===READY)return;text(SPEC.title,20,30,22,p.ink,\"left\");text(S.score+\" \"+score+\" \"+S.best+\" \"+best,20,58,13,p.ink,\"left\");text(S.turn+\" \"+turn+\" \"+S.locks+\" \"+locks,width-20,32,15,p.ink,\"right\");text(S.time.replace(\"{n}\",Math.ceil(timeLeft)),width-20,60,22,timeLeft<10?p.bad:p.ink,\"right\")}\nfunction fx(){for(const q of particles){ctx.globalAlpha=q.life*2;ctx.fillStyle=q.color;ctx.fillRect(q.x,q.y,q.size,q.size)}ctx.globalAlpha=1;for(const f of floats){ctx.globalAlpha=f.life;text(f.text,f.x,f.y,16,f.color,\"center\")}ctx.globalAlpha=1}\nfunction overlay(){ctx.fillStyle=\"rgba(0,0,0,.70)\";ctx.fillRect(0,0,width,height);text(SPEC.title,width/2,height*.43,36,p.ink,\"center\");text(SPEC.roles.objective,width/2,height*.43+42,15,p.ink,\"center\");text(state===OVER?S.restart:S.start,width/2,height*.43+94,25,p.accent,\"center\");if(state===OVER)text(S.score+\" \"+score+\" \"+S.best+\" \"+best,width/2,height*.43+130,15,p.ink,\"center\")}\nfunction burst(x,y,c,n){for(let i=0;i<n;i++){const a=Math.random()*Math.PI*2,s=35+Math.random()*100;particles.push({x,y,vx:Math.cos(a)*s,vy:Math.sin(a)*s,life:.5,size:3+Math.random()*5,color:c})}}function trim(a,pred){for(let i=a.length-1;i>=0;i--)if(pred(a[i]))a.splice(i,1)}function alpha(hex,a){const n=parseInt(hex.slice(1),16);return`rgba(${n>>16&255},${n>>8&255},${n&255},${a})`}function lighten(hex,t){const n=parseInt(hex.slice(1),16),r=n>>16&255,g=n>>8&255,b=n&255,m=v=>Math.round(v+(255-v)*t);return`rgb(${m(r)},${m(g)},${m(b)})`}function text(t,x,y,s,c,a){ctx.fillStyle=c;ctx.font=`900 ${s}px system-ui`;ctx.textAlign=a;ctx.textBaseline=\"middle\";ctx.fillText(t,x,y)}\nfunction tick(now){if(!last)last=now;const frame=Math.min(.05,(now-last)/1000);last=now;acc+=frame;while(acc>=FIXED_DT){update(FIXED_DT);acc-=FIXED_DT}render();requestAnimationFrame(tick)}resize();requestAnimationFrame(tick);\n})();\n</script></body></html>\n","horde-survival.html":"<!doctype html>\n<html lang=\"{{LANG}}\">\n<head>\n<meta charset=\"utf-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>{{TITLE}}</title>\n<link rel=\"icon\" href=\"data:,\">\n<style>\nhtml,body{margin:0;width:100%;height:100%;overflow:hidden;background:{{BACKGROUND_A}};color:{{INK}};font-family:system-ui,-apple-system,BlinkMacSystemFont,\"Segoe UI\",sans-serif;touch-action:none;user-select:none}\ncanvas{display:block;width:100vw;height:100vh;touch-action:none}\n</style>\n</head>\n<body>\n<!--\nGAME_SPEC_ID: {{ID}}\nGAME_KIT: horde-survival\nSTYLE_KIT: {{STYLE_KIT}}\nMECHANICS: {{MECHANICS}}\nENEMY_SET: {{ENEMY_SET}}\n-->\n<canvas id=\"game\" aria-label=\"{{TITLE}}\"></canvas>\n<script>\n(() => {\n  \"use strict\";\n  const SPEC = {{SPEC_JSON}};\n  const S = SPEC.strings;\n  const canvas = document.getElementById(\"game\");\n  const ctx = canvas.getContext(\"2d\");\n  const STATE_READY = \"READY\";\n  const STATE_PLAYING = \"PLAYING\";\n  const STATE_LEVEL = \"LEVEL\";\n  const STATE_OVER = \"OVER\";\n  const FIXED_DT = 1 / 60;\n  let width = 0, height = 0, dpr = 1, state = STATE_READY, lastTime = 0, acc = 0;\n  let score = 0, best = Number(localStorage.getItem(\"tgg-\" + SPEC.id + \"-best\") || 0);\n  let timeLeft = SPEC.duration, spawnTimer = 0, fireTimer = 0, combo = 0, comboTimer = 0;\n  let upgradeChoices = [], cardRects = [], shake = 0, flash = 0, toast = \"\", toastTime = 0;\n  const pointer = { x: 0, y: 0, moved: false };\n  const player = { x: 0, y: 0, tx: 0, ty: 0, r: 27, hp: 5, maxHp: 5, level: 1, xp: 0, xpGoal: 8, invuln: 0, speed: 350, shots: 1, fireDelay: 0.38, magnet: 100 };\n  const enemies = [], bullets = [], xp = [], particles = [], floats = [];\n  const palette = SPEC.palette;\n  const enemyTypes = SPEC.enemyTypes;\n  const upgrades = [\n    { id: \"shots\", max: 4 },\n    { id: \"speed\", max: 3 },\n    { id: \"fire\", max: 4 },\n    { id: \"magnet\", max: 3 },\n    { id: \"heart\", max: 3 }\n  ];\n  const upgradeLevels = Object.fromEntries(upgrades.map(u => [u.id, 0]));\n\n  function resize() {\n    dpr = Math.max(1, Math.min(2, window.devicePixelRatio || 1));\n    width = innerWidth; height = innerHeight;\n    canvas.width = Math.floor(width * dpr); canvas.height = Math.floor(height * dpr);\n    canvas.style.width = width + \"px\"; canvas.style.height = height + \"px\";\n    ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n    if (!pointer.moved) { pointer.x = width / 2; pointer.y = height / 2; }\n    if (state === STATE_READY) { player.x = width / 2; player.y = height / 2; player.tx = player.x; player.ty = player.y; }\n  }\n\n  function reset() {\n    state = STATE_PLAYING; score = 0; timeLeft = SPEC.duration; spawnTimer = 0.2; fireTimer = 0.1; combo = 0; comboTimer = 0; toast = \"\";\n    enemies.length = bullets.length = xp.length = particles.length = floats.length = 0;\n    Object.assign(player, { x: width / 2, y: height / 2, tx: pointer.moved ? pointer.x : width / 2, ty: pointer.moved ? pointer.y : height / 2, hp: 5, maxHp: 5, level: 1, xp: 0, xpGoal: 8, invuln: 0, speed: 350, shots: 1, fireDelay: 0.38, magnet: 100 });\n    for (const key of Object.keys(upgradeLevels)) upgradeLevels[key] = 0;\n    for (let i = 0; i < 9; i++) spawnEnemy(true);\n  }\n\n  function pointerDown(e) {\n    e.preventDefault(); pointer.moved = true; pointer.x = e.clientX; pointer.y = e.clientY;\n    if (state === STATE_READY || state === STATE_OVER) { reset(); return; }\n    if (state === STATE_LEVEL) { chooseCard(pointer.x, pointer.y); return; }\n    player.tx = pointer.x; player.ty = pointer.y;\n  }\n  function pointerMove(e) {\n    e.preventDefault(); pointer.moved = true; pointer.x = e.clientX; pointer.y = e.clientY;\n    if (state === STATE_PLAYING) { player.tx = pointer.x; player.ty = pointer.y; }\n  }\n  canvas.addEventListener(\"pointerdown\", pointerDown); canvas.addEventListener(\"pointermove\", pointerMove); canvas.addEventListener(\"pointerup\", e => e.preventDefault()); canvas.addEventListener(\"pointercancel\", e => e.preventDefault()); addEventListener(\"resize\", resize);\n\n  function spawnEnemy(opening) {\n    const type = enemyTypes[Math.floor(Math.random() * enemyTypes.length)] || \"drowsy\";\n    const def = enemyDef(type);\n    const side = Math.floor(Math.random() * 4), m = 64; let x = 0, y = 0;\n    if (side === 0) { x = Math.random() * width; y = -m; } else if (side === 1) { x = width + m; y = Math.random() * height; } else if (side === 2) { x = Math.random() * width; y = height + m; } else { x = -m; y = Math.random() * height; }\n    if (opening) { const a = Math.random() * Math.PI * 2, d = 230 + Math.random() * 160; x = player.x + Math.cos(a) * d; y = player.y + Math.sin(a) * d; }\n    const elapsed = SPEC.duration - timeLeft, scale = 1 + elapsed / 80;\n    enemies.push({ x, y, r: def.r, hp: Math.ceil(def.hp * scale * SPEC.tuning.spawnRate), maxHp: Math.ceil(def.hp * scale), speed: def.speed * SPEC.tuning.speed + elapsed * 1.4, type, hit: 0, wind: 0, dash: 0, vx: 0, vy: 0 });\n  }\n  function enemyDef(type) {\n    const defs = {\n      foamling: { r: 15, hp: 1, speed: 110, color: palette.ink, label: \".\" },\n      drowsy: { r: 22, hp: 2, speed: 82, color: palette.bad, label: \"Z\" },\n      jitter: { r: 18, hp: 2, speed: 126, color: palette.good, label: \"!\" },\n      mug: { r: 29, hp: 5, speed: 64, color: palette.accent, label: \"M\" },\n      charger: { r: 23, hp: 3, speed: 78, color: palette.bad, label: \">\" },\n      splitter: { r: 24, hp: 3, speed: 72, color: palette.accent, label: \"S\" },\n      eliteAlarm: { r: 36, hp: 12, speed: 58, color: palette.accent, label: \"E\" },\n      bug: { r: 22, hp: 2, speed: 86, color: palette.bad, label: \"B\" },\n      popup: { r: 24, hp: 3, speed: 70, color: palette.accent, label: \"P\" },\n      lagSpike: { r: 19, hp: 2, speed: 120, color: palette.good, label: \"L\" }\n    };\n    return defs[type] || defs.drowsy;\n  }\n\n  function update(dt) {\n    if (state === STATE_READY || state === STATE_OVER) { updateEffects(dt); return; }\n    if (state === STATE_LEVEL) { updateEffects(dt * 0.2); return; }\n    timeLeft -= dt; if (timeLeft <= 0 || player.hp <= 0) return end();\n    movePlayer(dt); player.invuln = Math.max(0, player.invuln - dt); comboTimer = Math.max(0, comboTimer - dt); if (!comboTimer) combo = 0;\n    shake = Math.max(0, shake - dt * 3); flash = Math.max(0, flash - dt * 3); toastTime = Math.max(0, toastTime - dt);\n    spawnTimer -= dt; if (spawnTimer <= 0) { const count = timeLeft < SPEC.duration * 0.35 ? 3 : timeLeft < SPEC.duration * 0.65 ? 2 : 1; for (let i = 0; i < count; i++) spawnEnemy(false); spawnTimer = Math.max(0.12, 0.58 - (SPEC.duration - timeLeft) * 0.008) / SPEC.tuning.spawnRate; }\n    fireTimer -= dt; if (fireTimer <= 0) { fire(); fireTimer = player.fireDelay; }\n    updateEnemies(dt); updateBullets(dt); updateXp(dt); updateEffects(dt);\n  }\n  function movePlayer(dt) {\n    const dx = player.tx - player.x, dy = player.ty - player.y, len = Math.hypot(dx, dy);\n    if (len > 1) { const step = Math.min(len, player.speed * dt); player.x += dx / len * step; player.y += dy / len * step; }\n    player.x = clamp(player.x, player.r + 10, width - player.r - 10); player.y = clamp(player.y, player.r + 78, height - player.r - 12);\n  }\n  function fire() {\n    const target = nearestEnemy(); if (!target) return;\n    const base = Math.atan2(target.y - player.y, target.x - player.x), spread = player.shots === 1 ? 0 : 0.18;\n    for (let i = 0; i < player.shots; i++) { const a = base + (i - (player.shots - 1) / 2) * spread; bullets.push({ x: player.x, y: player.y, vx: Math.cos(a) * 540, vy: Math.sin(a) * 540, r: 7, life: 1.2, damage: 1 }); }\n    burst(player.x, player.y, palette.ink, 5, 90);\n  }\n  function nearestEnemy() { let bestEnemy = null, bestDist = Infinity; for (const e of enemies) { const d = dist(player.x, player.y, e.x, e.y); if (d < bestDist) { bestDist = d; bestEnemy = e; } } return bestEnemy; }\n  function updateEnemies(dt) {\n    for (const e of enemies) {\n      if (e.type === \"charger\" && dist(e.x, e.y, player.x, player.y) < 250 && e.dash <= 0) { e.wind += dt; if (e.wind > 0.45) { const a = Math.atan2(player.y - e.y, player.x - e.x); e.vx = Math.cos(a) * e.speed * 4; e.vy = Math.sin(a) * e.speed * 4; e.dash = 0.42; e.wind = 0; } }\n      if (e.dash > 0) { e.x += e.vx * dt; e.y += e.vy * dt; e.dash -= dt; } else { const a = Math.atan2(player.y - e.y, player.x - e.x); e.x += Math.cos(a) * e.speed * dt; e.y += Math.sin(a) * e.speed * dt; }\n      e.hit = Math.max(0, e.hit - dt * 5);\n      if (dist(e.x, e.y, player.x, player.y) < e.r + player.r && player.invuln <= 0) { player.hp -= e.type === \"eliteAlarm\" || e.type === \"mug\" ? 2 : 1; player.invuln = 0.8; combo = 0; shake = 0.4; flash = 0.5; addFloat(\"-\" + S.hp, player.x, player.y - 40, palette.bad); }\n    }\n  }\n  function updateBullets(dt) {\n    for (const b of bullets) { b.x += b.vx * dt; b.y += b.vy * dt; b.life -= dt; }\n    for (const b of bullets) for (const e of enemies) if (b.life > 0 && dist(b.x, b.y, e.x, e.y) < b.r + e.r) { e.hp -= b.damage; e.hit = 1; b.life = 0; burst(b.x, b.y, palette.ink, 7, 120); if (e.hp <= 0) kill(e); break; }\n    trim(bullets, b => b.life <= 0 || b.x < -80 || b.x > width + 80 || b.y < -80 || b.y > height + 80); trim(enemies, e => e.hp <= 0);\n  }\n  function kill(e) { combo++; comboTimer = 2.4; score += Math.round(10 * comboMult() * SPEC.tuning.scoreMultiplier); addFloat(\"+\" + Math.round(10 * comboMult() * SPEC.tuning.scoreMultiplier), e.x, e.y - 24, palette.ink); burst(e.x, e.y, enemyDef(e.type).color, 18, 180); for (let i = 0; i < (e.type === \"eliteAlarm\" ? 6 : e.type === \"mug\" ? 3 : 1); i++) xp.push({ x: e.x, y: e.y, r: 8, vx: (Math.random() - 0.5) * 70, vy: (Math.random() - 0.5) * 70, life: 8 }); if (e.type === \"splitter\") for (let i = 0; i < 2; i++) enemies.push({ x: e.x + (Math.random() - 0.5) * 30, y: e.y + (Math.random() - 0.5) * 30, r: 13, hp: 1, maxHp: 1, speed: 130, type: \"foamling\", hit: 0, wind: 0, dash: 0, vx: 0, vy: 0 }); }\n  function updateXp(dt) {\n    for (const orb of xp) { const d = dist(orb.x, orb.y, player.x, player.y); if (d < player.magnet) { const a = Math.atan2(player.y - orb.y, player.x - orb.x); orb.vx += Math.cos(a) * 500 * dt; orb.vy += Math.sin(a) * 500 * dt; } orb.x += orb.vx * dt; orb.y += orb.vy * dt; orb.vx *= 0.96; orb.vy *= 0.96; orb.life -= dt; if (d < player.r + orb.r) { orb.life = 0; player.xp += 1; score += 2; if (player.xp >= player.xpGoal) levelUp(); } }\n    trim(xp, o => o.life <= 0);\n  }\n  function levelUp() { state = STATE_LEVEL; player.xp -= player.xpGoal; player.level++; player.xpGoal = Math.ceil(player.xpGoal * 1.35 + 4); upgradeChoices = rollUpgrades(); toast = S.levelUp; toastTime = 1.2; burst(player.x, player.y, palette.accent, 38, 240); }\n  function rollUpgrades() { return upgrades.filter(u => upgradeLevels[u.id] < u.max).sort(() => Math.random() - 0.5).slice(0, 3); }\n  function chooseCard(x, y) { for (const c of cardRects) if (x >= c.x && x <= c.x + c.w && y >= c.y && y <= c.y + c.h) { const u = c.upgrade; upgradeLevels[u.id]++; if (u.id === \"shots\") player.shots++; if (u.id === \"speed\") player.speed += 42; if (u.id === \"fire\") player.fireDelay = Math.max(0.14, player.fireDelay - 0.05); if (u.id === \"magnet\") player.magnet += 45; if (u.id === \"heart\") { player.maxHp++; player.hp = Math.min(player.maxHp, player.hp + 2); } state = STATE_PLAYING; return; } }\n  function updateEffects(dt) { for (const p of particles) { p.x += p.vx * dt; p.y += p.vy * dt; p.vy += 90 * dt; p.life -= dt; } for (const f of floats) { f.y += f.vy * dt; f.life -= dt; } trim(particles, p => p.life <= 0); trim(floats, f => f.life <= 0); }\n  function end() { state = STATE_OVER; best = Math.max(best, score); localStorage.setItem(\"tgg-\" + SPEC.id + \"-best\", String(best)); }\n\n  function render() {\n    ctx.save(); if (shake > 0) ctx.translate((Math.random() - 0.5) * shake * 18, (Math.random() - 0.5) * shake * 18);\n    background(); drawOrbs(); drawEnemies(); drawBullets(); drawPlayer(); drawEffects(); hud();\n    if (flash > 0) { ctx.fillStyle = hexAlpha(palette.bad, flash * 0.18); ctx.fillRect(0, 0, width, height); }\n    if (state === STATE_READY || state === STATE_OVER) overlay(); if (state === STATE_LEVEL) levelOverlay(); ctx.restore();\n  }\n  function background() { const g = ctx.createLinearGradient(0, 0, width, height); g.addColorStop(0, palette.backgroundA); g.addColorStop(1, palette.backgroundB); ctx.fillStyle = g; ctx.fillRect(0, 0, width, height); ctx.fillStyle = \"rgba(255,255,255,.055)\"; for (let y = 0; y < height; y += 42) ctx.fillRect(0, y, width, 1); }\n  const SHAPE_LIST = [\"circle\", \"blob\", \"triangle\", \"square\", \"diamond\", \"hex\", \"pentagon\", \"star\"];\n  const SHAPE_MAP = { foamling: \"circle\", drowsy: \"blob\", jitter: \"triangle\", mug: \"square\", charger: \"diamond\", splitter: \"hex\", eliteAlarm: \"star\", bug: \"triangle\", popup: \"square\", lagSpike: \"diamond\", corruptBlock: \"hex\", firewallElite: \"star\" };\n  function enemyShape(type) { if (SHAPE_MAP[type]) return SHAPE_MAP[type]; let h = 0; for (const c of String(type)) h = (h * 31 + c.charCodeAt(0)) >>> 0; return SHAPE_LIST[h % SHAPE_LIST.length]; }\n  function shapePath(shape, r) {\n    ctx.beginPath();\n    if (shape === \"circle\") { ctx.arc(0, 0, r, 0, Math.PI * 2); return; }\n    if (shape === \"square\") { ctx.roundRect(-r, -r, r * 2, r * 2, r * 0.3); return; }\n    if (shape === \"blob\") { ctx.roundRect(-r, -r, r * 2, r * 2, r * 0.72); return; }\n    if (shape === \"star\") { for (let i = 0; i < 10; i++) { const rr = i % 2 ? r * 0.46 : r; const a = -Math.PI / 2 + i * Math.PI / 5; const x = Math.cos(a) * rr, y = Math.sin(a) * rr; i ? ctx.lineTo(x, y) : ctx.moveTo(x, y); } ctx.closePath(); return; }\n    const n = shape === \"triangle\" ? 3 : shape === \"diamond\" ? 4 : shape === \"pentagon\" ? 5 : shape === \"hex\" ? 6 : 0;\n    if (!n) { ctx.arc(0, 0, r, 0, Math.PI * 2); return; }\n    const rot = shape === \"diamond\" ? 0 : -Math.PI / 2;\n    for (let i = 0; i < n; i++) { const a = rot + i * Math.PI * 2 / n; const x = Math.cos(a) * r, y = Math.sin(a) * r; i ? ctx.lineTo(x, y) : ctx.moveTo(x, y); }\n    ctx.closePath();\n  }\n  function lighten(hex, t) { const n = parseInt(hex.slice(1), 16); const r = n >> 16 & 255, g = n >> 8 & 255, b = n & 255; const m = v => Math.round(v + (255 - v) * t); return `rgb(${m(r)},${m(g)},${m(b)})`; }\n  function drawEnemies() {\n    for (const e of enemies) {\n      const def = enemyDef(e.type); const shape = enemyShape(e.type); const elite = e.type === \"eliteAlarm\" || /elite/i.test(e.type);\n      ctx.save(); ctx.translate(e.x, e.y); ctx.scale(1 + e.hit * 0.12, 1 + e.hit * 0.12);\n      ctx.fillStyle = \"rgba(0,0,0,.28)\"; ctx.beginPath(); ctx.ellipse(4, 10, e.r * 1.05, e.r * .6, 0, 0, Math.PI * 2); ctx.fill();\n      const g = ctx.createRadialGradient(-e.r * .35, -e.r * .4, e.r * .12, 0, 0, e.r * 1.15);\n      g.addColorStop(0, lighten(def.color, .5)); g.addColorStop(.55, def.color); g.addColorStop(1, hexAlpha(def.color, .92));\n      ctx.shadowColor = def.color; ctx.shadowBlur = elite ? 24 : 13;\n      shapePath(shape, e.r); ctx.fillStyle = g; ctx.fill();\n      ctx.shadowBlur = 0;\n      ctx.strokeStyle = palette.ink; ctx.lineWidth = elite ? 5 : 3; ctx.stroke();\n      const ang = Math.atan2(player.y - e.y, player.x - e.x), ex = Math.cos(ang), ey = Math.sin(ang);\n      const er = Math.max(2.5, e.r * .2), pr = er * .52;\n      for (const s of [-1, 1]) {\n        const cx = s * e.r * .32, cy = -e.r * .1;\n        ctx.fillStyle = \"#fff\"; ctx.beginPath(); ctx.arc(cx, cy, er, 0, Math.PI * 2); ctx.fill();\n        ctx.fillStyle = \"#10131a\"; ctx.beginPath(); ctx.arc(cx + ex * pr, cy + ey * pr, pr, 0, Math.PI * 2); ctx.fill();\n      }\n      if (e.hit > 0.05) { shapePath(shape, e.r); ctx.fillStyle = hexAlpha(palette.ink, e.hit * 0.5); ctx.fill(); }\n      ctx.restore();\n    }\n  }\n  function drawBullets() { ctx.fillStyle = palette.ink; for (const b of bullets) { ctx.beginPath(); ctx.ellipse(b.x, b.y, b.r * 1.7, b.r, 0, 0, Math.PI * 2); ctx.fill(); } }\n  function drawOrbs() { ctx.fillStyle = palette.good; for (const o of xp) { ctx.beginPath(); ctx.arc(o.x, o.y, o.r, 0, Math.PI * 2); ctx.fill(); } }\n  function drawPlayer() {\n    const target = nearestEnemy(); const aim = target ? Math.atan2(target.y - player.y, target.x - player.x) : -Math.PI / 2;\n    ctx.save(); ctx.translate(player.x, player.y);\n    ctx.fillStyle = \"rgba(0,0,0,.25)\"; ctx.beginPath(); ctx.ellipse(4, 9, player.r * 1.05, player.r * .68, 0, 0, Math.PI * 2); ctx.fill();\n    if (player.invuln > 0) { ctx.fillStyle = hexAlpha(palette.good, .28); ctx.beginPath(); ctx.arc(0, 0, player.r * 1.22, 0, Math.PI * 2); ctx.fill(); }\n    ctx.rotate(aim);\n    ctx.fillStyle = palette.ink; ctx.beginPath(); ctx.roundRect(0, -player.r * .26, player.r * 1.35, player.r * .52, player.r * .26); ctx.fill();\n    const pg = ctx.createRadialGradient(-player.r * .3, -player.r * .35, player.r * .1, 0, 0, player.r * 1.1);\n    pg.addColorStop(0, lighten(palette.accent, .55)); pg.addColorStop(1, palette.accent);\n    ctx.shadowColor = palette.accent; ctx.shadowBlur = 16;\n    ctx.fillStyle = pg; ctx.beginPath(); ctx.arc(0, 0, player.r, 0, Math.PI * 2); ctx.fill();\n    ctx.shadowBlur = 0;\n    ctx.strokeStyle = palette.ink; ctx.lineWidth = 3; ctx.stroke();\n    ctx.fillStyle = palette.ink; ctx.beginPath(); ctx.arc(player.r * .34, 0, player.r * .17, 0, Math.PI * 2); ctx.fill();\n    ctx.restore();\n  }\n  function drawEffects() { for (const p of particles) { ctx.globalAlpha = Math.max(0, p.life * 2); ctx.fillStyle = p.color; ctx.fillRect(p.x - p.size / 2, p.y - p.size / 2, p.size, p.size); } ctx.globalAlpha = 1; for (const f of floats) { ctx.globalAlpha = Math.max(0, f.life); ctx.fillStyle = f.color; ctx.font = \"900 16px system-ui\"; ctx.textAlign = \"center\"; ctx.fillText(f.text, f.x, f.y); } ctx.globalAlpha = 1; }\n  function hud() { if (state === STATE_READY) return; panel(14, 12, 240, 88); text(SPEC.title, 28, 34, 19, palette.ink, \"left\"); text(`${S.score} ${score}  ${S.best} ${best}`, 28, 62, 13, palette.ink, \"left\"); bar(28, 84, 190, 8, player.hp / player.maxHp, palette.good); panel(width - 224, 12, 210, 88); text(formatTime(timeLeft), width - 34, 34, 22, timeLeft < 10 ? palette.bad : palette.ink, \"right\"); text(`${S.level} ${player.level} x${comboMult()} ${combo} ${S.combo}`, width - 34, 62, 13, palette.ink, \"right\"); bar(width - 204, 84, 170, 8, player.xp / player.xpGoal, palette.accent); }\n  function overlay() { ctx.fillStyle = \"rgba(0,0,0,.68)\"; ctx.fillRect(0, 0, width, height); text(SPEC.title, width / 2, height * .42, 36, palette.ink, \"center\"); text(SPEC.roles.player + \" / \" + SPEC.roles.objective, width / 2, height * .42 + 40, 15, palette.ink, \"center\"); text(state === STATE_OVER ? S.restart : S.start, width / 2, height * .42 + 92, 24, palette.accent, \"center\"); if (state === STATE_OVER) text(`${S.score} ${score}  ${S.best} ${best}`, width / 2, height * .42 + 128, 15, palette.ink, \"center\"); }\n  function levelOverlay() { ctx.fillStyle = \"rgba(0,0,0,.72)\"; ctx.fillRect(0, 0, width, height); text(`${S.levelUp}: ${S.chooseCard}`, width / 2, height * .24, 30, palette.ink, \"center\"); cardRects = []; const cw = Math.min(220, width * .27), ch = 180, gap = 18, total = upgradeChoices.length * cw + (upgradeChoices.length - 1) * gap; let x = width / 2 - total / 2, y = height * .38; for (const u of upgradeChoices) { const label = upgradeLabel(u); cardRects.push({ x, y, w: cw, h: ch, upgrade: u }); panel(x, y, cw, ch); ctx.strokeStyle = palette.accent; ctx.lineWidth = 3; ctx.strokeRect(x + 4, y + 4, cw - 8, ch - 8); text(label[0], x + cw / 2, y + 48, 21, palette.ink, \"center\"); text(`${S.level} ${upgradeLevels[u.id]}/${u.max}`, x + cw / 2, y + 80, 13, palette.accent, \"center\"); wrap(label[1], x + 22, y + 110, cw - 44, 17, palette.ink); x += cw + gap; } }\n  function upgradeLabel(upgrade) { return S.upgrades && S.upgrades[upgrade.id] ? S.upgrades[upgrade.id] : [upgrade.id, upgrade.id]; }\n  function formatTime(value) { return S.time.replace(\"{n}\", Math.ceil(value)); }\n  function panel(x, y, w, h) { ctx.fillStyle = \"rgba(0,0,0,.45)\"; ctx.beginPath(); ctx.roundRect(x, y, w, h, 15); ctx.fill(); }\n  function bar(x, y, w, h, ratio, color) { ctx.fillStyle = \"rgba(255,255,255,.16)\"; ctx.beginPath(); ctx.roundRect(x, y, w, h, h / 2); ctx.fill(); ctx.fillStyle = color; ctx.beginPath(); ctx.roundRect(x, y, w * clamp(ratio, 0, 1), h, h / 2); ctx.fill(); }\n  function text(s, x, y, size, color, align) { ctx.fillStyle = color; ctx.font = `900 ${size}px system-ui`; ctx.textAlign = align; ctx.textBaseline = \"middle\"; ctx.fillText(s, x, y); }\n  function wrap(s, x, y, w, lh, color) { ctx.fillStyle = color; ctx.font = \"700 13px system-ui\"; ctx.textAlign = \"left\"; ctx.textBaseline = \"top\"; let line = \"\", yy = y; for (const ch of s) { const next = line + ch; if (ctx.measureText(next).width > w && line) { ctx.fillText(line, x, yy); line = ch; yy += lh; } else line = next; } if (line) ctx.fillText(line, x, yy); }\n  function burst(x, y, color, count, power) { for (let i = 0; i < count; i++) { const a = Math.random() * Math.PI * 2, s = 30 + Math.random() * power; particles.push({ x, y, vx: Math.cos(a) * s, vy: Math.sin(a) * s, life: .35 + Math.random() * .3, size: 3 + Math.random() * 5, color }); } }\n  function addFloat(text, x, y, color) { floats.push({ text, x, y, vy: -42, life: .9, color }); }\n  function comboMult() { return Math.min(5, 1 + Math.floor(combo / 12)); }\n  function trim(list, pred) { for (let i = list.length - 1; i >= 0; i--) if (pred(list[i])) list.splice(i, 1); }\n  function dist(ax, ay, bx, by) { return Math.hypot(ax - bx, ay - by); }\n  function clamp(v, min, max) { return Math.max(min, Math.min(max, v)); }\n  function hexAlpha(hex, a) { const n = parseInt(hex.slice(1), 16); return `rgba(${n >> 16 & 255},${n >> 8 & 255},${n & 255},${a})`; }\n  function tick(now) { if (!lastTime) lastTime = now; const frame = Math.min(.05, (now - lastTime) / 1000); lastTime = now; acc += frame; while (acc >= FIXED_DT) { update(FIXED_DT); acc -= FIXED_DT; } render(); requestAnimationFrame(tick); }\n  resize(); requestAnimationFrame(tick);\n})();\n</script>\n</body>\n</html>\n","idle-automation.html":"<!doctype html>\n<html lang=\"{{LANG}}\"><head><meta charset=\"utf-8\"><meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\"><title>{{TITLE}}</title><link rel=\"icon\" href=\"data:,\"><style>\nhtml,body{margin:0;width:100%;height:100%;overflow:hidden;background:{{BACKGROUND_A}};color:{{INK}};font-family:system-ui,-apple-system,BlinkMacSystemFont,\"Segoe UI\",sans-serif;touch-action:none;user-select:none}canvas{display:block;width:100vw;height:100vh;touch-action:none}\n</style></head><body>\n<!--\nGAME_SPEC_ID: {{ID}}\nGAME_KIT: idle-automation\nSTYLE_KIT: {{STYLE_KIT}}\nMECHANICS: {{MECHANICS}}\nENEMY_SET: {{ENEMY_SET}}\n-->\n<canvas id=\"game\" aria-label=\"{{TITLE}}\"></canvas><script>\n(() => {\n\"use strict\";\nconst SPEC = {{SPEC_JSON}},S=SPEC.strings,canvas=document.getElementById(\"game\"),ctx=canvas.getContext(\"2d\"),READY=\"READY\",PLAYING=\"PLAYING\",OVER=\"OVER\",FIXED_DT=1/60;\nlet width=0,height=0,dpr=1,state=READY,last=0,acc=0,score=0,best=Number(localStorage.getItem(\"tgg-\"+SPEC.id+\"-best\")||0),timeLeft=SPEC.duration,cash=30,flash=0,notice=\"\",noticeTime=0;\nconst p=SPEC.palette,stations=[],rects=[],particles=[],floats=[];\nfunction resize(){dpr=Math.max(1,Math.min(2,devicePixelRatio||1));width=innerWidth;height=innerHeight;canvas.width=Math.floor(width*dpr);canvas.height=Math.floor(height*dpr);canvas.style.width=width+\"px\";canvas.style.height=height+\"px\";ctx.setTransform(dpr,0,0,dpr,0,0);layout()}\nfunction layout(){rects.length=0;const w=Math.min(230,width*.25),gap=24,total=stations.length*w+(stations.length-1)*gap,x0=(width-total)/2,y=height*.42;for(let i=0;i<stations.length;i++)rects.push({x:x0+i*(w+gap),y,w,h:150,station:stations[i]})}\nfunction reset(){state=PLAYING;score=0;cash=30;timeLeft=SPEC.duration;notice=\"\";stations.length=0;particles.length=floats.length=0;for(const name of (S.stations||[\"Input\",\"Process\",\"Output\"]))stations.push({name,level:1,progress:0,rate:.28+Math.random()*.08,bottleneck:false});layout()}\nfunction pointerdown(e){e.preventDefault();if(state===READY||state===OVER){reset();return}for(const r of rects)if(inside(e.clientX,e.clientY,r)){upgrade(r.station,r.x+r.w/2,r.y+60);return}}\nfunction pointermove(e){e.preventDefault()}function pointerup(e){e.preventDefault()}\ncanvas.addEventListener(\"pointerdown\",pointerdown);canvas.addEventListener(\"pointermove\",pointermove);canvas.addEventListener(\"pointerup\",pointerup);canvas.addEventListener(\"pointercancel\",pointerup);addEventListener(\"resize\",resize);\nfunction upgrade(s,x,y){const cost=s.bottleneck?18:20+s.level*12;if(cash<cost){flash=.22;notice=S.needCash;noticeTime=.8;return}cash-=cost;if(s.bottleneck){s.bottleneck=false;notice=S.bottleneckCleared;burst(x,y,p.good,16)}else{s.level++;s.rate+=.08;notice=s.name+\" \"+S.upgraded;burst(x,y,p.accent,16)}noticeTime=.9}\nfunction update(dt){if(state!==PLAYING){updateFx(dt);return}timeLeft-=dt;if(timeLeft<=0)over();for(const s of stations){const slow=s.bottleneck?.28:1;s.progress+=dt*s.rate*s.level*slow*SPEC.tuning.speed;if(s.progress>=1){s.progress-=1;const gain=Math.round((6+s.level*4)*SPEC.tuning.scoreMultiplier);cash+=gain;score+=gain;addFloat(\"+\"+gain,rectX(s),height*.35,p.good);if(Math.random()<.08+s.level*.01)s.bottleneck=true}}flash=Math.max(0,flash-dt*3);noticeTime=Math.max(0,noticeTime-dt);updateFx(dt)}\nfunction rectX(s){const r=rects.find(r=>r.station===s);return r?r.x+r.w/2:width/2}\nfunction over(){state=OVER;best=Math.max(best,score);localStorage.setItem(\"tgg-\"+SPEC.id+\"-best\",String(best))}\nfunction updateFx(dt){for(const q of particles){q.x+=q.vx*dt;q.y+=q.vy*dt;q.life-=dt}for(const f of floats){f.y-=34*dt;f.life-=dt}trim(particles,q=>q.life<=0);trim(floats,f=>f.life<=0)}\nfunction render(){ctx.save();bg();line();stationsDraw();fx();hud();if(flash){ctx.fillStyle=alpha(p.bad,flash*.22);ctx.fillRect(0,0,width,height)}if(state!==PLAYING)overlay();ctx.restore()}\nfunction bg(){const g=ctx.createLinearGradient(0,0,0,height);g.addColorStop(0,p.backgroundA);g.addColorStop(1,p.backgroundB);ctx.fillStyle=g;ctx.fillRect(0,0,width,height);const v=ctx.createRadialGradient(width/2,height*.45,height*.2,width/2,height*.5,height*.95);v.addColorStop(0,\"rgba(0,0,0,0)\");v.addColorStop(1,\"rgba(0,0,0,.4)\");ctx.fillStyle=v;ctx.fillRect(0,0,width,height)}\nfunction line(){ctx.strokeStyle=alpha(p.ink,.18);ctx.lineWidth=5;ctx.beginPath();ctx.moveTo(width*.12,height*.56);ctx.lineTo(width*.88,height*.56);ctx.stroke();if(noticeTime>0)text(notice,width/2,height*.28,20,p.accent,\"center\")}\nfunction stationsDraw(){for(const r of rects){const s=r.station;ctx.fillStyle=\"rgba(0,0,0,.22)\";ctx.beginPath();ctx.roundRect(r.x+2,r.y+4,r.w,r.h,14);ctx.fill();const cg=ctx.createLinearGradient(0,r.y,0,r.y+r.h);cg.addColorStop(0,alpha(s.bottleneck?p.bad:p.ink,.16));cg.addColorStop(1,alpha(s.bottleneck?p.bad:p.ink,.06));ctx.fillStyle=cg;ctx.beginPath();ctx.roundRect(r.x,r.y,r.w,r.h,14);ctx.fill();ctx.strokeStyle=s.bottleneck?p.bad:p.accent;ctx.lineWidth=2.5;ctx.stroke();text(s.name,r.x+r.w/2,r.y+30,20,p.ink,\"center\");text(S.level+\" \"+s.level,r.x+r.w/2,r.y+58,14,p.accent,\"center\");const bx=r.x+22,by=r.y+92,bw=r.w-44;ctx.fillStyle=alpha(p.backgroundA,.55);ctx.beginPath();ctx.roundRect(bx,by,bw,12,6);ctx.fill();const pc=s.bottleneck?p.bad:p.good;ctx.save();ctx.shadowColor=pc;ctx.shadowBlur=10;ctx.fillStyle=pc;ctx.beginPath();ctx.roundRect(bx,by,bw*Math.max(0,Math.min(1,s.progress)),12,6);ctx.fill();ctx.restore();text(s.bottleneck?S.bottleneck:S.upgrade,r.x+r.w/2,r.y+126,13,p.ink,\"center\")}}\nfunction hud(){if(state===READY)return;text(SPEC.title,20,30,22,p.ink,\"left\");text(S.score+\" \"+score+\" \"+S.best+\" \"+best,20,58,13,p.ink,\"left\");text(S.cash+\" \"+cash,width-20,32,17,p.ink,\"right\");text(S.time.replace(\"{n}\",Math.ceil(timeLeft)),width-20,60,22,timeLeft<10?p.bad:p.ink,\"right\")}\nfunction fx(){for(const q of particles){ctx.globalAlpha=q.life*2;ctx.fillStyle=q.color;ctx.fillRect(q.x,q.y,q.size,q.size)}ctx.globalAlpha=1;for(const f of floats){ctx.globalAlpha=f.life;text(f.text,f.x,f.y,16,f.color,\"center\")}ctx.globalAlpha=1}\nfunction overlay(){ctx.fillStyle=\"rgba(0,0,0,.66)\";ctx.fillRect(0,0,width,height);text(SPEC.title,width/2,height*.43,36,p.ink,\"center\");text(SPEC.roles.objective,width/2,height*.43+42,15,p.ink,\"center\");text(state===OVER?S.restart:S.start,width/2,height*.43+94,25,p.accent,\"center\");if(state===OVER)text(S.score+\" \"+score+\" \"+S.best+\" \"+best,width/2,height*.43+130,15,p.ink,\"center\")}\nfunction inside(x,y,r){return x>=r.x&&x<=r.x+r.w&&y>=r.y&&y<=r.y+r.h}function burst(x,y,c,n){for(let i=0;i<n;i++){const a=Math.random()*Math.PI*2,s=35+Math.random()*100;particles.push({x,y,vx:Math.cos(a)*s,vy:Math.sin(a)*s,life:.5,size:3+Math.random()*5,color:c})}}function addFloat(t,x,y,c){floats.push({text:t,x,y,life:.9,color:c})}function trim(a,pred){for(let i=a.length-1;i>=0;i--)if(pred(a[i]))a.splice(i,1)}function alpha(hex,a){const n=parseInt(hex.slice(1),16);return`rgba(${n>>16&255},${n>>8&255},${n&255},${a})`}function text(t,x,y,s,c,a){ctx.fillStyle=c;ctx.font=`900 ${s}px system-ui`;ctx.textAlign=a;ctx.textBaseline=\"middle\";ctx.fillText(t,x,y)}\nfunction tick(now){if(!last)last=now;const frame=Math.min(.05,(now-last)/1000);last=now;acc+=frame;while(acc>=FIXED_DT){update(FIXED_DT);acc-=FIXED_DT}render();requestAnimationFrame(tick)}resize();requestAnimationFrame(tick);\n})();\n</script></body></html>\n","match-puzzle.html":"<!doctype html>\n<html lang=\"{{LANG}}\"><head><meta charset=\"utf-8\"><meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\"><title>{{TITLE}}</title><link rel=\"icon\" href=\"data:,\"><style>\nhtml,body{margin:0;width:100%;height:100%;overflow:hidden;background:{{BACKGROUND_A}};color:{{INK}};font-family:system-ui,-apple-system,BlinkMacSystemFont,\"Segoe UI\",sans-serif;touch-action:none;user-select:none}canvas{display:block;width:100vw;height:100vh;touch-action:none}\n</style></head><body>\n<!--\nGAME_SPEC_ID: {{ID}}\nGAME_KIT: match-puzzle\nSTYLE_KIT: {{STYLE_KIT}}\nMECHANICS: {{MECHANICS}}\nENEMY_SET: {{ENEMY_SET}}\n-->\n<canvas id=\"game\" aria-label=\"{{TITLE}}\"></canvas><script>\n(() => {\n\"use strict\";\nconst SPEC = {{SPEC_JSON}},S=SPEC.strings,canvas=document.getElementById(\"game\"),ctx=canvas.getContext(\"2d\"),READY=\"READY\",PLAYING=\"PLAYING\",OVER=\"OVER\",FIXED_DT=1/60;\nlet width=0,height=0,dpr=1,state=READY,last=0,acc=0,score=0,best=Number(localStorage.getItem(\"tgg-\"+SPEC.id+\"-best\")||0),timeLeft=SPEC.duration,combo=0,shake=0,flash=0;\nconst p=SPEC.palette,N=7,board=[],particles=[],floats=[],colors=[];\nlet ox=0,oy=0,tile=54;\nfunction initColors(){colors.length=0;colors.push(p.good,p.accent,p.bad,p.ink,\"#60a5fa\")}\nfunction resize(){dpr=Math.max(1,Math.min(2,devicePixelRatio||1));width=innerWidth;height=innerHeight;canvas.width=Math.floor(width*dpr);canvas.height=Math.floor(height*dpr);canvas.style.width=width+\"px\";canvas.style.height=height+\"px\";ctx.setTransform(dpr,0,0,dpr,0,0);tile=Math.min(62,Math.floor(Math.min(width*.72,height*.72)/N));ox=(width-tile*N)/2;oy=(height-tile*N)/2+24}\nfunction reset(){state=PLAYING;score=0;timeLeft=SPEC.duration;combo=0;particles.length=floats.length=0;initColors();board.length=0;for(let y=0;y<N;y++){board[y]=[];for(let x=0;x<N;x++)board[y][x]=randTile()}}\nfunction randTile(){return Math.floor(Math.random()*colors.length)}\nfunction pointerdown(e){e.preventDefault();if(state===READY||state===OVER){reset();return}const x=Math.floor((e.clientX-ox)/tile),y=Math.floor((e.clientY-oy)/tile);if(x<0||x>=N||y<0||y>=N)return;const group=findGroup(x,y);if(group.length<2){combo=0;shake=.12;flash=.18;return}clearGroup(group)}\nfunction pointermove(e){e.preventDefault()}function pointerup(e){e.preventDefault()}\ncanvas.addEventListener(\"pointerdown\",pointerdown);canvas.addEventListener(\"pointermove\",pointermove);canvas.addEventListener(\"pointerup\",pointerup);canvas.addEventListener(\"pointercancel\",pointerup);addEventListener(\"resize\",resize);\nfunction findGroup(sx,sy){const type=board[sy][sx],seen=new Set(),out=[],stack=[[sx,sy]];while(stack.length){const [x,y]=stack.pop(),k=x+\",\"+y;if(seen.has(k)||x<0||x>=N||y<0||y>=N||board[y][x]!==type)continue;seen.add(k);out.push([x,y]);stack.push([x+1,y],[x-1,y],[x,y+1],[x,y-1])}return out}\nfunction clearGroup(group){combo++;const points=Math.round(group.length*group.length*(1+combo*.25)*SPEC.tuning.scoreMultiplier);score+=points;addFloat(\"+\"+points,ox+group[0][0]*tile+tile/2,oy+group[0][1]*tile,p.good);for(const [x,y] of group){burst(ox+x*tile+tile/2,oy+y*tile+tile/2,colors[board[y][x]],8);board[y][x]=null}collapse()}\nfunction collapse(){for(let x=0;x<N;x++){let col=[];for(let y=N-1;y>=0;y--)if(board[y][x]!==null)col.push(board[y][x]);while(col.length<N)col.push(randTile());for(let y=N-1;y>=0;y--)board[y][x]=col[N-1-y]}}\nfunction update(dt){if(state!==PLAYING){updateFx(dt);return}timeLeft-=dt;if(timeLeft<=0)over();shake=Math.max(0,shake-dt*3);flash=Math.max(0,flash-dt*3);updateFx(dt)}\nfunction over(){state=OVER;best=Math.max(best,score);localStorage.setItem(\"tgg-\"+SPEC.id+\"-best\",String(best))}\nfunction updateFx(dt){for(const q of particles){q.x+=q.vx*dt;q.y+=q.vy*dt;q.life-=dt}for(const f of floats){f.y-=40*dt;f.life-=dt}trim(particles,q=>q.life<=0);trim(floats,f=>f.life<=0)}\nfunction render(){ctx.save();if(shake)ctx.translate((Math.random()-.5)*shake*18,(Math.random()-.5)*shake*18);bg();drawBoard();fx();hud();if(flash){ctx.fillStyle=alpha(p.bad,flash*.22);ctx.fillRect(0,0,width,height)}if(state!==PLAYING)overlay();ctx.restore()}\nfunction bg(){const g=ctx.createLinearGradient(0,0,0,height);g.addColorStop(0,p.backgroundA);g.addColorStop(1,p.backgroundB);ctx.fillStyle=g;ctx.fillRect(0,0,width,height);const v=ctx.createRadialGradient(width/2,height*.45,height*.2,width/2,height*.5,height*.95);v.addColorStop(0,\"rgba(0,0,0,0)\");v.addColorStop(1,\"rgba(0,0,0,.4)\");ctx.fillStyle=v;ctx.fillRect(0,0,width,height)}\nfunction drawBoard(){if(board.length<N||colors.length===0)return;ctx.fillStyle=alpha(p.ink,.08);ctx.beginPath();ctx.roundRect(ox-12,oy-12,tile*N+24,tile*N+24,18);ctx.fill();for(let y=0;y<N;y++)for(let x=0;x<N;x++){const val=board[y][x];if(val==null)continue;const c=colors[val],cx=ox+x*tile+tile/2,cy=oy+y*tile+tile/2,s=tile-10;ctx.fillStyle=\"rgba(0,0,0,.22)\";ctx.beginPath();ctx.roundRect(cx-s/2+2,cy-s/2+3,s,s,12);ctx.fill();const g=ctx.createRadialGradient(cx-s*.25,cy-s*.3,s*.1,cx,cy,s*.85);g.addColorStop(0,lighten(c,.55));g.addColorStop(1,c);ctx.fillStyle=g;ctx.beginPath();ctx.roundRect(cx-s/2,cy-s/2,s,s,12);ctx.fill();ctx.strokeStyle=alpha(p.ink,.22);ctx.lineWidth=1.5;ctx.stroke();ctx.fillStyle=\"rgba(255,255,255,.42)\";ctx.beginPath();ctx.ellipse(cx-s*.16,cy-s*.2,s*.2,s*.12,-.5,0,Math.PI*2);ctx.fill()}}\nfunction hud(){if(state===READY)return;text(SPEC.title,20,30,22,p.ink,\"left\");text(S.score+\" \"+score+\" \"+S.best+\" \"+best,20,58,13,p.ink,\"left\");text(S.combo+\" \"+combo,width-20,32,15,p.ink,\"right\");text(S.time.replace(\"{n}\",Math.ceil(timeLeft)),width-20,60,22,timeLeft<10?p.bad:p.ink,\"right\")}\nfunction fx(){for(const q of particles){ctx.globalAlpha=q.life*2;ctx.fillStyle=q.color;ctx.fillRect(q.x,q.y,q.size,q.size)}ctx.globalAlpha=1;for(const f of floats){ctx.globalAlpha=f.life;text(f.text,f.x,f.y,16,f.color,\"center\")}ctx.globalAlpha=1}\nfunction overlay(){ctx.fillStyle=\"rgba(0,0,0,.66)\";ctx.fillRect(0,0,width,height);text(SPEC.title,width/2,height*.43,36,p.ink,\"center\");text(SPEC.roles.objective,width/2,height*.43+42,15,p.ink,\"center\");text(state===OVER?S.restart:S.start,width/2,height*.43+94,25,p.accent,\"center\");if(state===OVER)text(S.score+\" \"+score+\" \"+S.best+\" \"+best,width/2,height*.43+130,15,p.ink,\"center\")}\nfunction burst(x,y,c,n){for(let i=0;i<n;i++){const a=Math.random()*Math.PI*2,s=40+Math.random()*120;particles.push({x,y,vx:Math.cos(a)*s,vy:Math.sin(a)*s,life:.5,size:3+Math.random()*5,color:c})}}function addFloat(t,x,y,c){floats.push({text:t,x,y,life:.9,color:c})}function trim(a,pred){for(let i=a.length-1;i>=0;i--)if(pred(a[i]))a.splice(i,1)}function alpha(hex,a){const n=parseInt(hex.slice(1),16);return`rgba(${n>>16&255},${n>>8&255},${n&255},${a})`}function lighten(hex,t){const n=parseInt(hex.slice(1),16),r=n>>16&255,g=n>>8&255,b=n&255,m=v=>Math.round(v+(255-v)*t);return`rgb(${m(r)},${m(g)},${m(b)})`}function text(t,x,y,s,c,a){ctx.fillStyle=c;ctx.font=`900 ${s}px system-ui`;ctx.textAlign=a;ctx.textBaseline=\"middle\";ctx.fillText(t,x,y)}\nfunction tick(now){if(!last)last=now;const frame=Math.min(.05,(now-last)/1000);last=now;acc+=frame;while(acc>=FIXED_DT){update(FIXED_DT);acc-=FIXED_DT}render();requestAnimationFrame(tick)}\nresize();requestAnimationFrame(tick);\n})();\n</script></body></html>\n","merge-drop.html":"<!doctype html>\n<html lang=\"{{LANG}}\"><head><meta charset=\"utf-8\"><meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\"><title>{{TITLE}}</title><link rel=\"icon\" href=\"data:,\"><style>\nhtml,body{margin:0;width:100%;height:100%;overflow:hidden;background:{{BACKGROUND_A}};color:{{INK}};font-family:system-ui,-apple-system,BlinkMacSystemFont,\"Segoe UI\",sans-serif;touch-action:none;user-select:none}canvas{display:block;width:100vw;height:100vh;touch-action:none}\n</style></head><body>\n<!--\nGAME_SPEC_ID: {{ID}}\nGAME_KIT: merge-drop\nSTYLE_KIT: {{STYLE_KIT}}\nMECHANICS: {{MECHANICS}}\nENEMY_SET: {{ENEMY_SET}}\n-->\n<canvas id=\"game\" aria-label=\"{{TITLE}}\"></canvas><script>\n(() => {\n\"use strict\";\nconst SPEC = {{SPEC_JSON}},S=SPEC.strings,canvas=document.getElementById(\"game\"),ctx=canvas.getContext(\"2d\"),READY=\"READY\",PLAYING=\"PLAYING\",OVER=\"OVER\",FIXED_DT=1/60;\nconst COLS=5,ROWS=8,p=SPEC.palette,particles=[],floats=[];\nlet width=0,height=0,dpr=1,state=READY,last=0,acc=0,score=0,best=Number(localStorage.getItem(\"tgg-\"+SPEC.id+\"-best\")||0),timeLeft=SPEC.duration,combo=0,shake=0,flash=0,hoverCol=-1;\nlet grid=[],cur=2,nxt=2,faller=null,cell=60,ox=0,oy=0;\nfunction resize(){dpr=Math.max(1,Math.min(2,devicePixelRatio||1));width=innerWidth;height=innerHeight;canvas.width=Math.floor(width*dpr);canvas.height=Math.floor(height*dpr);canvas.style.width=width+\"px\";canvas.style.height=height+\"px\";ctx.setTransform(dpr,0,0,dpr,0,0);cell=Math.min(Math.floor(width*.82/COLS),Math.floor(height*.62/ROWS));ox=(width-cell*COLS)/2;oy=height-cell*ROWS-24}\nfunction reset(){state=PLAYING;score=0;timeLeft=SPEC.duration;combo=0;faller=null;particles.length=floats.length=0;grid=[];for(let r=0;r<ROWS;r++){grid[r]=[];for(let c=0;c<COLS;c++)grid[r][c]=0}cur=randVal();nxt=randVal()}\nfunction randVal(){return Math.random()<.78?2:4}\nfunction colFromX(x){return Math.max(0,Math.min(COLS-1,Math.floor((x-ox)/cell)))}\nfunction lowestEmpty(c){for(let r=ROWS-1;r>=0;r--)if(grid[r][c]===0)return r;return -1}\nfunction pointerdown(e){e.preventDefault();if(state===READY||state===OVER){reset();return}if(faller)return;const c=colFromX(e.clientX),r=lowestEmpty(c);if(r<0){flash=.18;shake=.1;if(grid[0].every((_,cc)=>lowestEmpty(cc)<0))over();return}faller={col:c,row:r,val:cur,y:oy-cell};cur=nxt;nxt=randVal()}\nfunction pointermove(e){e.preventDefault();hoverCol=colFromX(e.clientX)}\nfunction pointerup(e){e.preventDefault()}\ncanvas.addEventListener(\"pointerdown\",pointerdown);canvas.addEventListener(\"pointermove\",pointermove);canvas.addEventListener(\"pointerup\",pointerup);canvas.addEventListener(\"pointercancel\",pointerup);addEventListener(\"resize\",resize);\nfunction land(){let r=faller.row,c=faller.col,v=faller.val;grid[r][c]=v;faller=null;let merged=false;while(r+1<ROWS&&grid[r+1][c]===grid[r][c]){v*=2;grid[r+1][c]=v;grid[r][c]=0;r++;score+=Math.round(v*(1+combo*.2)*SPEC.tuning.scoreMultiplier);combo++;merged=true;burst(ox+c*cell+cell/2,oy+r*cell+cell/2,tileColor(v),16);addFloat(\"+\"+v,ox+c*cell+cell/2,oy+r*cell+cell/2-10,p.good)}if(!merged)combo=0;if(grid[0].every((x)=>x!==0)&&grid[0].every((_,cc)=>lowestEmpty(cc)<0))over()}\nfunction update(dt){if(state!==PLAYING){updateFx(dt);return}timeLeft-=dt;if(timeLeft<=0)over();if(faller){faller.y+=1500*dt;const ty=oy+faller.row*cell;if(faller.y>=ty){faller.y=ty;land()}}shake=Math.max(0,shake-dt*3);flash=Math.max(0,flash-dt*3);updateFx(dt)}\nfunction over(){state=OVER;best=Math.max(best,score);localStorage.setItem(\"tgg-\"+SPEC.id+\"-best\",String(best))}\nfunction updateFx(dt){for(const q of particles){q.x+=q.vx*dt;q.y+=q.vy*dt;q.vy+=120*dt;q.life-=dt}for(const f of floats){f.y-=42*dt;f.life-=dt}trim(particles,q=>q.life<=0);trim(floats,f=>f.life<=0)}\nfunction render(){ctx.save();if(shake)ctx.translate((Math.random()-.5)*shake*16,(Math.random()-.5)*shake*16);bg();well();tiles();drawFaller();fx();hud();if(flash){ctx.fillStyle=alpha(p.bad,flash*.22);ctx.fillRect(0,0,width,height)}if(state!==PLAYING)overlay();ctx.restore()}\nfunction bg(){const g=ctx.createLinearGradient(0,0,0,height);g.addColorStop(0,p.backgroundA);g.addColorStop(1,p.backgroundB);ctx.fillStyle=g;ctx.fillRect(0,0,width,height);const v=ctx.createRadialGradient(width/2,height*.45,height*.2,width/2,height*.5,height*.95);v.addColorStop(0,\"rgba(0,0,0,0)\");v.addColorStop(1,\"rgba(0,0,0,.42)\");ctx.fillStyle=v;ctx.fillRect(0,0,width,height)}\nfunction well(){ctx.fillStyle=alpha(p.ink,.07);ctx.beginPath();ctx.roundRect(ox-10,oy-10,cell*COLS+20,cell*ROWS+20,16);ctx.fill();if(state===PLAYING&&hoverCol>=0&&!faller){ctx.fillStyle=alpha(p.accent,.1);ctx.fillRect(ox+hoverCol*cell,oy,cell,cell*ROWS)}for(let c=0;c<=COLS;c++){ctx.strokeStyle=alpha(p.ink,.08);ctx.beginPath();ctx.moveTo(ox+c*cell,oy);ctx.lineTo(ox+c*cell,oy+cell*ROWS);ctx.stroke()}}\nfunction drawTile(cx,cy,v){const s=cell-8,c=tileColor(v);ctx.fillStyle=\"rgba(0,0,0,.25)\";ctx.beginPath();ctx.roundRect(cx-s/2+2,cy-s/2+3,s,s,12);ctx.fill();const g=ctx.createRadialGradient(cx-s*.25,cy-s*.3,s*.1,cx,cy,s*.85);g.addColorStop(0,lighten(c,.5));g.addColorStop(1,c);ctx.fillStyle=g;ctx.beginPath();ctx.roundRect(cx-s/2,cy-s/2,s,s,12);ctx.fill();ctx.strokeStyle=alpha(p.ink,.25);ctx.lineWidth=2;ctx.stroke();text(String(v),cx,cy,v<100?Math.floor(s*.42):v<1000?Math.floor(s*.32):Math.floor(s*.24),p.backgroundA,\"center\")}\nfunction tiles(){if(grid.length<ROWS)return;for(let r=0;r<ROWS;r++)for(let c=0;c<COLS;c++)if(grid[r][c])drawTile(ox+c*cell+cell/2,oy+r*cell+cell/2,grid[r][c])}\nfunction drawFaller(){if(faller)drawTile(ox+faller.col*cell+cell/2,faller.y+cell/2,faller.val)}\nfunction fx(){for(const q of particles){ctx.globalAlpha=Math.max(0,q.life*2);ctx.fillStyle=q.color;ctx.fillRect(q.x,q.y,q.size,q.size)}ctx.globalAlpha=1;for(const f of floats){ctx.globalAlpha=Math.max(0,f.life);text(f.text,f.x,f.y,16,f.color,\"center\")}ctx.globalAlpha=1}\nfunction hud(){if(state===READY)return;text(SPEC.title,20,30,21,p.ink,\"left\");text(S.score+\" \"+score+\"  \"+S.best+\" \"+best,20,58,13,p.ink,\"left\");text(S.time.replace(\"{n}\",Math.ceil(timeLeft)),width-20,32,22,timeLeft<10?p.bad:p.ink,\"right\");if(state===PLAYING)text(\"▼ \"+cur+\"   \"+nxt,width-20,60,14,p.accent,\"right\")}\nfunction overlay(){ctx.fillStyle=\"rgba(0,0,0,.66)\";ctx.fillRect(0,0,width,height);text(SPEC.title,width/2,height*.4,34,p.ink,\"center\");text(SPEC.roles.objective,width/2,height*.4+40,15,p.ink,\"center\");text(state===OVER?S.restart:S.start,width/2,height*.4+92,24,p.accent,\"center\");if(state===OVER)text(S.score+\" \"+score+\"  \"+S.best+\" \"+best,width/2,height*.4+128,15,p.ink,\"center\")}\nfunction tileColor(v){const i=Math.max(0,Math.round(Math.log2(v))-1);return [p.good,p.accent,p.bad,\"#60a5fa\",\"#f59e0b\",\"#a78bfa\",\"#34d399\",p.ink][i%8]}\nfunction burst(x,y,c,n){for(let i=0;i<n;i++){const a=Math.random()*Math.PI*2,s=40+Math.random()*120;particles.push({x,y,vx:Math.cos(a)*s,vy:Math.sin(a)*s,life:.5,size:3+Math.random()*5,color:c})}}\nfunction addFloat(t,x,y,c){floats.push({text:t,x,y,life:.9,color:c})}\nfunction trim(a,pred){for(let i=a.length-1;i>=0;i--)if(pred(a[i]))a.splice(i,1)}\nfunction alpha(hex,a){const n=parseInt(hex.slice(1),16);return`rgba(${n>>16&255},${n>>8&255},${n&255},${a})`}\nfunction lighten(hex,t){const n=parseInt(hex.slice(1),16),r=n>>16&255,g=n>>8&255,b=n&255,m=v=>Math.round(v+(255-v)*t);return`rgb(${m(r)},${m(g)},${m(b)})`}\nfunction text(t,x,y,s,c,a){ctx.fillStyle=c;ctx.font=`900 ${s}px system-ui`;ctx.textAlign=a;ctx.textBaseline=\"middle\";ctx.fillText(t,x,y)}\nfunction tick(now){if(!last)last=now;const frame=Math.min(.05,(now-last)/1000);last=now;acc+=frame;while(acc>=FIXED_DT){update(FIXED_DT);acc-=FIXED_DT}render();requestAnimationFrame(tick)}\nresize();requestAnimationFrame(tick);\n})();\n</script></body></html>\n","physics-arc.html":"<!doctype html>\n<html lang=\"{{LANG}}\"><head><meta charset=\"utf-8\"><meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\"><title>{{TITLE}}</title><link rel=\"icon\" href=\"data:,\"><style>\nhtml,body{margin:0;width:100%;height:100%;overflow:hidden;background:{{BACKGROUND_A}};color:{{INK}};font-family:system-ui,-apple-system,BlinkMacSystemFont,\"Segoe UI\",sans-serif;touch-action:none;user-select:none}canvas{display:block;width:100vw;height:100vh;touch-action:none}\n</style></head><body>\n<!--\nGAME_SPEC_ID: {{ID}}\nGAME_KIT: physics-arc\nSTYLE_KIT: {{STYLE_KIT}}\nMECHANICS: {{MECHANICS}}\nENEMY_SET: {{ENEMY_SET}}\n-->\n<canvas id=\"game\" aria-label=\"{{TITLE}}\"></canvas><script>\n(() => {\n\"use strict\";\nconst SPEC = {{SPEC_JSON}},S=SPEC.strings,canvas=document.getElementById(\"game\"),ctx=canvas.getContext(\"2d\"),READY=\"READY\",PLAYING=\"PLAYING\",OVER=\"OVER\",FIXED_DT=1/60;\nlet width=0,height=0,dpr=1,state=READY,last=0,acc=0,score=0,best=Number(localStorage.getItem(\"tgg-\"+SPEC.id+\"-best\")||0),timeLeft=SPEC.duration,shots=8,level=1,drag=null,shake=0,flash=0;\nconst p=SPEC.palette,projectiles=[],targets=[],blocks=[],particles=[],floats=[],launcher={x:0,y:0};\nfunction resize(){dpr=Math.max(1,Math.min(2,devicePixelRatio||1));width=innerWidth;height=innerHeight;canvas.width=Math.floor(width*dpr);canvas.height=Math.floor(height*dpr);canvas.style.width=width+\"px\";canvas.style.height=height+\"px\";ctx.setTransform(dpr,0,0,dpr,0,0);launcher.x=width*.18;launcher.y=height*.76}\nfunction reset(){state=PLAYING;score=0;timeLeft=SPEC.duration;shots=8;level=1;projectiles.length=targets.length=blocks.length=particles.length=floats.length=0;spawnStage()}\nfunction spawnStage(){targets.length=0;blocks.length=0;for(let i=0;i<3;i++)targets.push({x:width*(.62+.1*Math.random()),y:height*(.28+i*.18),r:22,hp:1+Math.floor(level/3),hit:false});for(let i=0;i<2;i++)blocks.push({x:width*(.42+.16*i),y:height*(.38+.18*Math.random()),w:34,h:110})}\nfunction pointerdown(e){e.preventDefault();if(state===READY||state===OVER){reset();return}if(shots<=0)return;drag={x:e.clientX,y:e.clientY}}\nfunction pointermove(e){e.preventDefault();if(drag){drag.x=e.clientX;drag.y=e.clientY}}function pointerup(e){e.preventDefault();if(!drag||state!==PLAYING){drag=null;return}const dx=launcher.x-drag.x,dy=launcher.y-drag.y,power=Math.min(680,Math.hypot(dx,dy)*4);projectiles.push({x:launcher.x,y:launcher.y,vx:dx/Math.max(1,Math.hypot(dx,dy))*power,vy:dy/Math.max(1,Math.hypot(dx,dy))*power,r:11,life:4});shots--;drag=null}\ncanvas.addEventListener(\"pointerdown\",pointerdown);canvas.addEventListener(\"pointermove\",pointermove);canvas.addEventListener(\"pointerup\",pointerup);canvas.addEventListener(\"pointercancel\",pointerup);addEventListener(\"resize\",resize);\nfunction update(dt){if(state!==PLAYING){updateFx(dt);return}timeLeft-=dt;if(timeLeft<=0||shots<=0&&projectiles.length===0)over();for(const pr of projectiles){pr.vy+=540*dt;pr.x+=pr.vx*dt;pr.y+=pr.vy*dt;pr.life-=dt;for(const b of blocks)if(pr.x>b.x&&pr.x<b.x+b.w&&pr.y>b.y&&pr.y<b.y+b.h){pr.vx*=-.65;pr.x+=Math.sign(pr.vx)*8;burst(pr.x,pr.y,p.ink,6)}for(const t of targets)if(!t.hit&&Math.hypot(pr.x-t.x,pr.y-t.y)<pr.r+t.r){t.hp--;pr.life=0;burst(t.x,t.y,p.good,22);if(t.hp<=0){t.hit=true;const gain=40+level*5;score+=gain;addFloat(\"+\"+gain,t.x,t.y,p.good)}}if(pr.y>height+80||pr.x>width+80||pr.life<=0)pr.done=true}trim(projectiles,q=>q.done);if(targets.length&&targets.every(t=>t.hit)){level++;shots+=3;score+=80;addFloat(S.stage,width/2,height*.24,p.accent);spawnStage()}shake=Math.max(0,shake-dt*3);flash=Math.max(0,flash-dt*3);updateFx(dt)}\nfunction over(){state=OVER;best=Math.max(best,score);localStorage.setItem(\"tgg-\"+SPEC.id+\"-best\",String(best))}\nfunction updateFx(dt){for(const q of particles){q.x+=q.vx*dt;q.y+=q.vy*dt;q.life-=dt}for(const f of floats){f.y-=42*dt;f.life-=dt}trim(particles,q=>q.life<=0);trim(floats,f=>f.life<=0)}\nfunction render(){ctx.save();bg();scene();fx();hud();if(state!==PLAYING)overlay();ctx.restore()}\nfunction bg(){const g=ctx.createLinearGradient(0,0,0,height);g.addColorStop(0,p.backgroundA);g.addColorStop(1,p.backgroundB);ctx.fillStyle=g;ctx.fillRect(0,0,width,height);const v=ctx.createRadialGradient(width/2,height*.45,height*.2,width/2,height*.5,height*.95);v.addColorStop(0,\"rgba(0,0,0,0)\");v.addColorStop(1,\"rgba(0,0,0,.4)\");ctx.fillStyle=v;ctx.fillRect(0,0,width,height)}\nfunction scene(){const gg=ctx.createLinearGradient(0,height*.82,0,height);gg.addColorStop(0,alpha(p.ink,.18));gg.addColorStop(1,alpha(p.ink,.04));ctx.fillStyle=gg;ctx.fillRect(0,height*.82,width,height*.18);for(const b of blocks){const g=ctx.createLinearGradient(b.x,0,b.x+b.w,0);g.addColorStop(0,alpha(p.bad,.6));g.addColorStop(1,alpha(p.bad,.38));ctx.fillStyle=g;ctx.beginPath();ctx.roundRect(b.x,b.y,b.w,b.h,8);ctx.fill();ctx.strokeStyle=alpha(p.ink,.3);ctx.lineWidth=2;ctx.stroke()}for(const t of targets){if(t.hit)continue;ctx.save();ctx.shadowColor=p.good;ctx.shadowBlur=14;const g=ctx.createRadialGradient(t.x-t.r*.3,t.y-t.r*.3,t.r*.1,t.x,t.y,t.r*1.2);g.addColorStop(0,lighten(p.good,.55));g.addColorStop(1,p.good);ctx.fillStyle=g;ctx.beginPath();ctx.arc(t.x,t.y,t.r,0,Math.PI*2);ctx.fill();ctx.restore();ctx.strokeStyle=alpha(p.backgroundA,.7);ctx.lineWidth=3;ctx.beginPath();ctx.arc(t.x,t.y,t.r*.6,0,Math.PI*2);ctx.stroke();ctx.strokeStyle=p.ink;ctx.lineWidth=2.5;ctx.beginPath();ctx.arc(t.x,t.y,t.r,0,Math.PI*2);ctx.stroke();text(String(t.hp),t.x,t.y,14,p.backgroundA,\"center\")}if(drag){const dx=launcher.x-drag.x,dy=launcher.y-drag.y,h=Math.max(1,Math.hypot(dx,dy)),power=Math.min(680,h*4);let sx=launcher.x,sy=launcher.y,vx=dx/h*power,vy=dy/h*power;ctx.fillStyle=alpha(p.ink,.5);for(let i=0;i<18;i++){sx+=vx*.05;sy+=vy*.05;vy+=540*.05;if(i%2===0){ctx.beginPath();ctx.arc(sx,sy,3,0,Math.PI*2);ctx.fill()}}ctx.strokeStyle=alpha(p.accent,.6);ctx.lineWidth=3;ctx.beginPath();ctx.moveTo(launcher.x,launcher.y);ctx.lineTo(drag.x,drag.y);ctx.stroke()}const aim=drag?Math.atan2(launcher.y-drag.y,launcher.x-drag.x):-Math.PI/4;ctx.save();ctx.translate(launcher.x,launcher.y);ctx.rotate(aim);ctx.fillStyle=p.ink;ctx.beginPath();ctx.roundRect(0,-9,34,18,9);ctx.fill();ctx.restore();const lg=ctx.createRadialGradient(launcher.x-8,launcher.y-8,4,launcher.x,launcher.y,26);lg.addColorStop(0,lighten(p.accent,.5));lg.addColorStop(1,p.accent);ctx.save();ctx.shadowColor=p.accent;ctx.shadowBlur=16;ctx.fillStyle=lg;ctx.beginPath();ctx.arc(launcher.x,launcher.y,24,0,Math.PI*2);ctx.fill();ctx.restore();ctx.strokeStyle=p.ink;ctx.lineWidth=3;ctx.stroke();ctx.save();ctx.shadowColor=p.accent;ctx.shadowBlur=12;ctx.fillStyle=lighten(p.accent,.3);for(const pr of projectiles){ctx.beginPath();ctx.arc(pr.x,pr.y,pr.r,0,Math.PI*2);ctx.fill()}ctx.restore()}\nfunction hud(){if(state===READY)return;text(SPEC.title,20,30,22,p.ink,\"left\");text(S.score+\" \"+score+\" \"+S.best+\" \"+best,20,58,13,p.ink,\"left\");text(S.shots+\" \"+shots+\" \"+S.stage+\" \"+level,width-20,32,15,p.ink,\"right\");text(S.time.replace(\"{n}\",Math.ceil(timeLeft)),width-20,60,22,timeLeft<10?p.bad:p.ink,\"right\")}\nfunction fx(){for(const q of particles){ctx.globalAlpha=q.life*2;ctx.fillStyle=q.color;ctx.fillRect(q.x,q.y,q.size,q.size)}ctx.globalAlpha=1;for(const f of floats){ctx.globalAlpha=f.life;text(f.text,f.x,f.y,16,f.color,\"center\")}ctx.globalAlpha=1}\nfunction overlay(){ctx.fillStyle=\"rgba(0,0,0,.66)\";ctx.fillRect(0,0,width,height);text(SPEC.title,width/2,height*.43,36,p.ink,\"center\");text(SPEC.roles.objective,width/2,height*.43+42,15,p.ink,\"center\");text(state===OVER?S.restart:S.start,width/2,height*.43+94,25,p.accent,\"center\");if(state===OVER)text(S.score+\" \"+score+\" \"+S.best+\" \"+best,width/2,height*.43+130,15,p.ink,\"center\")}\nfunction burst(x,y,c,n){for(let i=0;i<n;i++){const a=Math.random()*Math.PI*2,s=45+Math.random()*150;particles.push({x,y,vx:Math.cos(a)*s,vy:Math.sin(a)*s,life:.55,size:3+Math.random()*5,color:c})}}function addFloat(t,x,y,c){floats.push({text:t,x,y,life:.9,color:c})}function trim(a,pred){for(let i=a.length-1;i>=0;i--)if(pred(a[i]))a.splice(i,1)}function alpha(hex,a){const n=parseInt(hex.slice(1),16);return`rgba(${n>>16&255},${n>>8&255},${n&255},${a})`}function lighten(hex,t){const n=parseInt(hex.slice(1),16),r=n>>16&255,g=n>>8&255,b=n&255,m=v=>Math.round(v+(255-v)*t);return`rgb(${m(r)},${m(g)},${m(b)})`}function text(t,x,y,s,c,a){ctx.fillStyle=c;ctx.font=`900 ${s}px system-ui`;ctx.textAlign=a;ctx.textBaseline=\"middle\";ctx.fillText(t,x,y)}\nfunction tick(now){if(!last)last=now;const frame=Math.min(.05,(now-last)/1000);last=now;acc+=frame;while(acc>=FIXED_DT){update(FIXED_DT);acc-=FIXED_DT}render();requestAnimationFrame(tick)}resize();requestAnimationFrame(tick);\n})();\n</script></body></html>\n","platform-climb.html":"<!doctype html>\n<html lang=\"{{LANG}}\"><head><meta charset=\"utf-8\"><meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\"><title>{{TITLE}}</title><link rel=\"icon\" href=\"data:,\"><style>\nhtml,body{margin:0;width:100%;height:100%;overflow:hidden;background:{{BACKGROUND_A}};color:{{INK}};font-family:system-ui,-apple-system,BlinkMacSystemFont,\"Segoe UI\",sans-serif;touch-action:none;user-select:none}canvas{display:block;width:100vw;height:100vh;touch-action:none}\n</style></head><body>\n<!--\nGAME_SPEC_ID: {{ID}}\nGAME_KIT: platform-climb\nSTYLE_KIT: {{STYLE_KIT}}\nMECHANICS: {{MECHANICS}}\nENEMY_SET: {{ENEMY_SET}}\n-->\n<canvas id=\"game\" aria-label=\"{{TITLE}}\"></canvas><script>\n(() => {\n\"use strict\";\nconst SPEC = {{SPEC_JSON}},S=SPEC.strings,canvas=document.getElementById(\"game\"),ctx=canvas.getContext(\"2d\"),READY=\"READY\",PLAYING=\"PLAYING\",OVER=\"OVER\",FIXED_DT=1/60;\nlet width=0,height=0,dpr=1,state=READY,last=0,acc=0,score=0,best=Number(localStorage.getItem(\"tgg-\"+SPEC.id+\"-best\")||0),timeLeft=SPEC.duration,shake=0,flash=0;\nconst p=SPEC.palette,platforms=[],particles=[],floats=[],player={x:0,y:0,vx:0,vy:0,w:28,h:34,on:false,targetX:0};\nfunction resize(){dpr=Math.max(1,Math.min(2,devicePixelRatio||1));width=innerWidth;height=innerHeight;canvas.width=Math.floor(width*dpr);canvas.height=Math.floor(height*dpr);canvas.style.width=width+\"px\";canvas.style.height=height+\"px\";ctx.setTransform(dpr,0,0,dpr,0,0)}\nfunction reset(){state=PLAYING;score=0;timeLeft=SPEC.duration;platforms.length=particles.length=floats.length=0;player.x=width/2;player.y=height*.72;player.vx=0;player.vy=0;player.targetX=player.x;for(let i=0;i<8;i++)platforms.push({x:80+Math.random()*(width-220),y:height*.82-i*92,w:110+Math.random()*70,h:18,bad:Math.random()<.18})}\nfunction pointerdown(e){e.preventDefault();if(state===READY||state===OVER){reset();return}player.targetX=e.clientX;if(player.on){player.vy=-430;player.on=false;burst(player.x,player.y,p.accent,12)}}\nfunction pointermove(e){e.preventDefault();player.targetX=e.clientX}function pointerup(e){e.preventDefault()}\ncanvas.addEventListener(\"pointerdown\",pointerdown);canvas.addEventListener(\"pointermove\",pointermove);canvas.addEventListener(\"pointerup\",pointerup);canvas.addEventListener(\"pointercancel\",pointerup);addEventListener(\"resize\",resize);\nfunction update(dt){if(state!==PLAYING){updateFx(dt);return}timeLeft-=dt;if(timeLeft<=0)over();player.vx+=(player.targetX-player.x)*9*dt;player.vx*=.86;player.vy+=820*dt;player.x+=player.vx*dt;player.y+=player.vy*dt;player.x=Math.max(16,Math.min(width-16,player.x));player.on=false;for(const pl of platforms){pl.y+=44*dt*SPEC.tuning.speed;if(player.vy>0&&player.x>pl.x&&player.x<pl.x+pl.w&&player.y+player.h/2>pl.y&&player.y+player.h/2<pl.y+pl.h+18){player.y=pl.y-player.h/2;player.vy=-120;player.on=true;if(pl.bad){flash=.28;score=Math.max(0,score-20);addFloat(\"-20\",player.x,player.y,p.bad)}else{score+=Math.round(3*SPEC.tuning.scoreMultiplier)}}}if(platforms[0]&&platforms[0].y>height+40)platforms.shift();while(platforms.length<8){const top=Math.min(...platforms.map(pl=>pl.y));platforms.push({x:70+Math.random()*(width-200),y:top-92,w:100+Math.random()*80,h:18,bad:Math.random()<.2})}if(player.y>height+80)over();shake=Math.max(0,shake-dt*3);flash=Math.max(0,flash-dt*3);updateFx(dt)}\nfunction over(){state=OVER;best=Math.max(best,score);localStorage.setItem(\"tgg-\"+SPEC.id+\"-best\",String(best))}\nfunction updateFx(dt){for(const q of particles){q.x+=q.vx*dt;q.y+=q.vy*dt;q.life-=dt}for(const f of floats){f.y-=36*dt;f.life-=dt}trim(particles,q=>q.life<=0);trim(floats,f=>f.life<=0)}\nfunction render(){ctx.save();bg();world();fx();hud();if(flash){ctx.fillStyle=alpha(p.bad,flash*.25);ctx.fillRect(0,0,width,height)}if(state!==PLAYING)overlay();ctx.restore()}\nfunction bg(){const g=ctx.createLinearGradient(0,0,0,height);g.addColorStop(0,p.backgroundA);g.addColorStop(1,p.backgroundB);ctx.fillStyle=g;ctx.fillRect(0,0,width,height);ctx.strokeStyle=alpha(p.ink,.08);for(let y=0;y<height;y+=42){ctx.beginPath();ctx.moveTo(0,y);ctx.lineTo(width,y);ctx.stroke()}const v=ctx.createRadialGradient(width/2,height*.45,height*.2,width/2,height*.5,height*.95);v.addColorStop(0,\"rgba(0,0,0,0)\");v.addColorStop(1,\"rgba(0,0,0,.4)\");ctx.fillStyle=v;ctx.fillRect(0,0,width,height)}\nfunction world(){ctx.fillStyle=alpha(p.bad,.2);ctx.fillRect(0,height-44,width,44);for(const pl of platforms){const c=pl.bad?p.bad:p.accent;ctx.fillStyle=\"rgba(0,0,0,.25)\";ctx.beginPath();ctx.roundRect(pl.x,pl.y+3,pl.w,pl.h,8);ctx.fill();const g=ctx.createLinearGradient(0,pl.y,0,pl.y+pl.h);g.addColorStop(0,lighten(c,.4));g.addColorStop(1,c);ctx.fillStyle=g;ctx.beginPath();ctx.roundRect(pl.x,pl.y,pl.w,pl.h,8);ctx.fill();ctx.fillStyle=\"rgba(255,255,255,.25)\";ctx.beginPath();ctx.roundRect(pl.x+4,pl.y+3,pl.w-8,4,2);ctx.fill()}const px=player.x,py=player.y,w=player.w,h=player.h;ctx.fillStyle=\"rgba(0,0,0,.28)\";ctx.beginPath();ctx.ellipse(px,py+h/2,w*.6,6,0,0,Math.PI*2);ctx.fill();const g=ctx.createRadialGradient(px-w*.2,py-h*.2,4,px,py,h*.8);g.addColorStop(0,lighten(p.good,.5));g.addColorStop(1,p.good);ctx.save();ctx.shadowColor=p.good;ctx.shadowBlur=14;ctx.fillStyle=g;ctx.beginPath();ctx.roundRect(px-w/2,py-h/2,w,h,9);ctx.fill();ctx.restore();ctx.strokeStyle=p.ink;ctx.lineWidth=3;ctx.stroke();const dir=player.vx>20?1:player.vx<-20?-1:0;ctx.fillStyle=\"#fff\";ctx.beginPath();ctx.arc(px-6+dir*2,py-4,4.5,0,Math.PI*2);ctx.arc(px+6+dir*2,py-4,4.5,0,Math.PI*2);ctx.fill();ctx.fillStyle=\"#10131a\";ctx.beginPath();ctx.arc(px-6+dir*3,py-4,2.2,0,Math.PI*2);ctx.arc(px+6+dir*3,py-4,2.2,0,Math.PI*2);ctx.fill()}\nfunction hud(){if(state===READY)return;text(SPEC.title,20,30,22,p.ink,\"left\");text(S.score+\" \"+score+\" \"+S.best+\" \"+best,20,58,13,p.ink,\"left\");text(S.jumpMove,width-20,32,15,p.ink,\"right\");text(S.time.replace(\"{n}\",Math.ceil(timeLeft)),width-20,60,22,timeLeft<10?p.bad:p.ink,\"right\")}\nfunction fx(){for(const q of particles){ctx.globalAlpha=q.life*2;ctx.fillStyle=q.color;ctx.fillRect(q.x,q.y,q.size,q.size)}ctx.globalAlpha=1;for(const f of floats){ctx.globalAlpha=f.life;text(f.text,f.x,f.y,16,f.color,\"center\")}ctx.globalAlpha=1}\nfunction overlay(){ctx.fillStyle=\"rgba(0,0,0,.66)\";ctx.fillRect(0,0,width,height);text(SPEC.title,width/2,height*.43,36,p.ink,\"center\");text(SPEC.roles.objective,width/2,height*.43+42,15,p.ink,\"center\");text(state===OVER?S.restart:S.start,width/2,height*.43+94,25,p.accent,\"center\");if(state===OVER)text(S.score+\" \"+score+\" \"+S.best+\" \"+best,width/2,height*.43+130,15,p.ink,\"center\")}\nfunction burst(x,y,c,n){for(let i=0;i<n;i++){const a=Math.random()*Math.PI*2,s=35+Math.random()*110;particles.push({x,y,vx:Math.cos(a)*s,vy:Math.sin(a)*s,life:.5,size:3+Math.random()*5,color:c})}}function addFloat(t,x,y,c){floats.push({text:t,x,y,life:.9,color:c})}function trim(a,pred){for(let i=a.length-1;i>=0;i--)if(pred(a[i]))a.splice(i,1)}function alpha(hex,a){const n=parseInt(hex.slice(1),16);return`rgba(${n>>16&255},${n>>8&255},${n&255},${a})`}function lighten(hex,t){const n=parseInt(hex.slice(1),16),r=n>>16&255,g=n>>8&255,b=n&255,m=v=>Math.round(v+(255-v)*t);return`rgb(${m(r)},${m(g)},${m(b)})`}function text(t,x,y,s,c,a){ctx.fillStyle=c;ctx.font=`900 ${s}px system-ui`;ctx.textAlign=a;ctx.textBaseline=\"middle\";ctx.fillText(t,x,y)}\nfunction tick(now){if(!last)last=now;const frame=Math.min(.05,(now-last)/1000);last=now;acc+=frame;while(acc>=FIXED_DT){update(FIXED_DT);acc-=FIXED_DT}render();requestAnimationFrame(tick)}resize();requestAnimationFrame(tick);\n})();\n</script></body></html>\n","rhythm-tap.html":"<!doctype html>\n<html lang=\"{{LANG}}\"><head><meta charset=\"utf-8\"><meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\"><title>{{TITLE}}</title><link rel=\"icon\" href=\"data:,\"><style>\nhtml,body{margin:0;width:100%;height:100%;overflow:hidden;background:{{BACKGROUND_A}};color:{{INK}};font-family:system-ui,-apple-system,BlinkMacSystemFont,\"Segoe UI\",sans-serif;touch-action:none;user-select:none}canvas{display:block;width:100vw;height:100vh;touch-action:none}\n</style></head><body>\n<!--\nGAME_SPEC_ID: {{ID}}\nGAME_KIT: rhythm-tap\nSTYLE_KIT: {{STYLE_KIT}}\nMECHANICS: {{MECHANICS}}\nENEMY_SET: {{ENEMY_SET}}\n-->\n<canvas id=\"game\" aria-label=\"{{TITLE}}\"></canvas><script>\n(() => {\n\"use strict\";\nconst SPEC = {{SPEC_JSON}},S=SPEC.strings,canvas=document.getElementById(\"game\"),ctx=canvas.getContext(\"2d\"),READY=\"READY\",PLAYING=\"PLAYING\",OVER=\"OVER\",FIXED_DT=1/60;\nlet width=0,height=0,dpr=1,state=READY,last=0,acc=0,score=0,best=Number(localStorage.getItem(\"tgg-\"+SPEC.id+\"-best\")||0),timeLeft=SPEC.duration,combo=0,hp=8,spawnTimer=0,flash=0,notice=\"\",noticeTime=0;\nconst p=SPEC.palette,notes=[],particles=[],floats=[];let lanes=[],hitY=0;\nfunction resize(){dpr=Math.max(1,Math.min(2,devicePixelRatio||1));width=innerWidth;height=innerHeight;canvas.width=Math.floor(width*dpr);canvas.height=Math.floor(height*dpr);canvas.style.width=width+\"px\";canvas.style.height=height+\"px\";ctx.setTransform(dpr,0,0,dpr,0,0);lanes=[width*.25,width*.42,width*.58,width*.75];hitY=height*.76}\nfunction reset(){state=PLAYING;score=0;timeLeft=SPEC.duration;combo=0;hp=8;spawnTimer=.4;notes.length=particles.length=floats.length=0;notice=\"\"}\nfunction pointerdown(e){e.preventDefault();if(state===READY||state===OVER){reset();return}const lane=nearestLane(e.clientX);hit(lane)}\nfunction pointermove(e){e.preventDefault()}function pointerup(e){e.preventDefault()}\ncanvas.addEventListener(\"pointerdown\",pointerdown);canvas.addEventListener(\"pointermove\",pointermove);canvas.addEventListener(\"pointerup\",pointerup);canvas.addEventListener(\"pointercancel\",pointerup);addEventListener(\"resize\",resize);\nfunction nearestLane(x){let best=0,d=9999;for(let i=0;i<lanes.length;i++){const dd=Math.abs(lanes[i]-x);if(dd<d){d=dd;best=i}}return best}\nfunction hit(lane){let best=null,dist=9999;for(const n of notes)if(!n.hit&&n.lane===lane){const d=Math.abs(n.y-hitY);if(d<dist){dist=d;best=n}}if(!best||dist>70){miss(S.miss);return}best.hit=true;const perfect=dist<24,gain=perfect?40:18;combo++;score+=Math.round(gain*(1+Math.floor(combo/8))*SPEC.tuning.scoreMultiplier);notice=perfect?S.perfect:S.good;noticeTime=.7;addFloat(notice,lanes[lane],hitY-36,perfect?p.accent:p.good);burst(lanes[lane],hitY,perfect?p.accent:p.good,perfect?22:12)}\nfunction miss(label){combo=0;hp--;flash=.24;notice=label;noticeTime=.7;if(hp<=0)over()}\nfunction spawn(){notes.push({lane:Math.floor(Math.random()*lanes.length),y:-40,speed:(190+Math.random()*80)*SPEC.tuning.speed,hit:false})}\nfunction update(dt){if(state!==PLAYING){updateFx(dt);return}timeLeft-=dt;if(timeLeft<=0)over();spawnTimer-=dt;if(spawnTimer<=0){spawn();spawnTimer=Math.max(.32,.78-(SPEC.duration-timeLeft)*.006)/SPEC.tuning.spawnRate}for(const n of notes){n.y+=n.speed*dt;if(!n.hit&&n.y>hitY+86){n.hit=true;miss(S.miss)}}trim(notes,n=>n.hit&&n.y>hitY+120);flash=Math.max(0,flash-dt*3);noticeTime=Math.max(0,noticeTime-dt);updateFx(dt)}\nfunction over(){state=OVER;best=Math.max(best,score);localStorage.setItem(\"tgg-\"+SPEC.id+\"-best\",String(best))}\nfunction updateFx(dt){for(const q of particles){q.x+=q.vx*dt;q.y+=q.vy*dt;q.life-=dt}for(const f of floats){f.y-=42*dt;f.life-=dt}trim(particles,q=>q.life<=0);trim(floats,f=>f.life<=0)}\nfunction render(){ctx.save();bg();lanesDraw();notesDraw();fx();hud();if(flash){ctx.fillStyle=alpha(p.bad,flash*.25);ctx.fillRect(0,0,width,height)}if(state!==PLAYING)overlay();ctx.restore()}\nfunction bg(){const bgg=ctx.createLinearGradient(0,0,0,height);bgg.addColorStop(0,p.backgroundA);bgg.addColorStop(1,p.backgroundB);ctx.fillStyle=bgg;ctx.fillRect(0,0,width,height);ctx.strokeStyle=alpha(p.accent,.08);for(let y=0;y<height;y+=34){ctx.beginPath();ctx.moveTo(0,y);ctx.lineTo(width,y);ctx.stroke()}}\nfunction laneColor(i){return [p.good,p.accent,p.good,p.accent][i%4]}\nfunction lanesDraw(){for(let i=0;i<lanes.length;i++){const x=lanes[i];const lg=ctx.createLinearGradient(0,0,0,height);lg.addColorStop(0,alpha(laneColor(i),.02));lg.addColorStop(1,alpha(laneColor(i),.1));ctx.fillStyle=lg;ctx.fillRect(x-42,0,84,height);ctx.strokeStyle=alpha(p.ink,.12);ctx.strokeRect(x-42,0,84,height)}for(let i=0;i<lanes.length;i++){ctx.strokeStyle=alpha(laneColor(i),.5);ctx.lineWidth=2;ctx.beginPath();ctx.arc(lanes[i],hitY,26,0,Math.PI*2);ctx.stroke()}ctx.save();ctx.shadowColor=p.accent;ctx.shadowBlur=18;ctx.strokeStyle=p.accent;ctx.lineWidth=5;ctx.beginPath();ctx.moveTo(width*.14,hitY);ctx.lineTo(width*.86,hitY);ctx.stroke();ctx.restore();if(noticeTime>0)text(notice,width/2,hitY-72,28,notice===S.perfect?p.accent:notice===S.good?p.good:p.bad,\"center\")}\nfunction notesDraw(){for(const n of notes){const c=laneColor(n.lane),x=lanes[n.lane],w=60,h=32;ctx.save();ctx.shadowColor=c;ctx.shadowBlur=16;const g=ctx.createLinearGradient(0,n.y-16,0,n.y+16);g.addColorStop(0,lighten(c,.5));g.addColorStop(1,c);ctx.fillStyle=g;ctx.beginPath();ctx.roundRect(x-w/2,n.y-h/2,w,h,11);ctx.fill();ctx.restore();ctx.strokeStyle=alpha(p.ink,.3);ctx.lineWidth=2;ctx.stroke();ctx.fillStyle=\"rgba(255,255,255,.4)\";ctx.beginPath();ctx.roundRect(x-w/2+6,n.y-h/2+5,w-12,7,4);ctx.fill();text(S.tap,x,n.y,12,p.backgroundA,\"center\")}}\nfunction hud(){if(state===READY)return;text(SPEC.title,20,30,22,p.ink,\"left\");text(S.score+\" \"+score+\" \"+S.best+\" \"+best,20,58,13,p.ink,\"left\");text(S.hp+\" \"+hp+\" \"+S.combo+\" \"+combo,width-20,32,15,p.ink,\"right\");text(S.time.replace(\"{n}\",Math.ceil(timeLeft)),width-20,60,22,timeLeft<10?p.bad:p.ink,\"right\")}\nfunction fx(){for(const q of particles){ctx.globalAlpha=q.life*2;ctx.fillStyle=q.color;ctx.fillRect(q.x,q.y,q.size,q.size)}ctx.globalAlpha=1;for(const f of floats){ctx.globalAlpha=f.life;text(f.text,f.x,f.y,16,f.color,\"center\")}ctx.globalAlpha=1}\nfunction overlay(){ctx.fillStyle=\"rgba(0,0,0,.70)\";ctx.fillRect(0,0,width,height);text(SPEC.title,width/2,height*.43,36,p.ink,\"center\");text(SPEC.roles.objective,width/2,height*.43+42,15,p.ink,\"center\");text(state===OVER?S.restart:S.start,width/2,height*.43+94,25,p.accent,\"center\");if(state===OVER)text(S.score+\" \"+score+\" \"+S.best+\" \"+best,width/2,height*.43+130,15,p.ink,\"center\")}\nfunction burst(x,y,c,n){for(let i=0;i<n;i++){const a=Math.random()*Math.PI*2,s=40+Math.random()*130;particles.push({x,y,vx:Math.cos(a)*s,vy:Math.sin(a)*s,life:.5,size:3+Math.random()*5,color:c})}}function addFloat(t,x,y,c){floats.push({text:t,x,y,life:.9,color:c})}function trim(a,pred){for(let i=a.length-1;i>=0;i--)if(pred(a[i]))a.splice(i,1)}function alpha(hex,a){const n=parseInt(hex.slice(1),16);return`rgba(${n>>16&255},${n>>8&255},${n&255},${a})`}function lighten(hex,t){const n=parseInt(hex.slice(1),16),r=n>>16&255,g=n>>8&255,b=n&255,m=v=>Math.round(v+(255-v)*t);return`rgb(${m(r)},${m(g)},${m(b)})`}function text(t,x,y,s,c,a){ctx.fillStyle=c;ctx.font=`900 ${s}px system-ui`;ctx.textAlign=a;ctx.textBaseline=\"middle\";ctx.fillText(t,x,y)}\nfunction tick(now){if(!last)last=now;const frame=Math.min(.05,(now-last)/1000);last=now;acc+=frame;while(acc>=FIXED_DT){update(FIXED_DT);acc-=FIXED_DT}render();requestAnimationFrame(tick)}resize();requestAnimationFrame(tick);\n})();\n</script></body></html>\n","runner-lane.html":"<!doctype html>\n<html lang=\"{{LANG}}\">\n<head>\n<meta charset=\"utf-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>{{TITLE}}</title>\n<link rel=\"icon\" href=\"data:,\">\n<style>\nhtml,body{margin:0;width:100%;height:100%;overflow:hidden;background:{{BACKGROUND_A}};color:{{INK}};font-family:system-ui,-apple-system,BlinkMacSystemFont,\"Segoe UI\",sans-serif;touch-action:none;user-select:none}\ncanvas{display:block;width:100vw;height:100vh;touch-action:none}\n</style>\n</head>\n<body>\n<!--\nGAME_SPEC_ID: {{ID}}\nGAME_KIT: runner-lane\nSTYLE_KIT: {{STYLE_KIT}}\nMECHANICS: {{MECHANICS}}\nENEMY_SET: {{ENEMY_SET}}\n-->\n<canvas id=\"game\" aria-label=\"{{TITLE}}\"></canvas>\n<script>\n(() => {\n  \"use strict\";\n  const SPEC = {{SPEC_JSON}};\n  const S = SPEC.strings;\n  const canvas = document.getElementById(\"game\");\n  const ctx = canvas.getContext(\"2d\");\n  const STATE_READY = \"READY\", STATE_PLAYING = \"PLAYING\", STATE_OVER = \"OVER\";\n  const GAME_SECONDS = SPEC.duration, FIXED_DT = 1 / 60;\n  let width = 0, height = 0, dpr = 1, state = STATE_READY, last = 0, acc = 0;\n  let score = 0, best = Number(localStorage.getItem(\"tgg-\" + SPEC.id + \"-best\") || 0), timeLeft = GAME_SECONDS, spawnTimer = 0, speed = 250, combo = 0, shake = 0, flash = 0, roadScroll = 0;\n  const palette = SPEC.palette, lanes = [0, 0, 0], player = { lane: 1, x: 0, y: 0, r: 30, hp: 3 }, objects = [], particles = [], floats = [];\n\n  function resize() { dpr = Math.max(1, Math.min(2, devicePixelRatio || 1)); width = innerWidth; height = innerHeight; canvas.width = Math.floor(width * dpr); canvas.height = Math.floor(height * dpr); canvas.style.width = width + \"px\"; canvas.style.height = height + \"px\"; ctx.setTransform(dpr,0,0,dpr,0,0); lanes[0]=width*.25; lanes[1]=width*.5; lanes[2]=width*.75; player.x=lanes[player.lane]; player.y=height*.82; }\n  function reset(){ state=STATE_PLAYING; score=0; timeLeft=GAME_SECONDS; spawnTimer=.25; speed=250; combo=0; player.lane=1; player.hp=3; objects.length=particles.length=floats.length=0; }\n  function down(e){ e.preventDefault(); if(state===STATE_READY||state===STATE_OVER){ reset(); return; } player.lane = laneFromX(e.clientX); }\n  function move(e){ e.preventDefault(); if(state===STATE_PLAYING) player.lane = laneFromX(e.clientX); }\n  canvas.addEventListener(\"pointerdown\", down); canvas.addEventListener(\"pointermove\", move); canvas.addEventListener(\"pointerup\", e=>e.preventDefault()); canvas.addEventListener(\"pointercancel\", e=>e.preventDefault()); addEventListener(\"resize\", resize);\n  function laneFromX(x){ return Math.max(0, Math.min(2, Math.round((x / width) * 3 - .5))); }\n  function spawn(){ const lane=Math.floor(Math.random()*3), roll=Math.random(); objects.push({ lane, y:-50, r: roll < .18 ? 30 : 24, type: roll < .18 ? \"nitro\" : roll < .58 ? \"traffic\" : \"coin\", hit:false }); }\n  function update(dt){ if(state!==STATE_PLAYING){ updateFx(dt); return; } timeLeft-=dt; if(timeLeft<=0||player.hp<=0) return over(); speed += dt*8; roadScroll = (roadScroll + speed*dt) % 64; player.x += (lanes[player.lane]-player.x)*Math.min(1,dt*13); spawnTimer-=dt; if(spawnTimer<=0){ spawn(); spawnTimer=Math.max(.18,.72-(GAME_SECONDS-timeLeft)*.008)/SPEC.tuning.spawnRate; } for(const o of objects){ o.y += speed*dt*(o.type===\"nitro\"?.85:1); if(!o.hit&&Math.abs(o.y-player.y)<o.r+player.r&&o.lane===player.lane){ o.hit=true; if(o.type===\"traffic\"){ player.hp--; combo=0; shake=.35; flash=.45; addFloat(\"-\"+S.hp,player.x,player.y-50,palette.bad); } else { combo++; const pts=o.type===\"nitro\"?60:10; score+=Math.round(pts*(1+Math.floor(combo/5))*SPEC.tuning.scoreMultiplier); if(o.type===\"nitro\") speed+=80; addFloat(\"+\"+pts,player.x,player.y-50,palette.ink); burst(player.x,player.y,o.type===\"nitro\"?palette.accent:palette.good,18,180); } } } trim(objects,o=>o.hit||o.y>height+80); shake=Math.max(0,shake-dt*3); flash=Math.max(0,flash-dt*3); updateFx(dt); }\n  function over(){ state=STATE_OVER; best=Math.max(best,score); localStorage.setItem(\"tgg-\"+SPEC.id+\"-best\",String(best)); }\n  function updateFx(dt){ for(const p of particles){p.x+=p.vx*dt;p.y+=p.vy*dt;p.life-=dt;} for(const f of floats){f.y+=f.vy*dt;f.life-=dt;} trim(particles,p=>p.life<=0); trim(floats,f=>f.life<=0); }\n  function render(){ ctx.save(); if(shake>0)ctx.translate((Math.random()-.5)*shake*18,(Math.random()-.5)*shake*18); bg(); road(); objs(); car(); fx(); hud(); if(flash>0){ctx.fillStyle=alpha(palette.bad,flash*.2);ctx.fillRect(0,0,width,height)} if(state!==STATE_PLAYING) overlay(); ctx.restore(); }\n  function bg(){ const g=ctx.createLinearGradient(0,0,0,height); g.addColorStop(0,palette.backgroundA);g.addColorStop(1,palette.backgroundB);ctx.fillStyle=g;ctx.fillRect(0,0,width,height); const v=ctx.createRadialGradient(width/2,height*.45,height*.2,width/2,height*.5,height*.95);v.addColorStop(0,\"rgba(0,0,0,0)\");v.addColorStop(1,\"rgba(0,0,0,.4)\");ctx.fillStyle=v;ctx.fillRect(0,0,width,height); }\n  function road(){ for(let i=0;i<3;i++){ctx.fillStyle=i===player.lane?\"rgba(255,255,255,.14)\":\"rgba(255,255,255,.07)\";ctx.beginPath();ctx.moveTo(lanes[i]-52,height+30);ctx.lineTo(lanes[i]-18,70);ctx.lineTo(lanes[i]+18,70);ctx.lineTo(lanes[i]+52,height+30);ctx.closePath();ctx.fill();} ctx.strokeStyle=\"rgba(255,255,255,.22)\";ctx.lineWidth=4;ctx.setLineDash([26,30]);ctx.lineDashOffset=-roadScroll;for(let i=0;i<3;i++){ctx.beginPath();ctx.moveTo(lanes[i],70);ctx.lineTo(lanes[i],height+30);ctx.stroke();}ctx.setLineDash([]);ctx.lineDashOffset=0; }\n  function objs(){ for(const o of objects){ const x=lanes[o.lane], color=o.type===\"traffic\"?palette.bad:o.type===\"nitro\"?palette.accent:palette.good; ctx.fillStyle=\"rgba(0,0,0,.28)\";ctx.beginPath();ctx.ellipse(x+3,o.y+o.r*.7,o.r,o.r*.5,0,0,Math.PI*2);ctx.fill(); const g=ctx.createRadialGradient(x-o.r*.35,o.y-o.r*.4,o.r*.15,x,o.y,o.r*1.2);g.addColorStop(0,lighten(color,.5));g.addColorStop(1,color); ctx.save();ctx.shadowColor=color;ctx.shadowBlur=14;ctx.fillStyle=g;ctx.beginPath();ctx.roundRect(x-o.r,o.y-o.r,o.r*2,o.r*2,o.type===\"coin\"?o.r:11);ctx.fill();ctx.restore(); ctx.strokeStyle=palette.ink;ctx.lineWidth=2.5;ctx.stroke(); ctx.fillStyle=palette.backgroundA;ctx.font=`900 ${o.r}px system-ui`;ctx.textAlign=\"center\";ctx.textBaseline=\"middle\";ctx.fillText(o.type===\"traffic\"?\"!\":o.type===\"nitro\"?\"⚡\":\"$\",x,o.y); } }\n  function car(){ const x=player.x,y=player.y; ctx.fillStyle=\"rgba(0,0,0,.3)\";ctx.beginPath();ctx.ellipse(x,y+46,32,12,0,0,Math.PI*2);ctx.fill(); ctx.fillStyle=\"#1b2230\";for(const wy of [-26,30]){ctx.beginPath();ctx.roundRect(x-34,y+wy-9,12,30,5);ctx.fill();ctx.beginPath();ctx.roundRect(x+22,y+wy-9,12,30,5);ctx.fill();} const g=ctx.createLinearGradient(x-28,0,x+28,0);g.addColorStop(0,palette.accent);g.addColorStop(.5,lighten(palette.accent,.4));g.addColorStop(1,palette.accent); ctx.save();ctx.shadowColor=palette.accent;ctx.shadowBlur=16;ctx.fillStyle=g;ctx.beginPath();ctx.roundRect(x-28,y-42,56,84,16);ctx.fill();ctx.restore(); ctx.strokeStyle=palette.ink;ctx.lineWidth=4;ctx.stroke(); ctx.fillStyle=alpha(palette.backgroundA,.85);ctx.beginPath();ctx.roundRect(x-18,y-30,36,22,7);ctx.fill();ctx.beginPath();ctx.roundRect(x-18,y+8,36,16,6);ctx.fill(); }\n  function fx(){ for(const p of particles){ctx.globalAlpha=p.life*2;ctx.fillStyle=p.color;ctx.fillRect(p.x-p.size/2,p.y-p.size/2,p.size,p.size)}ctx.globalAlpha=1;for(const f of floats){ctx.globalAlpha=f.life;ctx.fillStyle=f.color;ctx.font=\"900 17px system-ui\";ctx.textAlign=\"center\";ctx.fillText(f.text,f.x,f.y)}ctx.globalAlpha=1; }\n  function hud(){ if(state===STATE_READY)return; text(SPEC.title,18,34,21,palette.ink,\"left\"); text(`${S.score} ${score} ${S.best} ${best}`,18,64,14,palette.ink,\"left\"); text(S.time.replace(\"{n}\",Math.ceil(timeLeft)),width-18,34,23,timeLeft<10?palette.bad:palette.ink,\"right\"); text(`${S.hp} ${player.hp} ${S.combo} ${combo}`,width-18,64,14,palette.ink,\"right\"); }\n  function overlay(){ctx.fillStyle=\"rgba(0,0,0,.68)\";ctx.fillRect(0,0,width,height);text(SPEC.title,width/2,height*.43,36,palette.ink,\"center\");text(SPEC.roles.player+\" / \"+SPEC.roles.objective,width/2,height*.43+40,15,palette.ink,\"center\");text(state===STATE_OVER?S.restart:S.start,width/2,height*.43+92,24,palette.accent,\"center\");if(state===STATE_OVER)text(`${S.score} ${score} ${S.best} ${best}`,width/2,height*.43+128,15,palette.ink,\"center\");}\n  function burst(x,y,color,count,power){for(let i=0;i<count;i++){const a=Math.random()*Math.PI*2,s=30+Math.random()*power;particles.push({x,y,vx:Math.cos(a)*s,vy:Math.sin(a)*s,life:.5,size:4+Math.random()*4,color})}}\n  function addFloat(text,x,y,color){floats.push({text,x,y,vy:-44,life:.9,color})}\n  function text(s,x,y,size,color,align){ctx.fillStyle=color;ctx.font=`900 ${size}px system-ui`;ctx.textAlign=align;ctx.textBaseline=\"middle\";ctx.fillText(s,x,y)}\n  function trim(list,pred){for(let i=list.length-1;i>=0;i--)if(pred(list[i]))list.splice(i,1)}\n  function alpha(hex,a){const n=parseInt(hex.slice(1),16);return`rgba(${n>>16&255},${n>>8&255},${n&255},${a})`}\n  function lighten(hex,t){const n=parseInt(hex.slice(1),16),r=n>>16&255,g=n>>8&255,b=n&255,m=v=>Math.round(v+(255-v)*t);return`rgb(${m(r)},${m(g)},${m(b)})`}\n  function tick(now){if(!last)last=now;const frame=Math.min(.05,(now-last)/1000);last=now;acc+=frame;while(acc>=FIXED_DT){update(FIXED_DT);acc-=FIXED_DT}render();requestAnimationFrame(tick)}\n  resize();requestAnimationFrame(tick);\n})();\n</script>\n</body>\n</html>\n","tile-match.html":"<!doctype html>\n<html lang=\"{{LANG}}\"><head><meta charset=\"utf-8\"><meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\"><title>{{TITLE}}</title><link rel=\"icon\" href=\"data:,\"><style>\nhtml,body{margin:0;width:100%;height:100%;overflow:hidden;background:{{BACKGROUND_A}};color:{{INK}};font-family:system-ui,-apple-system,BlinkMacSystemFont,\"Segoe UI\",sans-serif;touch-action:none;user-select:none}canvas{display:block;width:100vw;height:100vh;touch-action:none}\n</style></head><body>\n<!--\nGAME_SPEC_ID: {{ID}}\nGAME_KIT: tile-match\nSTYLE_KIT: {{STYLE_KIT}}\nMECHANICS: {{MECHANICS}}\nENEMY_SET: {{ENEMY_SET}}\n-->\n<canvas id=\"game\" aria-label=\"{{TITLE}}\"></canvas><script>\n(() => {\n\"use strict\";\nconst SPEC = {{SPEC_JSON}},S=SPEC.strings,canvas=document.getElementById(\"game\"),ctx=canvas.getContext(\"2d\"),READY=\"READY\",PLAYING=\"PLAYING\",OVER=\"OVER\",FIXED_DT=1/60;\nconst p=SPEC.palette,SLOTS=7,SHAPES=[\"circle\",\"square\",\"triangle\",\"diamond\",\"star\",\"hex\"],COLORS=[p.good,p.accent,p.bad,\"#60a5fa\",\"#f59e0b\",\"#a78bfa\"],particles=[],floats=[];\nlet width=0,height=0,dpr=1,state=READY,last=0,acc=0,score=0,best=Number(localStorage.getItem(\"tgg-\"+SPEC.id+\"-best\")||0),timeLeft=SPEC.duration,round=1,shake=0,flash=0,ts=56;\nlet tiles=[],tray=[];\nfunction resize(){dpr=Math.max(1,Math.min(2,devicePixelRatio||1));width=innerWidth;height=innerHeight;canvas.width=Math.floor(width*dpr);canvas.height=Math.floor(height*dpr);canvas.style.width=width+\"px\";canvas.style.height=height+\"px\";ctx.setTransform(dpr,0,0,dpr,0,0);ts=Math.min(64,Math.floor(Math.min(width,height)*.13));if(state!==PLAYING&&tiles.length)layout()}\nfunction reset(){state=PLAYING;score=0;timeLeft=SPEC.duration;round=1;tray=[];particles.length=floats.length=0;layout()}\nfunction layout(){tiles=[];const n=Math.min(54,30+round*6),types=Math.min(6,4+round),per=Math.ceil(n/types/3)*3,pool=[];for(let t=0;t<types;t++)for(let k=0;k<per;k++)pool.push(t);shuffle(pool);const bw=Math.min(width*.78,560),bh=Math.min(height*.48,460),bx=(width-bw)/2,by=Math.max(96,height*.16);for(let i=0;i<pool.length;i++){const layer=i%3;tiles.push({type:pool[i],x:bx+ts/2+Math.random()*(bw-ts),y:by+ts/2+Math.random()*(bh-ts),z:layer*100+i,gone:false})}}\nfunction shuffle(a){for(let i=a.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[a[i],a[j]]=[a[j],a[i]]}}\nfunction covered(t){for(const u of tiles)if(!u.gone&&u.z>t.z&&Math.abs(u.x-t.x)<ts*.86&&Math.abs(u.y-t.y)<ts*.86)return true;return false}\nfunction pointerdown(e){e.preventDefault();if(state===READY||state===OVER){reset();return}if(state!==PLAYING)return;const x=e.clientX,y=e.clientY;let pick=null;for(const t of tiles)if(!t.gone&&Math.abs(x-t.x)<ts/2&&Math.abs(y-t.y)<ts/2&&!covered(t)&&(!pick||t.z>pick.z))pick=t;if(pick)take(pick)}\nfunction pointermove(e){e.preventDefault()}function pointerup(e){e.preventDefault()}\ncanvas.addEventListener(\"pointerdown\",pointerdown);canvas.addEventListener(\"pointermove\",pointermove);canvas.addEventListener(\"pointerup\",pointerup);canvas.addEventListener(\"pointercancel\",pointerup);addEventListener(\"resize\",resize);\nfunction take(t){t.gone=true;tray.push(t.type);burst(t.x,t.y,COLORS[t.type],10);collapse();if(tray.length>SLOTS){over();return}if(tiles.every(x=>x.gone)){round++;score+=60;addFloat(\"+60\",width/2,height*.5,p.accent);layout()}}\nfunction collapse(){let again=true;while(again){again=false;for(let t=0;t<COLORS.length;t++){if(tray.filter(v=>v===t).length>=3){let removed=0;tray=tray.filter(v=>{if(v===t&&removed<3){removed++;return false}return true});score+=Math.round(30*SPEC.tuning.scoreMultiplier);addFloat(\"+30\",width/2,height*.78,p.good);again=true}}}}\nfunction update(dt){if(state!==PLAYING){updateFx(dt);return}timeLeft-=dt;if(timeLeft<=0)over();shake=Math.max(0,shake-dt*3);flash=Math.max(0,flash-dt*3);updateFx(dt)}\nfunction over(){state=OVER;best=Math.max(best,score);localStorage.setItem(\"tgg-\"+SPEC.id+\"-best\",String(best))}\nfunction updateFx(dt){for(const q of particles){q.x+=q.vx*dt;q.y+=q.vy*dt;q.life-=dt}for(const f of floats){f.y-=42*dt;f.life-=dt}trim(particles,q=>q.life<=0);trim(floats,f=>f.life<=0)}\nfunction render(){ctx.save();if(shake)ctx.translate((Math.random()-.5)*shake*14,(Math.random()-.5)*shake*14);bg();drawTiles();drawTray();fx();hud();if(flash){ctx.fillStyle=alpha(p.bad,flash*.22);ctx.fillRect(0,0,width,height)}if(state!==PLAYING)overlay();ctx.restore()}\nfunction bg(){const g=ctx.createLinearGradient(0,0,0,height);g.addColorStop(0,p.backgroundA);g.addColorStop(1,p.backgroundB);ctx.fillStyle=g;ctx.fillRect(0,0,width,height);const v=ctx.createRadialGradient(width/2,height*.45,height*.2,width/2,height*.5,height*.95);v.addColorStop(0,\"rgba(0,0,0,0)\");v.addColorStop(1,\"rgba(0,0,0,.42)\");ctx.fillStyle=v;ctx.fillRect(0,0,width,height)}\nfunction shapePath(shape,cx,cy,r){ctx.beginPath();if(shape===\"circle\"){ctx.arc(cx,cy,r,0,Math.PI*2);return}if(shape===\"square\"){ctx.roundRect(cx-r,cy-r,r*2,r*2,r*.3);return}if(shape===\"star\"){for(let i=0;i<10;i++){const rr=i%2?r*.46:r,a=-Math.PI/2+i*Math.PI/5;const x=cx+Math.cos(a)*rr,y=cy+Math.sin(a)*rr;i?ctx.lineTo(x,y):ctx.moveTo(x,y)}ctx.closePath();return}const nn=shape===\"triangle\"?3:shape===\"diamond\"?4:shape===\"hex\"?6:0;if(!nn){ctx.arc(cx,cy,r,0,Math.PI*2);return}const rot=shape===\"diamond\"?0:-Math.PI/2;for(let i=0;i<nn;i++){const a=rot+i*Math.PI*2/nn,x=cx+Math.cos(a)*r,y=cy+Math.sin(a)*r;i?ctx.lineTo(x,y):ctx.moveTo(x,y)}ctx.closePath()}\nfunction drawSym(cx,cy,type,r){const c=COLORS[type],sh=SHAPES[type];ctx.save();ctx.shadowColor=c;ctx.shadowBlur=8;const g=ctx.createRadialGradient(cx-r*.3,cy-r*.35,r*.1,cx,cy,r*1.1);g.addColorStop(0,lighten(c,.5));g.addColorStop(1,c);shapePath(sh,cx,cy,r);ctx.fillStyle=g;ctx.fill();ctx.restore();ctx.strokeStyle=alpha(p.ink,.3);ctx.lineWidth=2;ctx.stroke()}\nfunction drawTiles(){const sorted=tiles.filter(t=>!t.gone).slice().sort((a,b)=>a.z-b.z);for(const t of sorted){const cov=covered(t);ctx.fillStyle=\"rgba(0,0,0,.3)\";ctx.beginPath();ctx.roundRect(t.x-ts/2+2,t.y-ts/2+3,ts,ts,12);ctx.fill();ctx.fillStyle=cov?alpha(p.ink,.14):alpha(p.ink,.06);ctx.beginPath();ctx.roundRect(t.x-ts/2,t.y-ts/2,ts,ts,12);ctx.fill();ctx.strokeStyle=alpha(p.ink,.2);ctx.lineWidth=1.5;ctx.stroke();ctx.globalAlpha=cov?.5:1;drawSym(t.x,t.y,t.type,ts*.3);ctx.globalAlpha=1}}\nfunction drawTray(){const tw=Math.min(width*.92,460),sw=tw/SLOTS,tx=(width-tw)/2,ty=height-sw-26;ctx.fillStyle=alpha(p.ink,.08);ctx.beginPath();ctx.roundRect(tx-6,ty-6,tw+12,sw+12,14);ctx.fill();for(let i=0;i<SLOTS;i++){ctx.strokeStyle=alpha(p.ink,.2);ctx.lineWidth=1.5;ctx.beginPath();ctx.roundRect(tx+i*sw+3,ty+3,sw-6,sw-6,10);ctx.stroke()}for(let i=0;i<tray.length;i++)drawSym(tx+i*sw+sw/2,ty+sw/2,tray[i],sw*.3)}\nfunction fx(){for(const q of particles){ctx.globalAlpha=Math.max(0,q.life*2);ctx.fillStyle=q.color;ctx.fillRect(q.x,q.y,q.size,q.size)}ctx.globalAlpha=1;for(const f of floats){ctx.globalAlpha=Math.max(0,f.life);text(f.text,f.x,f.y,16,f.color,\"center\")}ctx.globalAlpha=1}\nfunction hud(){if(state===READY)return;text(SPEC.title,20,30,21,p.ink,\"left\");text(S.score+\" \"+score+\"  \"+S.best+\" \"+best,20,58,13,p.ink,\"left\");text(S.time.replace(\"{n}\",Math.ceil(timeLeft)),width-20,32,22,timeLeft<10?p.bad:p.ink,\"right\")}\nfunction overlay(){ctx.fillStyle=\"rgba(0,0,0,.66)\";ctx.fillRect(0,0,width,height);text(SPEC.title,width/2,height*.4,34,p.ink,\"center\");text(SPEC.roles.objective,width/2,height*.4+40,15,p.ink,\"center\");text(state===OVER?S.restart:S.start,width/2,height*.4+92,24,p.accent,\"center\");if(state===OVER)text(S.score+\" \"+score+\"  \"+S.best+\" \"+best,width/2,height*.4+128,15,p.ink,\"center\")}\nfunction burst(x,y,c,n){for(let i=0;i<n;i++){const a=Math.random()*Math.PI*2,s=40+Math.random()*110;particles.push({x,y,vx:Math.cos(a)*s,vy:Math.sin(a)*s,life:.5,size:3+Math.random()*5,color:c})}}\nfunction addFloat(t,x,y,c){floats.push({text:t,x,y,life:.9,color:c})}\nfunction trim(a,pred){for(let i=a.length-1;i>=0;i--)if(pred(a[i]))a.splice(i,1)}\nfunction alpha(hex,a){const n=parseInt(hex.slice(1),16);return`rgba(${n>>16&255},${n>>8&255},${n&255},${a})`}\nfunction lighten(hex,t){const n=parseInt(hex.slice(1),16),r=n>>16&255,g=n>>8&255,b=n&255,m=v=>Math.round(v+(255-v)*t);return`rgb(${m(r)},${m(g)},${m(b)})`}\nfunction text(t,x,y,s,c,a){ctx.fillStyle=c;ctx.font=`900 ${s}px system-ui`;ctx.textAlign=a;ctx.textBaseline=\"middle\";ctx.fillText(t,x,y)}\nfunction tick(now){if(!last)last=now;const frame=Math.min(.05,(now-last)/1000);last=now;acc+=frame;while(acc>=FIXED_DT){update(FIXED_DT);acc-=FIXED_DT}render();requestAnimationFrame(tick)}\nresize();requestAnimationFrame(tick);\n})();\n</script></body></html>\n","tower-defense.html":"<!doctype html>\n<html lang=\"{{LANG}}\">\n<head>\n<meta charset=\"utf-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>{{TITLE}}</title>\n<link rel=\"icon\" href=\"data:,\">\n<style>\nhtml,body{margin:0;width:100%;height:100%;overflow:hidden;background:{{BACKGROUND_A}};color:{{INK}};font-family:system-ui,-apple-system,BlinkMacSystemFont,\"Segoe UI\",sans-serif;touch-action:none;user-select:none}\ncanvas{display:block;width:100vw;height:100vh;touch-action:none}\n</style>\n</head>\n<body>\n<!--\nGAME_SPEC_ID: {{ID}}\nGAME_KIT: tower-defense\nSTYLE_KIT: {{STYLE_KIT}}\nMECHANICS: {{MECHANICS}}\nENEMY_SET: {{ENEMY_SET}}\n-->\n<canvas id=\"game\" aria-label=\"{{TITLE}}\"></canvas>\n<script>\n(() => {\n  \"use strict\";\n  const SPEC = {{SPEC_JSON}};\n  const S = SPEC.strings;\n  const canvas = document.getElementById(\"game\");\n  const ctx = canvas.getContext(\"2d\");\n  const STATE_READY = \"READY\", STATE_PLAYING = \"PLAYING\", STATE_OVER = \"OVER\";\n  const FIXED_DT = 1 / 60;\n  let width = 0, height = 0, dpr = 1, state = STATE_READY, last = 0, acc = 0;\n  let score = 0, best = Number(localStorage.getItem(\"tgg-\" + SPEC.id + \"-best\") || 0), timeLeft = SPEC.duration;\n  let lives = 10, coins = 55, spawnTimer = 0, wave = 1, shake = 0, flash = 0, hoverNode = null;\n  const palette = SPEC.palette, lanes = [], nodes = [], towers = [], enemies = [], bullets = [], particles = [], floats = [];\n\n  function resize() {\n    dpr = Math.max(1, Math.min(2, devicePixelRatio || 1));\n    width = innerWidth; height = innerHeight;\n    canvas.width = Math.floor(width * dpr); canvas.height = Math.floor(height * dpr);\n    canvas.style.width = width + \"px\"; canvas.style.height = height + \"px\";\n    ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n    layout();\n  }\n\n  function layout() {\n    lanes.length = 0; nodes.length = 0;\n    const top = Math.max(120, height * 0.24);\n    const gap = Math.max(82, height * 0.17);\n    for (let i = 0; i < 3; i += 1) lanes.push(top + i * gap);\n    const cols = [0.24, 0.42, 0.60, 0.78];\n    for (const lane of lanes) for (const c of cols) nodes.push({ x: width * c, y: lane - 38, r: 22, tower: null });\n  }\n\n  function reset() {\n    state = STATE_PLAYING; score = 0; timeLeft = SPEC.duration; lives = 10; coins = 55; spawnTimer = 0.3; wave = 1;\n    towers.length = enemies.length = bullets.length = particles.length = floats.length = 0;\n    for (const node of nodes) node.tower = null;\n  }\n\n  function pointerdown(event) {\n    event.preventDefault();\n    if (state === STATE_READY || state === STATE_OVER) { reset(); return; }\n    if (state !== STATE_PLAYING) return;\n    const node = nearestNode(event.clientX, event.clientY);\n    if (node && !node.tower && coins >= 20) {\n      const tower = { x: node.x, y: node.y, r: 19, range: 150, cooldown: 0, level: 1 };\n      node.tower = tower; towers.push(tower); coins -= 20;\n      addFloat(\"-20\", tower.x, tower.y - 28, palette.accent);\n      burst(tower.x, tower.y, palette.good, 14, 90);\n    } else if (node && node.tower && coins >= 15) {\n      node.tower.level += 1; node.tower.range += 12; coins -= 15;\n      addFloat(S.up, node.x, node.y - 28, palette.accent);\n      burst(node.x, node.y, palette.accent, 12, 80);\n    } else {\n      flash = 0.18;\n    }\n  }\n\n  function pointermove(event) {\n    event.preventDefault();\n    hoverNode = nearestNode(event.clientX, event.clientY);\n  }\n\n  function pointerup(event) { event.preventDefault(); }\n  canvas.addEventListener(\"pointerdown\", pointerdown);\n  canvas.addEventListener(\"pointermove\", pointermove);\n  canvas.addEventListener(\"pointerup\", pointerup);\n  canvas.addEventListener(\"pointercancel\", pointerup);\n  addEventListener(\"resize\", resize);\n\n  function update(dt) {\n    if (state !== STATE_PLAYING) { updateFx(dt); return; }\n    timeLeft -= dt;\n    wave = 1 + Math.floor((SPEC.duration - timeLeft) / 15);\n    if (timeLeft <= 0 || lives <= 0) return over();\n    spawnTimer -= dt;\n    if (spawnTimer <= 0) {\n      spawnEnemy();\n      spawnTimer = Math.max(0.34, (1.25 - wave * 0.12) / SPEC.tuning.spawnRate);\n    }\n    for (const enemy of enemies) {\n      enemy.x += enemy.speed * dt * SPEC.tuning.speed;\n      if (!enemy.done && enemy.x > width + 42) {\n        enemy.done = true; lives -= enemy.kind === \"tank\" ? 2 : 1; shake = 0.26; flash = 0.35;\n      }\n    }\n    for (const tower of towers) {\n      tower.cooldown -= dt;\n      if (tower.cooldown <= 0) {\n        const target = enemies.filter(e => !e.done && e.hp > 0).sort((a, b) => dist(tower, a) - dist(tower, b))[0];\n        if (target && dist(tower, target) <= tower.range) {\n          tower.cooldown = Math.max(0.22, 0.78 - tower.level * 0.08);\n          bullets.push({ x: tower.x, y: tower.y, target, speed: 520, damage: 1 + tower.level });\n        }\n      }\n    }\n    for (const bullet of bullets) {\n      if (!bullet.target || bullet.target.done || bullet.target.hp <= 0) { bullet.done = true; continue; }\n      const dx = bullet.target.x - bullet.x, dy = bullet.target.y - bullet.y, d = Math.max(1, Math.hypot(dx, dy));\n      bullet.x += dx / d * bullet.speed * dt; bullet.y += dy / d * bullet.speed * dt;\n      if (d < 18) {\n        bullet.done = true; bullet.target.hp -= bullet.damage;\n        burst(bullet.target.x, bullet.target.y, palette.accent, 5, 55);\n        if (bullet.target.hp <= 0) {\n          bullet.target.done = true; const gain = bullet.target.kind === \"tank\" ? 18 : 10;\n          coins += 8; score += Math.round(gain * SPEC.tuning.scoreMultiplier); addFloat(\"+\" + gain, bullet.target.x, bullet.target.y, palette.good);\n        }\n      }\n    }\n    trim(enemies, e => e.done); trim(bullets, b => b.done);\n    shake = Math.max(0, shake - dt * 3); flash = Math.max(0, flash - dt * 3);\n    updateFx(dt);\n  }\n\n  function over() {\n    state = STATE_OVER;\n    best = Math.max(best, score);\n    localStorage.setItem(\"tgg-\" + SPEC.id + \"-best\", String(best));\n  }\n\n  function spawnEnemy() {\n    const lane = lanes[Math.floor(Math.random() * lanes.length)];\n    const roll = Math.random();\n    const kind = roll < 0.18 ? \"tank\" : roll < 0.42 ? \"swarm\" : \"runner\";\n    enemies.push({\n      x: -40, y: lane, kind,\n      hp: kind === \"tank\" ? 8 + wave * 2 : kind === \"swarm\" ? 3 + wave : 4 + wave,\n      maxHp: kind === \"tank\" ? 8 + wave * 2 : kind === \"swarm\" ? 3 + wave : 4 + wave,\n      speed: kind === \"tank\" ? 34 + wave * 3 : kind === \"swarm\" ? 72 + wave * 5 : 54 + wave * 4,\n      done: false\n    });\n  }\n\n  function updateFx(dt) {\n    for (const p of particles) { p.x += p.vx * dt; p.y += p.vy * dt; p.life -= dt; }\n    for (const f of floats) { f.y -= 34 * dt; f.life -= dt; }\n    trim(particles, p => p.life <= 0); trim(floats, f => f.life <= 0);\n  }\n\n  function render() {\n    ctx.save();\n    if (shake) ctx.translate((Math.random() - 0.5) * shake * 18, (Math.random() - 0.5) * shake * 18);\n    background(); map(); drawNodes(); drawTowers(); drawEnemies(); drawBullets(); drawFx(); hud();\n    if (flash) { ctx.fillStyle = alpha(palette.bad, flash * 0.24); ctx.fillRect(0, 0, width, height); }\n    if (state !== STATE_PLAYING) overlay();\n    ctx.restore();\n  }\n\n  function background() {\n    const g = ctx.createLinearGradient(0, 0, 0, height);\n    g.addColorStop(0, palette.backgroundA); g.addColorStop(1, palette.backgroundB);\n    ctx.fillStyle = g; ctx.fillRect(0, 0, width, height);\n    ctx.strokeStyle = \"rgba(255,255,255,.08)\";\n    for (let x = 0; x < width; x += 44) { ctx.beginPath(); ctx.moveTo(x, 0); ctx.lineTo(x, height); ctx.stroke(); }\n    const v = ctx.createRadialGradient(width / 2, height * 0.42, height * 0.2, width / 2, height * 0.5, height * 0.95);\n    v.addColorStop(0, \"rgba(0,0,0,0)\"); v.addColorStop(1, \"rgba(0,0,0,.42)\");\n    ctx.fillStyle = v; ctx.fillRect(0, 0, width, height);\n  }\n\n  function map() {\n    for (const lane of lanes) {\n      ctx.strokeStyle = alpha(palette.ink, 0.18); ctx.lineWidth = 34; ctx.lineCap = \"round\";\n      ctx.beginPath(); ctx.moveTo(-30, lane); ctx.lineTo(width + 30, lane); ctx.stroke();\n      ctx.strokeStyle = alpha(palette.accent, 0.52); ctx.lineWidth = 3;\n      ctx.beginPath(); ctx.moveTo(0, lane); ctx.lineTo(width, lane); ctx.stroke();\n    }\n    ctx.fillStyle = alpha(palette.bad, 0.24); ctx.fillRect(width - 22, lanes[0] - 48, 12, lanes[2] - lanes[0] + 96);\n  }\n\n  function drawNodes() {\n    for (const node of nodes) {\n      ctx.fillStyle = node === hoverNode ? alpha(palette.accent, 0.36) : alpha(palette.ink, 0.08);\n      ctx.beginPath(); ctx.roundRect(node.x - 24, node.y - 24, 48, 48, 12); ctx.fill();\n      ctx.strokeStyle = node.tower ? palette.good : alpha(palette.ink, 0.32); ctx.lineWidth = 2; ctx.stroke();\n    }\n    if (hoverNode && hoverNode.tower) {\n      ctx.strokeStyle = alpha(palette.accent, 0.5); ctx.lineWidth = 2; ctx.setLineDash([7, 7]);\n      ctx.beginPath(); ctx.arc(hoverNode.tower.x, hoverNode.tower.y, hoverNode.tower.range, 0, Math.PI * 2); ctx.stroke();\n      ctx.setLineDash([]);\n    }\n  }\n\n  function drawTowers() {\n    for (const tower of towers) {\n      ctx.fillStyle = \"rgba(0,0,0,.3)\"; ctx.beginPath(); ctx.ellipse(tower.x + 2, tower.y + tower.r * 0.7, tower.r, tower.r * 0.5, 0, 0, Math.PI * 2); ctx.fill();\n      const g = ctx.createRadialGradient(tower.x - tower.r * 0.4, tower.y - tower.r * 0.4, tower.r * 0.15, tower.x, tower.y, tower.r * 1.2);\n      g.addColorStop(0, lighten(palette.good, 0.55)); g.addColorStop(1, palette.good);\n      ctx.save(); ctx.shadowColor = palette.good; ctx.shadowBlur = 14; ctx.fillStyle = g;\n      ctx.beginPath(); ctx.arc(tower.x, tower.y, tower.r, 0, Math.PI * 2); ctx.fill(); ctx.restore();\n      ctx.strokeStyle = palette.ink; ctx.lineWidth = 3; ctx.stroke();\n      text(String(tower.level), tower.x, tower.y, 14, palette.backgroundA, \"center\");\n    }\n  }\n\n  function drawEnemies() {\n    for (const e of enemies) {\n      const col = e.kind === \"tank\" ? palette.bad : e.kind === \"swarm\" ? palette.accent : palette.ink, R = 18;\n      ctx.fillStyle = \"rgba(0,0,0,.28)\"; ctx.beginPath(); ctx.ellipse(e.x + 3, e.y + R * 0.7, R, R * 0.5, 0, 0, Math.PI * 2); ctx.fill();\n      const g = ctx.createRadialGradient(e.x - R * 0.4, e.y - R * 0.4, R * 0.15, e.x, e.y, R * 1.2);\n      g.addColorStop(0, lighten(col, 0.5)); g.addColorStop(1, col);\n      ctx.save(); ctx.shadowColor = col; ctx.shadowBlur = 12; ctx.fillStyle = g; ctx.beginPath();\n      if (e.kind === \"tank\") { ctx.roundRect(e.x - R, e.y - R, R * 2, R * 2, 7); }\n      else if (e.kind === \"swarm\") { ctx.arc(e.x, e.y, R * 0.85, 0, Math.PI * 2); }\n      else { ctx.moveTo(e.x, e.y - R); ctx.lineTo(e.x + R, e.y); ctx.lineTo(e.x, e.y + R); ctx.lineTo(e.x - R, e.y); ctx.closePath(); }\n      ctx.fill(); ctx.restore();\n      ctx.strokeStyle = palette.ink; ctx.lineWidth = 2.5; ctx.stroke();\n      ctx.fillStyle = alpha(palette.backgroundA, 0.9); ctx.fillRect(e.x - 18, e.y - 28, 36, 5);\n      ctx.fillStyle = palette.good; ctx.fillRect(e.x - 18, e.y - 28, 36 * Math.max(0, e.hp / e.maxHp), 5);\n    }\n  }\n\n  function drawBullets() {\n    ctx.save(); ctx.shadowColor = palette.accent; ctx.shadowBlur = 10; ctx.fillStyle = palette.accent;\n    for (const b of bullets) { ctx.beginPath(); ctx.arc(b.x, b.y, 5, 0, Math.PI * 2); ctx.fill(); }\n    ctx.restore();\n  }\n\n  function drawFx() {\n    for (const p of particles) { ctx.globalAlpha = Math.max(0, p.life * 2); ctx.fillStyle = p.color; ctx.fillRect(p.x, p.y, p.size, p.size); }\n    ctx.globalAlpha = 1;\n    for (const f of floats) { ctx.globalAlpha = Math.max(0, f.life); text(f.text, f.x, f.y, 16, f.color, \"center\"); }\n    ctx.globalAlpha = 1;\n  }\n\n  function hud() {\n    if (state === STATE_READY) return;\n    text(SPEC.title, 20, 30, 22, palette.ink, \"left\");\n    text(S.score + \" \" + score + \"  \" + S.best + \" \" + best, 20, 58, 13, palette.ink, \"left\");\n    text(S.coins + \" \" + coins + \"  \" + S.lives + \" \" + lives + \"  \" + S.wave + \" \" + wave, 20, 84, 13, palette.ink, \"left\");\n    text(S.time.replace(\"{n}\", Math.ceil(timeLeft)), width - 20, 32, 23, timeLeft < 10 ? palette.bad : palette.ink, \"right\");\n  }\n\n  function overlay() {\n    ctx.fillStyle = \"rgba(0,0,0,.68)\"; ctx.fillRect(0, 0, width, height);\n    text(SPEC.title, width / 2, height * 0.42, 36, palette.ink, \"center\");\n    text(SPEC.roles.objective, width / 2, height * 0.42 + 42, 15, palette.ink, \"center\");\n    text(state === STATE_OVER ? S.restart : S.start, width / 2, height * 0.42 + 94, 25, palette.accent, \"center\");\n    if (state === STATE_OVER) text(S.score + \" \" + score + \"  \" + S.best + \" \" + best, width / 2, height * 0.42 + 130, 15, palette.ink, \"center\");\n  }\n\n  function nearestNode(x, y) {\n    let bestNode = null, bestDist = 9999;\n    for (const node of nodes) {\n      const d = Math.hypot(x - node.x, y - node.y);\n      if (d < 36 && d < bestDist) { bestDist = d; bestNode = node; }\n    }\n    return bestNode;\n  }\n\n  function burst(x, y, color, count, power) {\n    for (let i = 0; i < count; i += 1) {\n      const a = Math.random() * Math.PI * 2, s = Math.random() * power;\n      particles.push({ x, y, vx: Math.cos(a) * s, vy: Math.sin(a) * s, life: 0.5, size: 3 + Math.random() * 4, color });\n    }\n  }\n\n  function addFloat(textValue, x, y, color) { floats.push({ text: textValue, x, y, color, life: 0.9 }); }\n  function dist(a, b) { return Math.hypot(a.x - b.x, a.y - b.y); }\n  function trim(list, pred) { for (let i = list.length - 1; i >= 0; i -= 1) if (pred(list[i])) list.splice(i, 1); }\n  function text(value, x, y, size, color, align) { ctx.fillStyle = color; ctx.font = \"900 \" + size + \"px system-ui\"; ctx.textAlign = align; ctx.textBaseline = \"middle\"; ctx.fillText(value, x, y); }\n  function alpha(hex, a) { const n = parseInt(hex.slice(1), 16); return \"rgba(\" + ((n >> 16) & 255) + \",\" + ((n >> 8) & 255) + \",\" + (n & 255) + \",\" + a + \")\"; }\n  function lighten(hex, t) { const n = parseInt(hex.slice(1), 16); const r = (n >> 16) & 255, g = (n >> 8) & 255, b = n & 255; const m = v => Math.round(v + (255 - v) * t); return \"rgb(\" + m(r) + \",\" + m(g) + \",\" + m(b) + \")\"; }\n  function tick(now) { if (!last) last = now; const frame = Math.min(0.05, (now - last) / 1000); last = now; acc += frame; while (acc >= FIXED_DT) { update(FIXED_DT); acc -= FIXED_DT; } render(); requestAnimationFrame(tick); }\n\n  resize();\n  requestAnimationFrame(tick);\n})();\n</script>\n</body>\n</html>\n"}};
